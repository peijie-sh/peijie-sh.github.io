<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Maven学习笔记（4） --- 聚合和继承</title>
    <url>/2018/10/09/Maven%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%EF%BC%884%EF%BC%89%20---%20%E8%81%9A%E5%90%88%E5%92%8C%E7%BB%A7%E6%89%BF/</url>
    <content><![CDATA[<p>最近重新读了一下《Maven实战》，记录一下一直被我忽略的<code>聚合</code>和<code>继承</code>的关系。</p>
<h1 id="聚合"><a href="#聚合" class="headerlink" title="聚合"></a>聚合</h1><p>首先，一个模块有一个pom（Project object Model）文件。当一个项目比较大的时候，按一定规则划分成不同的模块是一个比较好的方式。这时候管理一个项目，如果每个模块座位一个项目，那么打包编译的时候需要到每一个模块下去执行mvn命令，太麻烦了。于是，有个继承模块。</p>
<p>继承模块是一个空模块，只有pom文件，没有src、resource之类的目录结构。在pom文件中，用<code>&lt;modules&gt;</code>和<code>&lt;module&gt;</code>标签指定要管理的子模块。另外，<code>&lt;packaging&gt;</code>标签必须指定为pom。</p>
<ul>
<li>e.g：<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">packaging</span>&gt;</span>pom<span class="tag">&lt;/<span class="name">packaging</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">modules</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">module</span>&gt;</span>account-model<span class="tag">&lt;/<span class="name">module</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">module</span>&gt;</span>account-service<span class="tag">&lt;/<span class="name">module</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">module</span>&gt;</span>account-task<span class="tag">&lt;/<span class="name">module</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">modules</span>&gt;</span></span><br></pre></td></tr></table></figure>
以上情况适用于聚合模块处于目录顶层，子模块是聚合模块的子目录的情况。如果子模块和聚合模块是平行的目录关系，那么应该根据实际路径来写。</li>
</ul>
<a id="more"></a>
<ul>
<li>e.g：<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">packaging</span>&gt;</span>pom<span class="tag">&lt;/<span class="name">packaging</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">modules</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">module</span>&gt;</span>../account-model<span class="tag">&lt;/<span class="name">module</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">module</span>&gt;</span>../account-service<span class="tag">&lt;/<span class="name">module</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">module</span>&gt;</span>../account-task<span class="tag">&lt;/<span class="name">module</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">modules</span>&gt;</span></span><br></pre></td></tr></table></figure>
这样，一个聚合模块就做好了，打包编译只需要在聚合模块下执行命令即可。</li>
</ul>
<h1 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h1><p>当多个子模块需要的依赖相同和重复时，就推荐使用继承来消除重复了。声明一个可被继承的父模块，也是要把<code>&lt;packaging&gt;</code>标签指定为pom。子模块中指定父模块的坐标</p>
<ul>
<li>e.g：<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.demo<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>account-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<p>父模块中可以声明<code>&lt;dependencyManagement&gt;</code>，这个标签不会真的引入依赖，只是定义公用的坐标，子模块继承后仍然需要指定<code>&lt;groupId&gt;</code>和<code>&lt;artifactId&gt;</code>，可省略<code>&lt;version&gt;</code>标签。这么做看起来没节省多少工作，但是能有效管理依赖，避免项目出现不同版本的依赖问题。</p>
<p>如果父模块和子模块是平行目录，那么子模块中需要指定父模块的地址。</p>
<ul>
<li>e.g：<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.demo<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>account-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">relativePath</span>&gt;</span>../account-parent/pom.xml<span class="tag">&lt;/<span class="name">relativePath</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br></pre></td></tr></table></figure>
Maven默认是从上级目录解析父模块，所以如果子模块在父模块的下级目录，可省略<code>&lt;relativePath&gt;</code>。</li>
</ul>
<p><strong>一般推荐顶层目录中声明模块，它即是聚合模块，也是可被继承的父模块</strong></p>
<h1 id="import依赖范围"><a href="#import依赖范围" class="headerlink" title="import依赖范围"></a>import依赖范围</h1><p>一直不知道import依赖范围的作用，这里记录一下。</p>
<p>这个依赖范围，只在<code>&lt;dependencyManagement&gt;</code>元素下才有效。<br>它的作用是指向一个pom，将目标pom中的<code>&lt;dependencyManagement&gt;</code>配置导入并且合并到当前pom的<code>&lt;dependencyManagement&gt;</code>中。<br>也就是说，一个新模块的<code>&lt;dependencyManagement&gt;</code>中，有一堆配置在其他某个pom中的<code>&lt;dependencyManagement&gt;</code>里配置过了，此处就不必再复制粘贴一遍，可以直接使用import引入进来。</p>
<ul>
<li>e.g：有一个现成的a-parent模块配置了<code>&lt;dependencyManagement&gt;</code>，在b-parent模块中就不必拷贝大段配置了<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependencyManagement</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">              <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.demo<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">              <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>a-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">              <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">              <span class="tag">&lt;<span class="name">type</span>&gt;</span>pom<span class="tag">&lt;/<span class="name">type</span>&gt;</span></span><br><span class="line">              <span class="tag">&lt;<span class="name">scope</span>&gt;</span>import<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span>        </span><br><span class="line"><span class="tag">&lt;/<span class="name">dependencyManagement</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Maven</tag>
      </tags>
  </entry>
  <entry>
    <title>JDK默认使用的垃圾回收器</title>
    <url>/2018/09/09/JDK%E9%BB%98%E8%AE%A4%E4%BD%BF%E7%94%A8%E7%9A%84%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6%E5%99%A8/</url>
    <content><![CDATA[<h1 id="查看方法"><a href="#查看方法" class="headerlink" title="查看方法"></a>查看方法</h1><blockquote>
<p>java -XX:+PrintCommandLineFlags -version</p>
</blockquote>
<p><img src="https://user-gold-cdn.xitu.io/2018/9/10/165c281fa5a86eca?w=959&h=128&f=png&s=15930" alt="这里写图片描述"> </p>
<a id="more"></a>
<p>引用《深入理解Java虚拟机：JVM高级特性与最佳实践》的介绍：<br><img src="https://user-gold-cdn.xitu.io/2018/9/10/165c281fa572a1b9?w=994&h=390&f=png&s=173349" alt="这里写图片描述"></p>
<p>所以，jdk8环境下，默认使用 Parallel Scavenge（新生代）+ Serial Old（老年代）</p>
<ul>
<li>-XX:+PrintCommandLineFlagsjvm参数可查看默认设置收集器类型</li>
<li>-XX:+PrintGCDetails亦可通过打印的GC日志的新生代、老年代名称判断</li>
</ul>
]]></content>
      <categories>
        <category>日常记录</category>
      </categories>
      <tags>
        <tag>垃圾回收器</tag>
      </tags>
  </entry>
  <entry>
    <title>理解JVM（七）：垃圾回收器</title>
    <url>/2018/06/29/%E7%90%86%E8%A7%A3JVM%EF%BC%88%E4%B8%83%EF%BC%89%EF%BC%9A%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6%E5%99%A8/</url>
    <content><![CDATA[<h1 id="一些概念"><a href="#一些概念" class="headerlink" title="一些概念"></a>一些概念</h1><h2 id="并行（Parallel）"><a href="#并行（Parallel）" class="headerlink" title="并行（Parallel）"></a>并行（Parallel）</h2><p>指多条垃圾收集线程并行工作，但此时用户线程仍然处于等待状态。</p>
<h2 id="并发（Concurrent）"><a href="#并发（Concurrent）" class="headerlink" title="并发（Concurrent）"></a>并发（Concurrent）</h2><p>指用户线程与垃圾收集线程同时执行（但不一定是并行的，可能会交替执行），用户程序在继续运行，而垃圾收集程序运行于另一个CPU上。</p>
<h2 id="吞吐量"><a href="#吞吐量" class="headerlink" title="吞吐量"></a>吞吐量</h2><p>CPU用于运行用户代码的时间与CPU总消耗时间的比值，即吞吐量=运行用户代码时间/（运行用户代码时间+垃圾收集时间）。虚拟机总共运行了100分钟，其中垃圾收集花掉1分钟，那吞吐量就是99%。</p>
<a id="more"></a>
<h1 id="HotSpot虚拟机的垃圾回收器"><a href="#HotSpot虚拟机的垃圾回收器" class="headerlink" title="HotSpot虚拟机的垃圾回收器"></a>HotSpot虚拟机的垃圾回收器</h1><h2 id="Serial"><a href="#Serial" class="headerlink" title="Serial"></a>Serial</h2><ul>
<li>最基本的单线程垃圾收集器。使用一个CPU或一条收集线程去执行垃圾收集工作。</li>
<li>工作时会<code>Stop The World</code>，暂停所有用户线程，造成卡顿。适合运行在Client模式下的虚拟机。</li>
<li>用作新生代收集器，复制算法。</li>
</ul>
<h2 id="ParNew"><a href="#ParNew" class="headerlink" title="ParNew"></a>ParNew</h2><ul>
<li>Serial收集器的多线程版本，和Serial的唯一区别就是使用了多条线程去垃圾收集。</li>
<li>除了Serial，只有它可以和CMS搭配使用的收集器。</li>
<li>用作新生代收集器，复制算法。</li>
</ul>
<h2 id="Parallel-Scavenge"><a href="#Parallel-Scavenge" class="headerlink" title="Parallel Scavenge"></a>Parallel Scavenge</h2><ul>
<li>用作新生代收集器，复制算法。</li>
<li>关注高吞吐量，可以高效率地利用CPU时间，尽快完成程序的运算任务，主要适合在后台运算而不需要太多交互的任务。</li>
<li>Parallel Scavenge收集器提供了两个参数用于精确控制吞吐量，分别是控制最大垃圾收集停顿时间的<code>-XX：MaxGCPauseMillis</code>参数以及直接设置吞吐量大小的<code>-XX：GCTimeRatio</code>参数。</li>
</ul>
<h2 id="Serial-Old"><a href="#Serial-Old" class="headerlink" title="Serial Old"></a>Serial Old</h2><ul>
<li>Serial收集器的老年代版本，单线程，标记-整理 算法。</li>
<li>一般用于Client模式的虚拟机。</li>
<li>当虚拟机是Server模式时，有2个用途：一种用途是在JDK 1.5以及之前的版本中与Parallel Scavenge收集器搭配使用 ，另一种用途就是作为CMS收集器的后备预案，在并发收集发生Concurrent Mode Failure时使用。</li>
</ul>
<h2 id="Parallel-Old"><a href="#Parallel-Old" class="headerlink" title="Parallel Old"></a>Parallel Old</h2><ul>
<li>Parallel Scavenge收集器的老年代版本，使用多线程和 标记-整理 算法。在JDK 1.6中开始提供。</li>
<li>在注重吞吐量的场合，配合Parallel Scavenge收集器使用。</li>
</ul>
<h2 id="CMS（Concurrent-Mark-Sweep）"><a href="#CMS（Concurrent-Mark-Sweep）" class="headerlink" title="CMS（Concurrent Mark Sweep）"></a>CMS（Concurrent Mark Sweep）</h2><ul>
<li>一种以获取最短回收停顿时间为目标的收集器。适合需要与用户交互的程序，良好的响应速度能提升用户体验。</li>
<li>基于 标记—清除 算法。适合作为老年代收集器。</li>
<li>收集过程分4步：<ul>
<li>初始标记（CMS initial mark）：只是标记一下GC Roots能直接关联到的对象，速度很快，会<code>Stop The World</code>。</li>
<li>并发标记（CMS concurrent mark）：进行GC Roots Tracing（可达性分析）的过程。</li>
<li>重新标记（CMS remark）：会<code>Stop The World</code>。为了修正并发标记期间因用户程序继续运作而导致标记产生变动的那一部分对象的标记记录，这个阶段的停顿时间一般比初始标记阶段稍长些，但远比并发标记的时间短。</li>
<li>并发清除（CMS concurrent sweep）：回收内存。</li>
</ul>
</li>
<li>耗时最长的并发标记和并发清除过程收集器线程都可以与用户线程一起工作，所以是并发执行的。</li>
<li>缺点：<ul>
<li>并发阶段，虽然不会导致用户线程暂停，但会占用一部分线程（CPU资源），导致应用变慢，吞吐量降低。默认启动收集线程数是（CPU数量+3）/4。即当CPU在4个以上时，并发回收时垃圾收集线程不少于25%的CPU资源，并且随着CPU数量的增加而下降。但是当CPU不足4个（譬如2个）时，CMS对用户程序的影响就可能变得很大。</li>
<li>无法清除浮动垃圾。并发清除阶段，用户线程还在运行，还会产生新垃圾。这些垃圾不会在此次GC中被标记，只能等到下次GC被回收。</li>
<li>标记-清除 算法会产生大量不连续内存，导致分配大对象时内存不够，提前触发Full GC。</li>
</ul>
</li>
</ul>
<h2 id="G1"><a href="#G1" class="headerlink" title="G1"></a>G1</h2><ul>
<li>在JDK1.7提供的先进垃圾收集器。</li>
<li>既适合新生代，也适合老年代。</li>
<li>空间整合：使用 标记-整理 算法，不产生碎片空间。</li>
<li>整个Java堆被分为多个大小相同的的块（region）。新生代和老年代不再是物理隔离的，而是一部分region块组成的集合。</li>
<li>默认把堆平均分成2048个region，最小1M，最大32M，必须是2的幂次方，可以通过<code>-XX:G1HeapRegionSize</code>参数指定。region分为4种：<ul>
<li>E：eden区，新生代</li>
<li>S：survivor区，新生代</li>
<li>O：old区，老年代</li>
<li>H：humongous区，用来放大对象。当新建对象大小超过region大小一半时，直接在新的一个或多个连续region中分配，并标记为H</li>
</ul>
</li>
<li>可预测的停顿时间：估算每个region内的垃圾可回收的空间以及回收需要的时间（经验值），记录在一个优先列表中。收集时，优先回收价值最大的region，而不是在整个堆进行全区域回收。这样提高了回收效率，得名：Garbage-First。</li>
<li>G1中有2种GC：<ul>
<li>young GC：新生代eden区没有足够可用空间时触发。存活的对象移到survivor区或晋升old区。</li>
<li>mixed GC：当old区对象很多时，老年代对象空间占堆总空间的比值达到阈值（<code>-XX:InitiatingHeapOccupancyPercent</code>默认45%）会触发，它除了回收年轻代，也回收 部分 老年代（回收价值高的部分region）。</li>
</ul>
</li>
<li>mixed GC回收步骤：<ul>
<li>初始标记（Initial Marking）：只是标记一下GC Roots能直接关联到的对象，并且修改TAMS（Next Top at Mark Start）的值，让下一阶段用户程序并发运行时，能在正确可用的Region中创建新对象。这阶段需要停顿线程（STW），但耗时很短，共用YGC的停顿，所以一般伴随着YGC发生。</li>
<li>并发标记（Concurrent Marking）：进行可达性分析，找出存活对象，耗时长，但可与用户线程并发执行。</li>
<li>最终标记（Final Marking）：修正并发标记阶段用户线程运行导致的变动记录。会STW，但可以并行执行，时间不会很长。</li>
<li>筛选回收（Live Data Counting and Evacuation）：根据每个region的回收价值和回收成本排序，根据用户配置的GC停顿时间开始回收。</li>
</ul>
</li>
<li>当对象分配过快，mixed GC来不及回收，G1会退化，触发Full GC，它使用单线程的Serial收集器来回收，整个过程STW，要尽量避免这种情况。</li>
<li>当内存很少的时候（存活对象占用大量空间），没有足够空间来复制对象，会导致回收失败。这时会保留被移动过的对象和没移动的对象，只调整引用。失败发生后，收集器认为存活对象被移动了，有足够空间让应用程序使用，于是用户线程继续工作，等待下一次触发GC。如果内存不够，就会触发Full GC。</li>
</ul>
<p><a href="https://docs.oracle.com/javase/10/gctuning/garbage-first-garbage-collector.htm#JSGCT-GUID-1CDEB6B6-9463-4998-815D-05E095BFBD0F" target="_blank" rel="noopener">参考G1的详细介绍</a></p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>垃圾回收器</tag>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title>理解JVM（六）：线程安全和锁优化</title>
    <url>/2018/06/27/%E7%90%86%E8%A7%A3JVM%EF%BC%88%E5%85%AD%EF%BC%89%EF%BC%9A%E7%BA%BF%E7%A8%8B%E5%AE%89%E5%85%A8%E5%92%8C%E9%94%81%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<h1 id="线程安全的实现方法"><a href="#线程安全的实现方法" class="headerlink" title="线程安全的实现方法"></a>线程安全的实现方法</h1><h2 id="互斥同步"><a href="#互斥同步" class="headerlink" title="互斥同步"></a>互斥同步</h2><p>互斥是因，同步是果；互斥是方法，同步是目的。</p>
<h3 id="synchronized关键字"><a href="#synchronized关键字" class="headerlink" title="synchronized关键字"></a>synchronized关键字</h3><ul>
<li><code>synchronized</code>关键字是基本的互斥同步手段。它在编译后会在同步代码块前后加入2条字节码指令：<code>monitorenter</code>和<code>monitorexit</code>。</li>
<li>这两个字节码都需要一个<code>reference</code>类型的参数来指明要锁定和解锁的对象。如果Java程序中的<code>synchronized</code>指定了对象参数，那就是这个对象的<code>reference</code>；如果没有指定，就根据<code>synchronized</code>修饰的是实例方法还是类方法，去取对应的对象实例或Class对象来作为锁对象。</li>
<li>执行<code>monitorenter</code>指令时，首先要尝试获取对象的锁。如果这个对象没被锁定，或当前线程已经拥有了那个对象的锁，把锁的计数器加1；在执行<code>monitorexit</code>指令时会将锁计数器减1。当计数器为0时，锁就被释放。如果获取对象锁失败，那当前线程就要阻塞等待，直到对象锁被另外一个线程释放为止。</li>
<li><code>synchronized</code>同步块对同一条线程来说是可重入的，不会出现自己把自己锁死的问题。</li>
<li>同步块在已进入的线程执行完之前，会阻塞后面其他线程的进入。</li>
<li>Java的线程是映射到操作系统的原生线程之上的，如果要阻塞或唤醒一个线程，都需要操作系统来完成，这就需要从用户态转换到核心态中，因此状态转换需要耗费很多的处理器时间，所以synchronized是Java语言中一个重量级的操作。不过虚拟机会有一些优化措施，比如自旋等待。<a id="more"></a>
<h3 id="ReentrantLock重入锁"><a href="#ReentrantLock重入锁" class="headerlink" title="ReentrantLock重入锁"></a>ReentrantLock重入锁</h3>重入锁位于<code>java.util.concurrent</code>包。基本用法和<code>synchronized</code>相似，只是代码写法有区别：<code>synchronized</code>是原生语法层面的实现。<code>ReentrantLock</code>是API层面，使用<code>lock()</code>和<code>unlock()</code>方法配合<code>try/finally</code>语句块来实现。</li>
</ul>
<p>重入锁有3个高级特性：</p>
<ul>
<li>等待可中断：当持有锁的线程长期不释放锁时，正在等待的线程可以选择放弃等待，改为处理其他事情。可中断特性对处理执行时间非常长的同步块很有帮助。</li>
<li>可实现公平锁：公平锁是指多个线程在等待同一个锁时，必须按照申请锁的时间顺序来依次获得锁；而非公平锁则不保证这一点，在锁被释放时，任何一个等待锁的线程都有机会获得锁。<code>synchronized</code>中的锁是非公平的，<code>ReentrantLock</code>默认情况下也是非公平的，但可以通过带布尔值的构造函数要求使用公平锁。</li>
<li>锁可以绑定多个条件：一个<code>ReentrantLock</code>对象可以同时绑定多个<code>Condition</code>对象，而在<code>synchronized</code>中，锁对象的<code>wait()</code>和<code>notify()</code>或<code>notifyAll()</code>方法可以实现一个隐含的条件，如果要和多于一个的条件关联的时候，就不得不额外地添加一个锁，而<code>ReentrantLock</code>则无须这样做，只需要多次调用<code>newCondition()</code>方法即可。</li>
</ul>
<h3 id="性能比较"><a href="#性能比较" class="headerlink" title="性能比较"></a>性能比较</h3><ul>
<li>JDK1.6之前，在多线程环境下，<code>synchronized</code>的吞吐量随着处理器数量增加而下降得非常严重。</li>
<li>JDK1.6之后，虚拟机做了优化，2种方式性能差不多。推荐优先使用<code>synchronized</code>方式。</li>
</ul>
<h2 id="非阻塞同步"><a href="#非阻塞同步" class="headerlink" title="非阻塞同步"></a>非阻塞同步</h2><p>互斥同步最主要的问题就是进行线程阻塞和唤醒所带来的性能问题，因此这种同步也称为阻塞同步（Blocking Synchronization）。</p>
<p>按处理问题的方式来说：</p>
<ul>
<li>互斥同步是悲观并发策略：无论是否产生共享数据争用，都会做同步措施（加锁，用户态内核态转换等）。</li>
<li>非阻塞同步是一种乐观并发策略：它基于冲突检测。通俗的说，就是先执行代码，若没有发生共享数据争用，就成功执行；若发生共享数据争用，就采取补偿措施（比如不断重试，直到成功），这种策略不会导致线程阻塞。</li>
</ul>
<p>CAS操作：<br>CAS指令需要有3个操作数，分别是内存位置（在Java中可以简单理解为变量的内存地址，用V表示）、旧的预期值（用A表示）和新值（用B表示）。CAS指令执行时，当且仅当V符合旧预期值A时，处理器用新值B更新V的值，否则它就不执行更新，但是无论是否更新了V的值，都会返回V的旧值，这个处理过程是个原子操作。</p>
<p>ABA问题：<br>如果一个变量V初次读取的时候是A值，并且在准备赋值的时候检查到它仍然为A值，那我们就能说它的值没有被其他线程改变过了吗？如果在这段期间它的值曾经被改成了B，后来又被改回为A，那CAS操作就会误认为它从来没有被改变过。</p>
<h2 id="无同步方案"><a href="#无同步方案" class="headerlink" title="无同步方案"></a>无同步方案</h2><p>如果一个方法本来就不涉及共享数据，那它就无须任何同步措施去保证正确性。</p>
<ul>
<li>可重入代码：这种代码也叫做纯代码（Pure Code），可以在代码执行的任何时刻中断它，转而去执行另外一段代码（包括递归调用它本身），而在控制权返回后，原来的程序不会出现任何错误。</li>
<li>线程本地存储：一段代码中所需要的数据必须与其他代码共享，并且可以把共享数据的可见范围限制在同一个线程之内，这样，无须同步也能保证线程之间不出现数据争用的问题。<ul>
<li>Java语言中，如果一个变量要被多线程访问，可以使用volatile关键字声明它为“易变的”；如果一个变量要被某个线程独享，Java中就没有类似C++中__declspec（thread） 这样的关键字，不过还是可以通过java.lang.ThreadLocal类来实现线程本地存储的功能。每一个线程的Thread对象中都有一个ThreadLocalMap对象，这个对象存储了一组以ThreadLocal.threadLocalHashCode为键，以本地线程变量为值的K-V值对，ThreadLocal对象就是当前线程的ThreadLocalMap的访问入口，每一个ThreadLocal对象都包含了一个独一无二的threadLocalHashCode值，使用这个值就可以在线程K-V值对中找回对应的本地线程变量。</li>
</ul>
</li>
</ul>
<h1 id="锁优化"><a href="#锁优化" class="headerlink" title="锁优化"></a>锁优化</h1><h2 id="适应性自旋（Adaptive-Spinning）"><a href="#适应性自旋（Adaptive-Spinning）" class="headerlink" title="适应性自旋（Adaptive Spinning）"></a>适应性自旋（Adaptive Spinning）</h2><p>线程阻塞的时候，让等待的线程不放弃cpu执行时间，而是执行一个自旋(一般是空循环)，这叫做自旋锁。</p>
<p>自旋等待本身虽然避免了线程切换的开销，但它是要占用处理器时间的，因此，如果锁被占用的时间很短，自旋等待的效果就非常好，反之，如果锁被占用的时间很长，那么自旋的线程只会白白消耗处理器资源，带来性能上的浪费。</p>
<p>因此，自旋等待的时间必须要有一定的限度。如果自旋超过了限定的次数仍然没有成功获得锁，就应当使用传统的方式去挂起线程了。自旋次数的默认值是10次，用户可以使用参数<code>-XX：PreBlockSpin</code>来更改。</p>
<p>JDK1.6引入了自适应的自旋锁。自适应意味着自旋的时间不再固定了，而是由前一次在同一个锁上的自旋时间及锁的拥有者的状态来决定。比如前一次自旋了3次就获得了一个锁，那么下一次虚拟机会允许他自旋更多次来获得这个锁。如果一个锁很少能通过自旋成功获得，那么之后再遇到这个情况就会省略自旋过程了。</p>
<h2 id="锁消除（Lock-Elimination）"><a href="#锁消除（Lock-Elimination）" class="headerlink" title="锁消除（Lock Elimination）"></a>锁消除（Lock Elimination）</h2><p>虚拟机即时编译器在运行时，对一些代码上要求同步，但是被检测到不可能存在共享数据竞争的锁进行消除。一般根据逃逸分析的数据支持来作为判定依据。</p>
<h2 id="锁粗化（Lock-Coarsening）"><a href="#锁粗化（Lock-Coarsening）" class="headerlink" title="锁粗化（Lock Coarsening）"></a>锁粗化（Lock Coarsening）</h2><p>原则上，我们在编写代码的时候，总是推荐将同步块的作用范围限制得尽量小——只在共享数据的实际作用域中才进行同步，这样是为了使需要同步的操作数量尽可能变小，如果存在锁竞争，那等待锁的线程也能尽快拿到锁。</p>
<p>但如果一系列操作频繁对同一个对象加锁解锁，或者加锁操作再循环体内，会耗费性能，这时虚拟机会扩大加锁范围。</p>
<h2 id="轻量级锁（Lightweight-Locking）"><a href="#轻量级锁（Lightweight-Locking）" class="headerlink" title="轻量级锁（Lightweight Locking）"></a>轻量级锁（Lightweight Locking）</h2><p>轻量级锁是JDK 1.6之中加入的新型锁机制。它的作用是在没有多线程竞争的前提下，减少传统的重量级锁使用操作系统互斥量产生的性能消耗。</p>
<p>HotSpot虚拟机的对象头（Object Header）分为两部分信息，第一部分用于存储对象自身的运行时数据，这部分称<code>为Mark Word</code>。还有一部分存储指向方法区对象类型数据的指针。</p>
<h3 id="加锁"><a href="#加锁" class="headerlink" title="加锁"></a>加锁</h3><p>在代码进入同步块的时候，如果此同步对象没有被锁定（锁标志位为“01”状态），虚拟机首先将在当前线程的栈帧中建立一个名为锁记录（Lock  Record）的空间，用于存储锁对象目前的Mark Word的拷贝（官方把这份拷贝加了一个Displaced前缀，即Displaced Mark Word）。然后，虚拟机将使用CAS操作尝试将对象的Mark Word更新为指向Lock Record的指针。如果这个更新动作成功，那么这个线程就拥有了该对象的锁，并且对象Mark Word的锁标志位（Mark Word的最后2bit）将转变为“00”，即表示此对象处于轻量级锁定状态。如果这个更新操作失败了，虚拟机首先会检查对象的Mark Word是否指向当前线程的栈帧，如果是说明当前线程已经拥有了这个对象的锁，那就可以直接进入同步块继续执行，否则说明这个锁对象已经被其他线程抢占了。如果有两条以上的线程争用同一个锁，那轻量级锁就不再有效，要膨胀为重量级锁，锁标志的状态值变为“10”，Mark Word中存储的就是指向重量级锁（互斥量）的指针，后面等待锁的线程也要进入阻塞状态。</p>
<h3 id="解锁"><a href="#解锁" class="headerlink" title="解锁"></a>解锁</h3><p>解锁过程也是通过CAS操作来进行的。如果对象的Mark Word仍然指向着线程的锁记录，那就用CAS操作把对象当前的Mark Word和线程中复制的Displaced Mark Word替换回来，如果替换成功，整个同步过程就完成了。如果替换失败，说明有其他线程尝试过获取该锁，那就要在释放锁的同时，唤醒被挂起的线程。</p>
<h3 id="性能"><a href="#性能" class="headerlink" title="性能"></a>性能</h3><p>没有锁竞争时，轻量级锁用CAS操作替代互斥量的开销，性能较优。有锁竞争时，除了互斥量开销，还有CAS操作开销，所以性能较差。但是，一般情况下，在整个同步周期内都是不存在竞争的”，这是一个经验数据。</p>
<h2 id="偏向锁（Biased-Locking）"><a href="#偏向锁（Biased-Locking）" class="headerlink" title="偏向锁（Biased Locking）"></a>偏向锁（Biased Locking）</h2><p>偏向锁也是JDK1.6中引入的锁优化，它的目的是消除数据在无竞争情况下的同步原语，进一步提高程序的运行性能。如果说轻量级锁是在无竞争的情况下使用CAS操作去消除同步使用的互斥量，那偏向锁就是在无竞争的情况下把整个同步都消除掉，连CAS操作都不做了。</p>
<p>当锁对象第一次被线程获取的时候，虚拟机将会把对象头中的标志位设为“01”，即偏向模式。同时使用CAS操作把获取到这个锁的线程的ID记录在对象的Mark Word之中，如果CAS操作成功，持有偏向锁的线程以后每次进入这个锁相关的同步块时，虚拟机都可以不再进行任何同步操作。当有另外一个线程去尝试获取这个锁时，偏向模式结束。</p>
<p>偏向锁可以提高带有同步但无竞争的程序性能，但并不一定总是对程序运行有利。如果程序中大多数的锁总是被多个不同的线程访问，那偏向模式就是多余的。在具体问题具体分析的前提下，有时候使用参数<code>-XX：-UseBiasedLocking</code>来禁止偏向锁优化反而可以提升性能。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>锁</tag>
      </tags>
  </entry>
  <entry>
    <title>理解JVM（五）：Java内存模型与线程</title>
    <url>/2018/06/26/%E7%90%86%E8%A7%A3JVM%EF%BC%88%E4%BA%94%EF%BC%89%EF%BC%9AJava%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B%E4%B8%8E%E7%BA%BF%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="Java内存模型"><a href="#Java内存模型" class="headerlink" title="Java内存模型"></a>Java内存模型</h1><p>JMM(Java Memory Model)是JVM定义的内存模型，用来屏蔽各种硬件和操作系统的内存访问差异。</p>
<ul>
<li>主内存：所有的变量都存储在主内存（Main Memory，类比物理内存）中。</li>
<li>工作内存：每条线程有自己的工作内存（Working Memory，类比处理器高速缓存），线程的工作内存中保存了被该线程使用到的变量的主内存副本拷贝，线程对变量的所有操作（读取、赋值等）都必须在工作内存中进行，而不能直接读写主内存中的变量。不同的线程之间也无法直接访问对方工作内存中的变量，线程间变量值的传递均需要通过主内存来完成。</li>
</ul>
<p><img src="http://upload-images.jianshu.io/upload_images/4059338-88a94bfaf810e38c" alt=""></p>
<a id="more"></a>
<h2 id="内存间的交互操作"><a href="#内存间的交互操作" class="headerlink" title="内存间的交互操作"></a>内存间的交互操作</h2><ul>
<li>lock（锁定）：作用于主内存的变量，它把一个变量标识为一条线程独占的状态。</li>
<li>unlock（解锁）：作用于主内存的变量，它把一个处于锁定状态的变量释放出来，释放后的变量才可以被其他线程锁定。</li>
<li>read（读取）：作用于主内存的变量，它把一个变量的值从主内存传输到线程的工作内存中，以便随后的load动作使用。</li>
<li>load（载入）：作用于工作内存的变量，它把read操作从主内存中得到的变量值放入工作内存的变量副本中。</li>
<li>use（使用）：作用于工作内存的变量，它把工作内存中一个变量的值传递给执行引擎，每当虚拟机遇到一个需要使用到变量的值的字节码指令时将会执行这个操作。</li>
<li>assign（赋值）：作用于工作内存的变量，它把一个从执行引擎接收到的值赋给工作内存的变量，每当虚拟机遇到一个给变量赋值的字节码指令时执行这个操作。</li>
<li>store（存储）：作用于工作内存的变量，它把工作内存中一个变量的值传送到主内存中，以便随后的write操作使用。</li>
<li>write（写入）：作用于主内存的变量，它把store操作从工作内存中得到的变量的值放入主内存的变量中。</li>
</ul>
<p>其中read和load，store和write必须成对使用，顺序但补一定连续的执行。通俗的说，就是执行了read，后面一定会执行load，但不一定read之后立马load；store和write也一样。lock和unlock也是成对出现，一个变量在同一时间点只能有一个线程对其进行lock。</p>
<ol>
<li><p>对于普通变量的操作：<br>创建变量，是在主内存中初始化。<br>线程用到的变量，会先从主内存中拷贝(<code>read</code>)出来，加载(<code>load</code>)到工作内存，然后引用(<code>use</code>)变量并运算赋值(<code>assign</code>)。然后存储(<code>store</code>)到工作内存，然后更新(<code>write</code>)掉原来的变量。</p>
<p> 普通变量的值在线程间传递均需要通过主内存来完成。例如，线程A修改一个普通变量的值，然后向主内存进行回写，另外一条线程B在线程A回写完成了之后再从主内存进行读取操作，新变量值才会对线程B可见。</p>
</li>
<li><p>对于<code>volatile</code>修饰的变量:过程和普通变量一样。但保证变量对所有线程的可见性，并且会禁止指令重排序的优化。</p>
<p> volatile的特殊规则保证了新值能立即同步到主内存，以及每次使用前立即从主内存刷新。因此，可以说volatile保证了多线程操作时变量的可见性，而普通变量则不能保证这一点。</p>
</li>
</ol>
<h2 id="先行发生原则-happens-before"><a href="#先行发生原则-happens-before" class="headerlink" title="先行发生原则(happens-before)"></a>先行发生原则(happens-before)</h2><p>它是判断数据是否存在竞争、线程是否安全的主要依据，依靠这个原则，我们可以通过几条规则一揽子地解决并发环境下两个操作之间是否可能存在冲突的所有问题。</p>
<p>先行发生是Java内存模型中定义的两项操作之间的偏序关系，如果说操作A先行发生于操作B，其实就是说在发生操作B之前，操作A产生的影响能被操作B观察到，“影响”包括修改了内存中共享变量的值、发送了消息、调用了方法等。</p>
<h1 id="线程"><a href="#线程" class="headerlink" title="线程"></a>线程</h1><p>线程是比进程更轻量级的调度执行单位，线程的引入，可以把一个进程的资源分配和执行调度分开，各个线程既可以共享进程资源（内存地址、文件I/O等），又可以独立调度（线程是CPU调度的基本单位）。</p>
<h2 id="实现线程的3种方式："><a href="#实现线程的3种方式：" class="headerlink" title="实现线程的3种方式："></a>实现线程的3种方式：</h2><ul>
<li>使用内核线程实现<ul>
<li>内核线程（Kernel-Level Thread,KLT）就是直接由操作系统内核（Kernel，下称内核）支持的线程，这种线程由内核来完成线程切换，内核通过操纵调度器（Scheduler）对线程进行调度，并负责将线程的任务映射到各个处理器上。每个内核线程可以视为内核的一个分身，这样操作系统就有能力同时处理多件事情，支持多线程的内核就叫做多线程内核（Multi-Threads Kernel）。</li>
<li>程序一般不直接使用内核线程，而是轻量级进程（通俗意义上的线程）。此2者<code>1:1</code>对应关系。创建，调用同步等都由系统执行，代价较高（需要在内核态和用户态之间来回切换），每个轻量级进程会消耗一定的内核资源（如内核线程的栈空间），因此一个系统支持的轻量级进程时有限的。</li>
</ul>
</li>
<li>使用用户线程实现<ul>
<li>广义来说，一个线程只要不是内核线程，就可以认为是用户线程。因此，轻量级进程也属于用户线程，但轻量级进程的实现始终是建立在内核之上的，许多操作都要进行系统调用，效率会受到限制。</li>
<li>狭义的说，用户线程指的是完全建立在用户空间的线程库上，系统内核不能感知线程存在的实现。用户线程的建立、同步、销毁和调度完全在用户态中完成，不需要内核的帮助。如果程序实现得当，这种线程不需要切换到内核态，因此操作可以是非常快速且低消耗的，也可以支持规模更大的线程数量，部分高性能数据库中的多线程就是由用户线程实现的。这种进程与用户线程之间<code>1:N</code>的关系称为一对多的线程模型。</li>
<li>使用用户线程的优势在于不需要系统内核支援，劣势也是没有系统内核的支援。所有的线程操作都需要用户程序自己处理，实现会很复杂，所以现在很少使用了。</li>
</ul>
</li>
<li>使用用户线程加轻量级进程混合实现<ul>
<li>既存在用户线程，也存在轻量级进程。用户线程还是完全建立在用户空间中，因此用户线程的创建、切换、析构等操作依然廉价，并且可以支持大规模的用户线程并发。而操作系统提供支持的轻量级进程则作为用户线程和内核线程之间的桥梁，这样可以使用内核提供的线程调度功能及处理器映射，并且用户线程的系统调用要通过轻量级线程来完成，大大降低了整个进程被完全阻塞的风险。在这种混合模式中，用户线程与轻量级进程的数量比是不定的，即为<code>N:M</code>的关系，这种就是多对多的线程模型。</li>
</ul>
</li>
</ul>
<p>Java线程实现：JDK1.2之前是用户线程，1.2和之后的版本，使用操作系统原生线程模型（内核线程）。</p>
<h2 id="Java线程调度"><a href="#Java线程调度" class="headerlink" title="Java线程调度"></a>Java线程调度</h2><p>线程调度是指系统为线程分配处理器使用权的过程，主要调度方式有两种：</p>
<ul>
<li>协同式线程调度（Cooperative Threads-Scheduling）：线程的执行时间由线程本身来控制，线程把自己的工作执行完了之后，要主动通知系统切换到另外一个线程上。<ul>
<li>好处：实现简单，而且由于线程要把自己的事情干完后才会进行线程切换，切换操作对线程自己是可知的，所以没有什么线程同步的问题。</li>
<li>坏处：线程执行时间不可控制，甚至如果一个线程编写有问题，一直不告知系统进行线程切换，那么程序就会一直阻塞在那里。</li>
</ul>
</li>
<li>抢占式线程调度（Preemptive Threads-Scheduling）：每个线程将由系统来分配执行时间，线程的切换不由线程本身来决定（在Java中，<code>Thread.yield()</code>可以让出执行时间，但是要获取执行时间的话，线程本身是没有什么办法的）。在这种实现线程调度的方式下，线程的执行时间是系统可控的，也不会有一个线程导致整个进程阻塞的问题，Java使用的线程调度方式就是抢占式调度 。</li>
</ul>
<p>虽然Java线程调度是系统自动完成的，但是我们还是可以“建议”系统给某些线程多分配一点执行时间，另外的线程少分配一点——通过设置线程优先级的方式(两个线程同时处于Ready状态时，优先级越高的线程越容易被系统选择执行)，不过这方法不是很可靠，因为系统线程优先级和Java的10种线程优先级不一定一一对应。</p>
<h2 id="线程状态"><a href="#线程状态" class="headerlink" title="线程状态"></a>线程状态</h2><p>在任意时间点，一个线程只有一种状态</p>
<ul>
<li>新建(New)：创建后尚未启动</li>
<li>运行(Runable)：正在执行或正在等待CPU为它分配执行时间</li>
<li>等待(Waiting)：<ul>
<li>无限等待(Waiting)：线程不会被分配CPU执行时间，等待被其他线程显式地唤醒。</li>
<li>期限等待(Timed Waiting)：线程不会被分配CPU执行时间，无须等待被其他线程显式地唤醒，在一定时间后它们会由系统自动唤醒。</li>
</ul>
</li>
<li>阻塞(Blocked)：被阻塞<ul>
<li>阻塞和等待的区别：<code>阻塞状态</code>在等待着获取到一个排他锁，这个事件将在另外一个线程放弃这个锁的时候发生；而<code>等待状态</code>则是在等待一段时间，或者唤醒动作的发生。在程序等待进入同步区域的时候，线程将进入这种状态。</li>
</ul>
</li>
<li>结束(Terminated)：已终止</li>
</ul>
<p><img src="http://upload-images.jianshu.io/upload_images/4059338-f295b711eb195acb" alt=""></p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>JMM</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring的事件机制</title>
    <url>/2018/06/15/Spring%E7%9A%84%E4%BA%8B%E4%BB%B6%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<p>当把一个事件发布到Spring提供的<code>ApplicationContext</code>中，被监听器侦测到，就会执行对应的处理方法。</p>
<h1 id="事件本身"><a href="#事件本身" class="headerlink" title="事件本身"></a>事件本身</h1><p>事件是一个自定义的类，需要继承Spring提供的<code>ApplicationEvent</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyEvent</span> <span class="keyword">extends</span> <span class="title">ApplicationEvent</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String msg;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">MyEvent</span><span class="params">(Object source, String msg)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(source);</span><br><span class="line">        <span class="keyword">this</span>.msg = msg;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<a id="more"></a>
<h1 id="事件监听"><a href="#事件监听" class="headerlink" title="事件监听"></a>事件监听</h1><p>基本方法是实现<code>ApplicationListener</code>接口，自定义一个监听器，实现<code>onApplicationEvent()</code>方法，然后添加到<code>ApplicationContext</code>。<br>比如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyListener</span> <span class="keyword">implements</span> <span class="title">ApplicationListener</span>&lt;<span class="title">MyEvent</span>&gt; </span>&#123;  </span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span>  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onApplicationEvent</span><span class="params">(MyEvent event)</span> </span>&#123;  </span><br><span class="line">        System.out.print(<span class="string">"监听到MyEvent事件"</span>);  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;  </span><br><span class="line">...</span><br><span class="line"><span class="comment">// SpringBoot的启动类中添加监听器</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">		SpringApplication application = <span class="keyword">new</span> SpringApplication(MyApplication<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">		application.addListeners(<span class="keyword">new</span> MyListener());</span><br><span class="line">		application.run(args);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>也可以使用注解<code>@EventListener</code>（推荐）：原理就是通过扫描这个注解，创建监听器并添加到<code>ApplicationContext</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Slf</span>4j</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyEventHandler</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@EventListener</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">handleEvent</span><span class="params">(MyEvent event)</span> </span>&#123;</span><br><span class="line">        log.info(<span class="string">"------------处理事件：&#123;&#125;"</span>, event.getMsg());</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">5</span> * <span class="number">1000L</span>);</span><br><span class="line">            log.info(<span class="string">"事件1(5s)处理完成"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="事件发布"><a href="#事件发布" class="headerlink" title="事件发布"></a>事件发布</h1><p>可以通过上下文对象的发布方法<code>ConfigurableApplicationContext::publishEvent()</code>来发布。<br>也可以实现<code>ApplicationEventPublisherAware</code>接口来发布（推荐）。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Slf</span>4j</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">EventService</span> <span class="keyword">implements</span> <span class="title">ApplicationEventPublisherAware</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> ApplicationEventPublisher publisher;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setApplicationEventPublisher</span><span class="params">(ApplicationEventPublisher applicationEventPublisher)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.publisher = applicationEventPublisher;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">doEventWork</span><span class="params">(String msg)</span> </span>&#123;</span><br><span class="line">        log.info(<span class="string">"------------publish event:"</span> + msg);</span><br><span class="line">        MyEvent event = <span class="keyword">new</span> MyEvent(<span class="keyword">this</span>, msg);</span><br><span class="line">        publisher.publishEvent(event);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"OK"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="测试代码"><a href="#测试代码" class="headerlink" title="测试代码"></a>测试代码</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="meta">@RunWith</span>(SpringRunner<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">class</span> <span class="title">EventServiceTest</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> EventService service;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">eventTest</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        String msg=<span class="string">"Java Code"</span>;</span><br><span class="line">        service.doEventWork(msg);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="https://user-gold-cdn.xitu.io/2018/6/15/16402c8b84e8e00b?w=1371&h=78&f=png&s=15710" alt=""></p>
<h1 id="注意"><a href="#注意" class="headerlink" title="注意"></a>注意</h1><p>如果2个事件之间是继承关系，会先监听到子类事件，处理完再监听父类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// MyEvent2 extends MyEvent</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Slf</span>4j</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyEventHandler</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@EventListener</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">handleEvent</span><span class="params">(MyEvent event)</span> </span>&#123;</span><br><span class="line">        log.info(<span class="string">"------------处理事件：&#123;&#125;"</span>, event.getMsg());</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">5</span> * <span class="number">1000L</span>);</span><br><span class="line">            log.info(<span class="string">"事件1(5s)处理完成"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@EventListener</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">handleEvent2</span><span class="params">(MyEvent2 event)</span> </span>&#123;</span><br><span class="line">        log.info(<span class="string">"------------处理事件2：&#123;&#125;"</span>, event.getMsg());</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">10</span> * <span class="number">1000L</span>);</span><br><span class="line">            log.info(<span class="string">"事件2(10s)处理完成"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当我publish一个子类事件MyEvent2时，日志如下：<br><img src="https://user-gold-cdn.xitu.io/2018/6/15/16402ca4e7d6a8c4?w=1421&h=136&f=png&s=33952" alt=""></p>
<hr>
<p>注意：默认是同步事件，如要使用异步，需要在Application上开启异步，并在监听器上使用<code>@Async</code></p>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
        <tag>Event</tag>
      </tags>
  </entry>
  <entry>
    <title>TCP的3次握手和4次挥手过程</title>
    <url>/2018/04/09/TCP%E7%9A%843%E6%AC%A1%E6%8F%A1%E6%89%8B%E5%92%8C4%E6%AC%A1%E6%8C%A5%E6%89%8B%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="建立连接（3次握手）"><a href="#建立连接（3次握手）" class="headerlink" title="建立连接（3次握手）"></a>建立连接（3次握手）</h1><p><img src="https://upload-images.jianshu.io/upload_images/4059338-8dec28773da8005b.png" alt="TCP3次握手.png"></p>
<a id="more"></a>
<ul>
<li>SYN是标志位，设置SYN=1，表示请求建立连接</li>
<li>服务端会同时和多个客户端建立连接，所以在报文交互时，需要带上序号seq，在响应是seq+1，以此来检测报文合法性</li>
</ul>
<h3 id="连接过程"><a href="#连接过程" class="headerlink" title="连接过程"></a>连接过程</h3><ul>
<li>客户端：我想建立一个连接。状态：CLOSED -&gt; SYN SENT</li>
<li>服务端：可以，我同意建立连接。状态：LISTEN -&gt; SYN RCVD</li>
<li>客户端：好的，我收到你的确认，我准备好接收数据了。状态：SYN SENT -&gt; ESTABLISHED</li>
<li>服务端听到客户端准备好了，也进入准备状态。状态：SYN RCVD -&gt; ESTABLISHED</li>
</ul>
<p>至此，TCP连接就建立完毕，开始互相发送数据了。</p>
<h1 id="断开连接（4次挥手）"><a href="#断开连接（4次挥手）" class="headerlink" title="断开连接（4次挥手）"></a>断开连接（4次挥手）</h1><p><img src="https://upload-images.jianshu.io/upload_images/4059338-140f04e2f323b7b5.png" alt="TCP4次挥手.png"></p>
<ul>
<li>FIN是标志位，设置FIN=1，表示请求断开连接</li>
</ul>
<h3 id="断开连接过程"><a href="#断开连接过程" class="headerlink" title="断开连接过程"></a>断开连接过程</h3><ul>
<li>客户端：我数据发完了，我要断开连接了。状态：ESTABLISHED -&gt; FIN WAIT 1</li>
<li>服务端：好，我知道你要断开连接了。状态：ESTABLISHED -&gt; CLOSE WAIT</li>
</ul>
<p>此时，客户端确认服务端已经知道要断开了。但是，服务端可能还有数据没发送完，所以客户端还能接收数据但不会发送数据。状态：FIN WAIT 1 -&gt; FIN WAIT 2</p>
<p>过了一会，服务端数据发完了。</p>
<ul>
<li>服务端：我准备好了，断开连接吧。状态：CLOSE WAIT -&gt; LAST ACK</li>
<li>客户端：好，那我断开连接了，再见。状态：FIN WAIT 2 -&gt; TIME WAIT</li>
<li>服务端：客户端已经断开了，我也断开吧。状态：LAST ACK -&gt; CLOSED</li>
<li>客户端等待2个MSL时间以后断开连接，状态：TIME WAIT -&gt; CLOSED</li>
</ul>
<h1 id="TCP握手挥手的状态"><a href="#TCP握手挥手的状态" class="headerlink" title="TCP握手挥手的状态"></a>TCP握手挥手的状态</h1><table>
<thead>
<tr>
<th align="center">状态</th>
<th align="center">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">CLOSED</td>
<td align="center">关闭状态，没有连接活动或正在进行</td>
</tr>
<tr>
<td align="center">LISTEN</td>
<td align="center">监听状态，服务器正在等待连接进入</td>
</tr>
<tr>
<td align="center">SYN SENT</td>
<td align="center">已经发出连接请求，等待确认</td>
</tr>
<tr>
<td align="center">SYN RCVD</td>
<td align="center">收到一个连接请求，尚未确认</td>
</tr>
<tr>
<td align="center">ESTABLISHED</td>
<td align="center">连接建立，正常数据传输状态</td>
</tr>
<tr>
<td align="center">FIN WAIT 1</td>
<td align="center">（主动关闭）已经发送关闭请求，等待确认</td>
</tr>
<tr>
<td align="center">FIN WAIT 2</td>
<td align="center">（主动关闭）收到对方关闭确认，等待对方关闭请求</td>
</tr>
<tr>
<td align="center">TIME WAIT</td>
<td align="center">完成双向关闭，等待所有分组死掉</td>
</tr>
<tr>
<td align="center">CLOSE WAIT</td>
<td align="center">（被动关闭）收到对方关闭请求，已经确认</td>
</tr>
<tr>
<td align="center">LAST ACK</td>
<td align="center">（被动关闭）等待最后一个关闭确认，并等待所有分组死掉</td>
</tr>
<tr>
<td align="center">CLOSING</td>
<td align="center">双方同时尝试关闭，等待对方确认</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>日常记录</category>
      </categories>
      <tags>
        <tag>TCP/IP</tag>
      </tags>
  </entry>
  <entry>
    <title>理解JVM（四）：JVM类加载机制</title>
    <url>/2018/02/06/%E7%90%86%E8%A7%A3JVM%EF%BC%88%E5%9B%9B%EF%BC%89%EF%BC%9AJVM%E7%B1%BB%E5%8A%A0%E8%BD%BD%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<h1 id="Class文件"><a href="#Class文件" class="headerlink" title="Class文件"></a>Class文件</h1><p>我们写的Java代码，经过编译器编译之后，就成为了<code>.class</code>文件，从本地机器码变成了字节码。Class文件是一组以8位字节为基础单位的二进制流，各个数据项目严格按照顺序紧凑地排列在Class文件之中，中间没有添加任何分隔符，这使得整个Class文件中存储的内容几乎全部是程序运行的必要数据，没有空隙存在。Class文件中只有2种数据结构：无符号数和表。</p>
<a id="more"></a>
<p>每个Class文件的头4个字节称为魔数（Magic Number），值为<code>0xCAFEBABE</code>。紧接着4个字节是Class文件的版本号。再往后，就是类的具体信息了，比如常量池、类索引、父类索引、接口索引、字段、方法等信息了。</p>
<p>所谓类的加载，就是把Class文件读到内存中。</p>
<h1 id="类的生命周期"><a href="#类的生命周期" class="headerlink" title="类的生命周期"></a>类的生命周期</h1><p><img src="https://user-gold-cdn.xitu.io/2018/6/21/164212e84e3ed0d4?w=1175&h=466&f=png&s=157532" alt=""><br>类从被加载到虚拟机内存中开始，到卸载出内存为止，它的整个生命周期包括：加载（Loading）、验证（Verification）、准备（Preparation）、解析（Resolution）、初始化（Initialization）、使用（Using）和卸载（Unloading）7个阶段。其中验证、准备、解析3个部分统称为连接（Linking）。</p>
<p>加载、验证、准备、初始化和卸载这5个阶段的顺序是确定的，类的加载过程必须按照这种顺序按部就班地开始，而解析阶段则不一定：它在某些情况下可以在初始化阶段之后再开始，这是为了支持Java语言的运行时绑定（也称为动态绑定或晚期绑定）。注意，是按部就班地“开始”，而不是按部就班地“进行”或“完成”，强调这点是因为这些阶段通常都是互相交叉地混合式进行的，通常会在一个阶段执行的过程中调用、激活另外一个阶段。</p>
<h2 id="加载"><a href="#加载" class="headerlink" title="加载"></a>加载</h2><p>在加载阶段，虚拟机做3件事：</p>
<ol>
<li>通过一个类的全限定名来获取定义此类的二进制字节流。</li>
<li>将这个字节流所代表的静态存储结构转化为方法区的运行时数据结构。</li>
<li>在内存中生成一个代表这个类的<code>java.lang.Class</code>对象，作为方法区这个类的各种数据的访问入口。</li>
</ol>
<h2 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h2><p>验证是连接阶段的第一步，这一阶段的目的是为了确保Class文件的字节流中包含的信息符合当前虚拟机的要求，并且不会危害虚拟机自身的安全。</p>
<p>验证阶段大致上会完成4个阶段的检验动作</p>
<ol>
<li>文件格式验证：验证字节流是否符合Class文件格式的规范，并且能被当前版本的虚拟机处理。比如是否以魔数<code>0xCAFEBABE</code>开头，主、次版本号是否能被当前虚拟机处理，常量类型，指向常量的索引是否符合要求等。这阶段的验证是基于二进制字节流进行的，只有通过了这个阶段的验证后，字节流才会进入内存的方法区中进行存储，所以后面的3个验证阶段全部是基于方法区的存储结构进行的，不会再直接操作字节流。</li>
<li>元数据验证：对字节码描述的信息进行语义分析，以保证其描述的信息符合Java语言规范的要求。比如继承关系。</li>
<li>字节码验证：对类的方法体进行校验分析，保证被校验类的方法在运行时不会做出危害虚拟机安全的事件。通过数据流和控制流分析，确定程序语义是合法的、符合逻辑的。</li>
<li>符号引用验证：对类自身以外（常量池中的各种符号引用）的信息进行匹配性校验，确保解析动作能正常执行。它发生在虚拟机将符号引用转化为直接引用的时候，这个转化动作将在连接的第三阶段——解析阶段中发生。</li>
</ol>
<p>验证阶段是非常重要的，但不是必须的。它对程序运行期没有影响，如果所引用的类经过反复验证，那么可以考虑采用<code>-Xverify:none</code>参数来关闭大部分的类验证措施，以缩短虚拟机类加载的时间。</p>
<h2 id="准备"><a href="#准备" class="headerlink" title="准备"></a>准备</h2><p>准备阶段是正式为类变量分配内存并设置类变量初始值的阶段，这些变量所使用的内存都将在方法区中进行分配。这个阶段中有两个容易产生混淆的概念需要强调一下，首先，这时候进行内存分配的仅包括类变量（被static修饰的变量），而不包括实例变量，实例变量将会在对象实例化时随着对象一起分配在Java堆中。其次，这里所说的初始值“通常情况”下是数据类型的零值。</p>
<blockquote>
<p>假设一个类变量的定义为：public static int value = 123;</p>
</blockquote>
<blockquote>
<p>那变量value在准备阶段过后的初始值为0而不是123，因为这时候尚未开始执行任何Java<br>方法，而把value赋值为123的putstatic指令是程序被编译后，存放于类构造器＜clinit＞()方<br>法之中，所以把value赋值为123的动作将在初始化阶段才会执行。</p>
</blockquote>
<p>当然也有特殊情况：如果类字段的字段属性表中存在ConstantValue属性，那在准备阶段变量value就会被初始化为ConstantValue属性所指定的值。</p>
<blockquote>
<p>假设上面类变量value的定义变为：public static final int value = 123;</p>
</blockquote>
<blockquote>
<p>编译时Javac将会为value生成ConstantValue属性，在准备阶段虚拟机就会根据ConstantValue的设置将value赋值为123。</p>
</blockquote>
<h2 id="解析"><a href="#解析" class="headerlink" title="解析"></a>解析</h2><p>解析阶段是虚拟机将常量池内的符号引用替换为直接引用的过程。解析动作主要针对类或接口、字段、类方法、接口方法、方法类型、方法句柄和调用点限定符7类符号引用进行。</p>
<ul>
<li>符号引用（Symbolic References）：符号引用以一组符号来描述所引用的目标，符号可<br>以是任何形式的字面量，只要使用时能无歧义地定位到目标即可。符号引用与虚拟机实现的<br>内存布局无关，引用的目标并不一定已经加载到内存中。各种虚拟机实现的内存布局可以各<br>不相同，但是它们能接受的符号引用必须都是一致的，因为符号引用的字面量形式明确定义<br>在Java虚拟机规范的Class文件格式中。</li>
<li>直接引用（Direct References）：直接引用可以是直接指向目标的指针、相对偏移量或是<br>一个能间接定位到目标的句柄。直接引用是和虚拟机实现的内存布局相关的，同一个符号引<br>用在不同虚拟机实例上翻译出来的直接引用一般不会相同。如果有了直接引用，那引用的目<br>标必定已经在内存中存在。</li>
</ul>
<h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><p>这一步开始执行类中定义的Java程序代码（或者说是字节码）。虚拟机会保证一个类的初始化方法在多线程环境中被正确地加锁、同步，如果多个线程同时去初始化一个类，那么只会有一个线程去执行这个类的初始化方法，其他线程都需要阻塞等待，直到活动线程执行完毕。</p>
<h3 id="JVM初始化步骤"><a href="#JVM初始化步骤" class="headerlink" title="JVM初始化步骤"></a>JVM初始化步骤</h3><ol>
<li>假如这个类还没有被加载和连接，则程序先加载并连接该类</li>
<li>假如该类的直接父类还没有被初始化，则先初始化其直接父类</li>
<li>假如类中有初始化语句，则系统依次执行这些初始化语句</li>
</ol>
<h3 id="类初始化时机"><a href="#类初始化时机" class="headerlink" title="类初始化时机"></a>类初始化时机</h3><p>只有当主动使用一个类的时候才会触发这个类的初始化，类的主动使用包括以下六种：</p>
<ul>
<li>创建类的实例，也就是new的方式</li>
<li>访问某个类或接口的静态变量，或者对该静态变量赋值</li>
<li>调用类的静态方法</li>
<li>反射，比如<code>Class.forName(&quot;com.mysql.jdbc.Driver&quot;);</code></li>
<li>初始化某个类的子类，则其父类也会被初始化</li>
<li>Java虚拟机启动时被标明为启动类的类（Java Test），直接使用java.exe命令来运行某个主类</li>
</ul>
<h1 id="类加载器"><a href="#类加载器" class="headerlink" title="类加载器"></a>类加载器</h1><p>虚拟机设计团队把类加载阶段中的“通过一个类的全限定名来获取描述此类的二进制字节流”这个动作放到Java虚拟机外部去实现，以便让应用程序自己决定如何去获取所需要的类。实现这个动作的代码模块称为“类加载器”。</p>
<h2 id="双亲委派模型"><a href="#双亲委派模型" class="headerlink" title="双亲委派模型"></a>双亲委派模型</h2><p>从Java虚拟机的角度来讲，只存在两种不同的类加载器：一种是启动类加载器（Bootstrap ClassLoader），这个类加载器使用C++语言实现，是虚拟机自身的一部分；另一种就是所有其他的类加载器，这些类加载器都由Java语言实现，独立于虚拟机外部，并且全都继承自抽象类<code>java.lang.ClassLoader</code>。</p>
<p>从Java开发人员的角度来看，类加载器可以划分为以下3种：</p>
<ol>
<li>启动类加载器（Bootstrap ClassLoader）：负责加载存放在JAVA_HOME\lib目录中的，或被<code>-Xbootclasspath</code>参数指定的路径中的，并且能被虚拟机识别的类库（如rt.jar，所有的java.开头的类均被Bootstrap ClassLoader加载）。启动类加载器是无法被Java程序直接引用的。</li>
<li>扩展类加载器（Extension ClassLoader）：这个加载器由<code>sun.misc.Launcher$ExtClassLoader</code>实现，它负责加载JAVA_HOME\lib\ext目录中的，或者被java.ext.dirs系统变量所指定的路径中的所有类库，开发者可以直接使用扩展类加载器。</li>
<li>应用程序类加载器（Application ClassLoader）：该类加载器由<code>sun.misc.Launcher$AppClassLoader</code>来实现，它负责加载用户类路径（ClassPath）所指定的类，开发者可以直接使用该类加载器，如果应用程序中没有自定义过自己的类加载器，一般情况下这个就是程序中默认的类加载器。</li>
</ol>
<p>我们的应用程序都是由这3种类加载器互相配合进行加载的，如果有必要，还可以加入<br>自己定义的类加载器。</p>
<p><img src="https://user-gold-cdn.xitu.io/2018/6/21/1642187992e17854?w=720&h=815&f=png&s=182504" alt=""></p>
<p>双亲委派模型要求除了顶层的启动类加载器外，其余的类加载器都应当有自己的父类加载器。这里类加载器之间的父子关系一般不会以<code>继承</code>的关系来实现，而是都使用<code>组合</code>关系来复用父加载器的代码。它不是强制性的约束模型，而是Java设计者推荐的一种类加载器实现方式。</p>
<p>双亲委派模型的工作过程：如果一个类加载器收到了类加载的请求，它首先不会自己去尝试加载这个类，而是把这个请求委派给父类加载器去完成，每一个层次的类加载器都是如此，因此所有的加载请求最终都应该传送到顶层的启动类加载器中，只有当父加载器反馈自己无法完成这个加载请求（它的搜索范围中没有找到所需的类）时，子加载器才会尝试自己去加载。</p>
<p>ClassLoader源码分析：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Class&lt;?&gt; loadClass(String name) <span class="keyword">throws</span> ClassNotFoundException &#123;</span><br><span class="line">        <span class="keyword">return</span> loadClass(name, <span class="keyword">false</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">synchronized</span> Class&lt;?&gt; loadClass(String name, <span class="keyword">boolean</span> resolve) <span class="keyword">throws</span> ClassNotFoundException &#123;</span><br><span class="line">        <span class="comment">// 首先判断该类型是否已经被加载</span></span><br><span class="line">        Class c = findLoadedClass(name);</span><br><span class="line">        <span class="keyword">if</span> (c == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">//如果没有被加载，就委托给父类加载或者委派给启动类加载器加载</span></span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (parent != <span class="keyword">null</span>) &#123;</span><br><span class="line">                     <span class="comment">//如果存在父类加载器，就委派给父类加载器加载</span></span><br><span class="line">                    c = parent.loadClass(name, <span class="keyword">false</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">//如果不存在父类加载器，就检查是否是由启动类加载器加载的类，通过调用本地方法native Class findBootstrapClass(String name)</span></span><br><span class="line">                    c = findBootstrapClass0(name);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">                <span class="comment">// 如果父类加载器和启动类加载器都不能完成加载任务，才调用自身的加载功能</span></span><br><span class="line">                c = findClass(name);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (resolve) &#123;</span><br><span class="line">            resolveClass(c);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> c;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>通过分析源码，我们知道，双亲委派模型可以保证每个类都只会被加载一次（类似缓存机制）。</p>
<h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ul>
<li>《深入理解Java虚拟机 第二版》</li>
<li><a href="http://www.ityouknow.com/jvm/2017/08/19/class-loading-principle.html" target="_blank" rel="noopener">纯洁的微笑</a></li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title>理解JVM（三）：JVM命令工具</title>
    <url>/2018/01/28/%E7%90%86%E8%A7%A3JVM%EF%BC%88%E4%B8%89%EF%BC%89%EF%BC%9AJVM%E5%91%BD%E4%BB%A4%E5%B7%A5%E5%85%B7/</url>
    <content><![CDATA[<h1 id="jps（JVM-Process-Status-Tool）"><a href="#jps（JVM-Process-Status-Tool）" class="headerlink" title="jps（JVM Process Status Tool）"></a>jps（JVM Process Status Tool）</h1><p>虚拟机进程状况工具，可以列出正在运行的虚拟机进程，并显示虚拟机执行主类（MainClass,main（）函数所在的类）名称以及这些进程的本地虚拟机唯一ID（Local Virtual Machine Identifier,LVMID）。</p>
<ul>
<li><p>命令格式：<code>jps[options][hostid]</code></p>
</li>
<li><p>参数：</p>
<ul>
<li><code>-q</code>：只输出LVMID，省略主类名称</li>
<li><code>-m</code>：输出虚拟机进程启动时传给主类main()函数的参数</li>
<li><code>-l</code>：输出主类全名，如果进程执行的时jar包则输出jar路径</li>
<li><code>-v</code>：输出虚拟机进程启动时的jvm参数<a id="more"></a>
<h1 id="jstat（JVM-Statistics-Monitoring-Tool）"><a href="#jstat（JVM-Statistics-Monitoring-Tool）" class="headerlink" title="jstat（JVM Statistics Monitoring Tool）"></a>jstat（JVM Statistics Monitoring Tool）</h1>虚拟机统计信息监视工具，用于监视虚拟机各种运行状态信息的命令行工具。它可以显示本地或者远程虚拟机进程中的类装载、内存、垃圾收集、JIT编译等运行数据，在没有GUI图形界面，只提供了纯文本控制台环境的服务器上，它将是运行期定位虚拟机性能问题的首选工具。</li>
</ul>
</li>
<li><p>命令格式：<code>jstat[option vmid[interval[s|ms][count]]]</code></p>
</li>
<li><p>对于命令格式中的VMID与LVMID需要特别说明一下：如果是本地虚拟机进程，VMID与LVMID是一致的；如果是远程虚拟机进程，那VMID的格式应当是：<code>[protocol：][//]lvmid[@hostname[：port]/servername]</code></p>
</li>
<li><p>参数interval和count代表查询间隔和次数，如果省略这两个参数，说明只查询一次。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 每250毫秒查询一次进程2764垃圾收集状况，一共查询20次</span><br><span class="line">jstat-gc 2764 250 20</span><br></pre></td></tr></table></figure></li>
<li><p>参数：主要分为3类：类装载、垃圾收集、运行<br>期编译状况</p>
<ul>
<li><code>-class</code>：监视类装载、卸载数量、总空间和类装载消耗的时间</li>
<li><code>-gc</code>：监视java堆状况，包括Eden区、2个Survivor区、老年代、永久代等的容量，已用空间，GC时间合计等信息</li>
<li><code>-gccapacity</code>：    监视内容与<code>-gc</code>相同，但输出主要关注Java堆各个区域使用到的最大、最小空间</li>
<li><code>-gcutil</code>：监视内容与<code>-gc</code>相同，但输出主要关注已使用空间占总空间的百分百</li>
<li><code>-gccause</code>：与<code>-gcutil</code>一样，但会额外输出上一次GC产生的原因</li>
<li><code>-gcnew</code>：监视新生代GC的状况</li>
<li><code>-gcnewcapacity</code>：监视内容与<code>-gcnew</code>相同，输出主要关注使用到的最大、最小空间</li>
<li><code>-gcold</code>：监视老年代GC的状况</li>
<li><code>-gcoldcapacity</code>：监视内容与<code>-gcold</code>相同，输出主要关注使用到的最大、最小空间</li>
<li><code>-gcpermcapacity</code>：输出永久代使用到的最大、最小空间</li>
<li><code>-compiler</code>：输出JIT编译过的方法、耗时等信息</li>
<li><code>-printcompilation</code>：输出已被JIT编译的方法</li>
</ul>
</li>
</ul>
<h1 id="jinfo（Configuration-Info-for-Java）"><a href="#jinfo（Configuration-Info-for-Java）" class="headerlink" title="jinfo（Configuration Info for Java）"></a>jinfo（Configuration Info for Java）</h1><p>Java配置信息工具，可实时查看和调整虚拟机各项参数。</p>
<ul>
<li>命令格式：<code>jinfo[option]pid</code></li>
<li>参数：<ul>
<li><code>-flag</code>：输出指定args参数的值</li>
<li><code>-flags</code>：不需要args参数，输出所有JVM参数的值</li>
<li><code>-sysprops</code>：输出系统属性，等同于System.getProperties()</li>
</ul>
</li>
</ul>
<h1 id="jmap（Memory-Map-for-Java）"><a href="#jmap（Memory-Map-for-Java）" class="headerlink" title="jmap（Memory Map for Java）"></a>jmap（Memory Map for Java）</h1><p>Java内存映像工具用于生成堆转储快照（dump文件）</p>
<ul>
<li>几种获得dump文件的方式<ul>
<li>jmap命令</li>
<li>-XX：+HeapDumpOnOutOfMemoryError参数，可以让虚拟机在OOM异常出现之后自动生成dump文件</li>
<li>-XX：+HeapDumpOnCtrlBreak参数则可以使用[Ctrl]+[Break]键让虚拟机生成dump文件</li>
<li>在Linux系统下通过<code>Kill-3</code>命令发送进程退出信号也能拿到dump文件</li>
</ul>
</li>
<li>命令格式：<code>jmap[option]vmid</code></li>
<li>参数：<ul>
<li><code>-dump</code>：生成堆转储快照</li>
<li><code>-finalizerinfo</code>：显示在F-Queue队列等待Finalizer线程执行finalizer方法的对象</li>
<li><code>-heap</code>：显示Java堆详细信息</li>
<li><code>-histo</code>：显示堆中对象的统计信息</li>
<li><code>-permstat</code>：以CLassLoader为统计口径显示永久代内存状态</li>
<li><code>-F</code>：当-dump没有响应时，强制生成dump快照</li>
</ul>
</li>
</ul>
<h1 id="jhat（JVM-Heap-Analysis-Tool）"><a href="#jhat（JVM-Heap-Analysis-Tool）" class="headerlink" title="jhat（JVM Heap Analysis Tool）"></a>jhat（JVM Heap Analysis Tool）</h1><p>与jmap搭配使用，用来分析jmap生成的堆转储快照。jhat内置了一个微型的HTTP/HTML服务器，生成dump文件的分析结果后，可以在浏览器中查看。</p>
<ul>
<li>命令格式：<code>jhat [dumpfile]</code></li>
<li>不推荐使用此命令，有2个原因：<ul>
<li>一般不会直接在服务器上分析dump文件，浪费服务器资源。</li>
<li>分析功能简陋，推荐用专业的可视化分析工具，比如VisualVM</li>
</ul>
</li>
</ul>
<h1 id="jstack（Stack-Trace-for-Java）"><a href="#jstack（Stack-Trace-for-Java）" class="headerlink" title="jstack（Stack Trace for Java）"></a>jstack（Stack Trace for Java）</h1><p>Java堆栈跟踪工具，用于生成虚拟机当前时刻的线程快照。</p>
<p>线程快照就是当前虚拟机内每一条线程正在执行的方法堆栈的集合，生成线程快照的主要目的是定位线程出现长时间停顿的原因，如线程间死锁、死循环、请求外部资源导致的长时间等待等都是导致线程长时间停顿的常见原因。</p>
<p>线程出现停顿的时候通过jstack来查看各个线程的调用堆栈，就可以知道没有响应的线程到底在后台做些什么事情，或者等待着什么资源。</p>
<ul>
<li>命令格式：<code>jstack[option]vmid</code></li>
<li>参数：<ul>
<li><code>-F</code>：当正常输出的请求不被响应时，强制输出线程堆栈</li>
<li><code>-l</code>：除堆栈外，显示关于锁的附加信息</li>
<li><code>-m</code>：如果调用本地方法，可显示C/C++的堆栈</li>
</ul>
</li>
<li>在JDK 1.5中，<code>java.lang.Thread</code>类新增了一个<code>getAllStackTraces()</code>方法用于获取虚拟机中所有线程的StackTraceElement对象。</li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title>理解JVM（二）：垃圾收集算法</title>
    <url>/2018/01/16/%E7%90%86%E8%A7%A3JVM%EF%BC%88%E4%BA%8C%EF%BC%89%EF%BC%9A%E5%9E%83%E5%9C%BE%E6%94%B6%E9%9B%86%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<h1 id="判断哪些对象需要被回收"><a href="#判断哪些对象需要被回收" class="headerlink" title="判断哪些对象需要被回收"></a>判断哪些对象需要被回收</h1><ul>
<li>引用计数算法：<ul>
<li>给对象中添加一个引用计数器，每当有一个地方引用时，计数器值就加1；当引用失效时，计数器值就减1；任何时刻计数器为0的对象就是不可能再被使用的。<br>但是JVM没有使用此方法，因为此方法无法解决2个对象相互循环引用的问题。</li>
</ul>
</li>
<li>可达性分析算法：<ul>
<li>这个算法的基本思路就是通过一系列的称为“GC Roots”的对象作为起始点，从这些节点开始向下搜索，搜索所走过的路径称为引用链（Reference Chain），当一个对象到GC Roots没有任何引用链相连时，则证明此对象是不可用的。<a id="more"></a></li>
</ul>
</li>
<li>在Java语言中，可作为GC Roots的对象包括下面几种：<ul>
<li>虚拟机栈（栈帧中的本地变量表）中引用的对象。</li>
<li>方法区中类静态属性引用的对象。</li>
<li>方法区中常量引用的对象。</li>
<li>本地方法栈中JNI（即一般说的Native方法）引用的对象。</li>
</ul>
</li>
</ul>
<h1 id="JDK1-2以后的引用分为4种，引用强度依次逐渐减弱"><a href="#JDK1-2以后的引用分为4种，引用强度依次逐渐减弱" class="headerlink" title="JDK1.2以后的引用分为4种，引用强度依次逐渐减弱"></a>JDK1.2以后的引用分为4种，引用强度依次逐渐减弱</h1><ul>
<li>强引用（Strong Reference）<ul>
<li>强引用就是指在程序代码之中普遍存在的，类似<code>Object obj=new Object()</code>这类的引用，只要强引用还存在，垃圾收集器永远不会回收掉被引用的对象。 </li>
</ul>
</li>
<li>软引用（Soft Reference）<ul>
<li>软引用是用来描述一些还有用但并非必需的对象。对于软引用关联着的对象，在系统将要发生内存溢出异常之前，将会把这些对象列进回收范围之中进行第二次回收。如果这次回收还没有足够的内存，才会抛出内存溢出异常。在JDK1.2之后，提供了<code>SoftReference</code>类来实现。 </li>
</ul>
</li>
<li>弱引用（Weak Reference）<ul>
<li>弱引用也是用来描述非必需对象的，但是它的强度比软引用更弱一些，被弱引用关联的对象只能生存到下一次垃圾收集发生之前。当垃圾收集器工作时，无论当前内存是否足够，都会回收掉只被弱引用关联的对象。在JDK1.2之后，提供了<code>WeakReference</code>类来实现。</li>
</ul>
</li>
<li>虚引用（Phantom Reference）<ul>
<li>虚引用也称为幽灵引用或者幻影引用，它是最弱的一种引用关系。一个对象是否有虚引用的存在，完全不会对其生存时间构成影响，也无法通过虚引用来取得一个对象实例。为一个对象设置虚引用关联的唯一目的就是能在这个对象被收集器回收时收到一个系统通知。在JDK 1.2之后，提供了<code>PhantomReference</code>类来实现。 </li>
</ul>
</li>
</ul>
<h1 id="垃圾收集算法"><a href="#垃圾收集算法" class="headerlink" title="垃圾收集算法"></a>垃圾收集算法</h1><h2 id="标记-清除算法（Mark-Sweep）"><a href="#标记-清除算法（Mark-Sweep）" class="headerlink" title="标记-清除算法（Mark-Sweep）"></a>标记-清除算法（Mark-Sweep）</h2><ul>
<li>此方法分为“标记”和“清除”两个阶段：首先标记出所有需要回收的对象，在标记完成后统一回收所有被标记的对象。它是最基础的收集算法，后续的收集算法都是基于这种思路并对其不足进行改进而得到的。</li>
<li>主要两个不足：一个是效率问题，标记和清除两个过程的效率都不高；另一个是空间问题，标记清除之后会产生大量不连续的内存碎片，空间碎片太多可能会导致以后在程序运行过程中需要分配较大对象时，无法找到足够的连续内存而不得不提前触发另一次垃圾收集动作。 </li>
</ul>
<p><img src="https://user-gold-cdn.xitu.io/2018/6/20/1641be7dcdba5403?w=813&h=567&f=png&s=56126" alt=""></p>
<h2 id="复制算法（Copying）"><a href="#复制算法（Copying）" class="headerlink" title="复制算法（Copying）"></a>复制算法（Copying）</h2><ul>
<li>此方法将可用内存按容量划分为大小相等的两块，每次只使用其中的一块。当这一块的内存用完了，就将还存活着的对象复制到另外一块上面，然后再把已使用过的内存空间一次清理掉。这样使得每次都是对整个半区进行内存回收，内存分配时也就不用考虑内存碎片等复杂情况，只要移动堆顶指针，按顺序分配内存即可，实现简单，运行高效。但是可用内存变成原来的一半，代价较大。</li>
<li>此方法一般用在回收新生代，因为新生代的对象98%都是很快就会被回收，所以不用1:1划分，而是分为一块较大的<code>Eden空间</code>和2块较小的<code>Survivor空间</code>。每次使用Eden和其中一块Survivor。当回收时，将Eden和Survivor中还存活着的对象一次性地复制到另外一块Survivor空间上，最后清理掉Eden和刚才用过的Survivor空间。HotSpot虚拟机默认Eden和Survivor的大小比例是8:1:1，即新生代中可用内存为90%，只有10%被浪费。<br><img src="https://user-gold-cdn.xitu.io/2018/6/20/1641beafe4849c59?w=806&h=548&f=png&s=55172" alt=""><h2 id="标记-整理算法（Mark-Compact）"><a href="#标记-整理算法（Mark-Compact）" class="headerlink" title="标记-整理算法（Mark-Compact）"></a>标记-整理算法（Mark-Compact）</h2></li>
<li>复制收集算法在对象存活率较高时就要进行较多的复制操作，效率将会变低。更关键的是，如果不想浪费50%的空间，就需要有额外的空间进行分配担保，以应对被使用的内存中所有对象都100%存活的极端情况，所以在老年代一般不能直接选用这种算法。</li>
<li>根据老年代的特点，有人提出了另外一种“标记-整理”（Mark-Compact）算法，标记过程仍然与“标记-清除”算法一样，但后续步骤不是直接对可回收对象进行清理，而是让所有存活的对象都向一端移动，然后直接清理掉端边界以外的内存。</li>
</ul>
<p><img src="https://user-gold-cdn.xitu.io/2018/6/20/1641bf807020ed1e?w=801&h=556&f=png&s=54150" alt=""></p>
<h2 id="分代收集算法（Generational-Collection）"><a href="#分代收集算法（Generational-Collection）" class="headerlink" title="分代收集算法（Generational  Collection）"></a>分代收集算法（Generational  Collection）</h2><ul>
<li>当前商业虚拟机的垃圾收集都采用“分代收集”算法，这种算法并没有什么新的思想，只是根据对象存活周期的不同将内存划分为几块。一般是把Java堆分为新生代和老年代，这样就可以根据各个年代的特点采用最适当的收集算法。在新生代中，每次垃圾收集时都发现有大批对象死去，只有少量存活，那就选用复制算法，只需要付出少量存活对象的复制成本就可以完成收集。而老年代中因为对象存活率高、没有额外空间对它进行分配担保，就必须使用“标记—清理”或者“标记—整理”算法来进行回收。 </li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title>Java中的异常</title>
    <url>/2017/12/05/Java%E4%B8%AD%E7%9A%84%E5%BC%82%E5%B8%B8/</url>
    <content><![CDATA[<h1 id="Java异常分类"><a href="#Java异常分类" class="headerlink" title="Java异常分类"></a>Java异常分类</h1><p>所有异常的根类为java.lang.Throwable，Throwable下面又派生了两个子类：</p>
<ul>
<li>Error</li>
<li>Exception</li>
</ul>
<h2 id="Error"><a href="#Error" class="headerlink" title="Error"></a>Error</h2><p>Error表示应用程序本身无法克服和恢复的一种严重问题，程序只有死的份了。例如，内存溢出和线程死锁等系统问题。</p>
<h2 id="Exception"><a href="#Exception" class="headerlink" title="Exception"></a>Exception</h2><p>Exception表示程序还能够克服和恢复的问题，其中又分为2类：</p>
<ul>
<li>系统异常：是软件本身缺陷所导致的问题，也就是软件开发人员考虑不周所导致的问题，软件使用者无法克服和恢复这种问题，但在这种问题下还可以让软件系统继续运行或者让软件死掉。例如，数组下标越界（ArrayIndexOutOfBoundsException），空指针异常（NullPointerException）、类转换异常（ClassCastException）。<ul>
<li>编译器不强制用try…catch处理或用throws声明，也称为<strong>unchecked</strong>异常。</li>
</ul>
</li>
<li>普通异常：是运行环境的变化或异常所导致的问题，是用户能够克服的问题，例如，网络断线，硬盘空间不够，发生这样的异常后，程序不应该死掉。<ul>
<li>编译器强制普通异常必须try…catch处理或用throws声明继续抛给上层调用方法处理，也称为<strong>checked</strong>异常。</li>
</ul>
</li>
</ul>
<h1 id="常见错误与异常"><a href="#常见错误与异常" class="headerlink" title="常见错误与异常"></a>常见错误与异常</h1><h2 id="Error："><a href="#Error：" class="headerlink" title="Error："></a>Error：</h2><ul>
<li>OutOfMemoryError：内存溢出</li>
<li>StackOverflowError：栈溢出</li>
</ul>
<h2 id="RuntimeException："><a href="#RuntimeException：" class="headerlink" title="RuntimeException："></a>RuntimeException：</h2><ul>
<li>ClassCastException：类型转换异常</li>
<li>IndexOutOfBoundsException：数组越界</li>
<li>NullPointerException：空指针</li>
<li>ArrayStoreException：数据存储异常，操作数组时类型不一致</li>
<li>BufferOverflowException：缓存区溢出</li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Exception</tag>
      </tags>
  </entry>
  <entry>
    <title>JDK8和JDK9双版本共存</title>
    <url>/2017/09/30/JDK8%E5%92%8CJDK9%E5%8F%8C%E7%89%88%E6%9C%AC%E5%85%B1%E5%AD%98/</url>
    <content><![CDATA[<p><img src="http://upload-images.jianshu.io/upload_images/4059338-19a112548aeefa7b.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="java9.jpg"></p>
<a id="more"></a>
<p>以前安装JDK，需要手动配置环境变量。JDK8多了自动配置环境变量，所以可以不用手动配置。<br>如果我已经装了JDK8，还想再装一个JDK9，安装完，自动配置的环境变量会指向JDK9版本。</p>
<h1 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h1><ol>
<li>删除自动配置的环境变量<br>自动配置的环境变量是一个隐藏目录：<code>C:\ProgramData\Oracle\Java\javapath</code>，删掉这个目录下的3个exe文件，系统就无法匹配到了。</li>
</ol>
<p><img src="http://upload-images.jianshu.io/upload_images/4059338-8a87044b407e014f.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="jdk.png"></p>
<ol start="2">
<li>手动配置环境变量<br>自动匹配是匹配不到了，所以我们用老办法，手动配置环境变量即可。<br>这样，我们可以根据环境变量里配置的JDK版本去实现版本切换了。</li>
</ol>
]]></content>
      <categories>
        <category>日常记录</category>
      </categories>
      <tags>
        <tag>JDK</tag>
      </tags>
  </entry>
  <entry>
    <title>搭建日志分析系统ELK</title>
    <url>/2017/08/18/%E6%90%AD%E5%BB%BA%E6%97%A5%E5%BF%97%E5%88%86%E6%9E%90%E7%B3%BB%E7%BB%9FELK/</url>
    <content><![CDATA[<p><img src="http://upload-images.jianshu.io/upload_images/4059338-cf6a458b1e4dc0e5.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="elasticsearch.jpg"></p>
<h1 id="日志分析组件3件套："><a href="#日志分析组件3件套：" class="headerlink" title="日志分析组件3件套："></a>日志分析组件3件套：</h1><ul>
<li>E：<code>Elasticsearch</code>是一个搜索引擎，基于<code>Lucene</code>，天然分布式，很容易水平扩展，屏蔽了复杂的分布式概念，对外提供<code>RESTful</code>API。</li>
<li>L：<code>Logstash</code>用于收集日志，写入<code>Elasticsearch</code>。</li>
<li>K：<code>Kibana</code>是一个展示层，基于NodeJS，可以图标形式展示数据，界面简洁。<a id="more"></a>
这3大组件，都可以在 <a href="https://www.elastic.co/cn/products" target="_blank" rel="noopener">这里</a> 直接下载。</li>
</ul>
<h1 id="搭建安装步骤："><a href="#搭建安装步骤：" class="headerlink" title="搭建安装步骤："></a>搭建安装步骤：</h1><ul>
<li><p>下载elasticsearch，解压后，运行<code>bin/elasticsearch -d</code>，以守护进程形式启动，打开localhost:9200会返回json信息如下，说明启动成功。<br><img src="http://upload-images.jianshu.io/upload_images/4059338-38c4534a2a73570c.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="json.png"></p>
</li>
<li><p>下载Logstash，解压，在/bin/config下创建一个配置文件<code>vi logstash.conf</code>。</p>
</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">input &#123;</span><br><span class="line">  tcp &#123;</span><br><span class="line">    host =&gt; <span class="string">"192.168.1.91"</span> <span class="comment"># 要监听的日志来源地址</span></span><br><span class="line">    port =&gt; 4567</span><br><span class="line">    mode =&gt; <span class="string">"server"</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">output &#123;</span><br><span class="line">  stdout&#123; codec =&gt; rubydebug &#125;</span><br><span class="line">  elasticsearch &#123;</span><br><span class="line">    hosts =&gt; [<span class="string">"192.168.1.91:9200"</span>]  <span class="comment"># elasticsearch中配置的host地址</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>指定配置文件并后台启动：<code>bin/logstash -f logstash.conf &amp;</code>。</li>
<li>在你的应用程序中配置日志输出到<code>Logstash</code>，比如<code>Logback.xml</code><ol>
<li>引入logstash-logback-encoder：<code>compile group: &#39;net.logstash.logback&#39;, name: &#39;logstash-logback-encoder&#39;, version: &#39;4.11&#39;</code></li>
<li>配置logback.xml</li>
</ol>
</li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">appender</span> <span class="attr">name</span>=<span class="string">"stash"</span> <span class="attr">class</span>=<span class="string">"net.logstash.logback.appender.LogstashTcpSocketAppender"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">destination</span>&gt;</span>192.168.1.91:4567<span class="tag">&lt;/<span class="name">destination</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- encoder is required --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">encoder</span> <span class="attr">class</span>=<span class="string">"net.logstash.logback.encoder.LogstashEncoder"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">appender</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">root</span> <span class="attr">level</span>=<span class="string">"DEBUG"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">"stdout"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">"fileout"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">"stash"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">root</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>下载Kibana，解压后启动：<code>bin/kibana &amp;</code>，打开<code>localhost:5601</code>即可。</li>
</ul>
<h1 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h1><ul>
<li>在安装Elasticsearch的时候，配置文件中我们把network.host改为0.0.0.0允许远程访问后，启动会报错，请按照 <a href="http://blog.csdn.net/u012246178/article/details/63253531" target="_blank" rel="noopener">如下操作</a>，放开linux的系统限制。</li>
<li><a href="https://www.elastic.co/guide/cn/elasticsearch/guide/current/index.html" target="_blank" rel="noopener">Elasticsearch入门参考手册</a></li>
</ul>
]]></content>
      <categories>
        <category>NoSQL</category>
      </categories>
      <tags>
        <tag>Logback</tag>
        <tag>Elasticsearch</tag>
        <tag>Logstash</tag>
        <tag>Kibana</tag>
      </tags>
  </entry>
  <entry>
    <title>Java8学习笔记之新日期API</title>
    <url>/2017/05/08/Java8%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%E4%B9%8B%E6%96%B0%E6%97%A5%E6%9C%9FAPI/</url>
    <content><![CDATA[<p>Java8对日期API做了改进，提供了许多好用的方法和接口。<br>首先，最基本也最重要的3个对象：</p>
<ul>
<li><code>LocalDate</code>：日期对象</li>
<li><code>LocalTime</code>：时间对象</li>
<li><code>LocalDateTime</code>：日期时间对象，是<code>LocalDate</code>和<code>LocalTime</code>的合集</li>
</ul>
<h1 id="对象的创建"><a href="#对象的创建" class="headerlink" title="对象的创建"></a>对象的创建</h1><p>以上这3个对象，都各自有3种创建方式。</p>
<ol>
<li>通过<code>.now()</code>得到当前日期时间对象。</li>
<li>通过<code>.of()</code>直接指定年月日，时分秒。</li>
<li><code>LocalDate</code>和<code>LocalTime</code>通过解析字符串创建，<code>LocalDateTime</code>可以通过前2个对象创建。</li>
</ol>
<a id="more"></a>
<h2 id="LocalDate对象3种创建方式以及相关方法示例"><a href="#LocalDate对象3种创建方式以及相关方法示例" class="headerlink" title="LocalDate对象3种创建方式以及相关方法示例"></a>LocalDate对象3种创建方式以及相关方法示例</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">localDate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    LocalDate date = LocalDate.of(<span class="number">2017</span>, <span class="number">5</span>, <span class="number">3</span>);</span><br><span class="line">    <span class="keyword">int</span> year = date.getYear();<span class="comment">// 2017</span></span><br><span class="line">    Month month = date.getMonth();<span class="comment">// MAY</span></span><br><span class="line">    <span class="keyword">int</span> day = date.getDayOfMonth();<span class="comment">// 3</span></span><br><span class="line">    DayOfWeek dow = date.getDayOfWeek();<span class="comment">// Wednesday</span></span><br><span class="line">    <span class="keyword">int</span> len = date.lengthOfMonth();<span class="comment">// 31</span></span><br><span class="line">    <span class="keyword">boolean</span> leap = date.isLeapYear();<span class="comment">// false</span></span><br><span class="line">    System.out.println(year + <span class="string">", "</span> + month + <span class="string">", "</span> + day + <span class="string">", "</span> + dow + <span class="string">", "</span> + len + <span class="string">", "</span> + leap);</span><br><span class="line"></span><br><span class="line">    LocalDate date2 = LocalDate.now();</span><br><span class="line">    <span class="keyword">int</span> year2 = date2.get(ChronoField.YEAR);<span class="comment">// 2017</span></span><br><span class="line">    <span class="keyword">int</span> month2 = date2.get(ChronoField.MONTH_OF_YEAR);<span class="comment">// 5</span></span><br><span class="line">    <span class="keyword">int</span> day2 = date2.get(ChronoField.DAY_OF_MONTH);<span class="comment">// 3</span></span><br><span class="line">    <span class="keyword">int</span> dow2 = date2.get(ChronoField.DAY_OF_WEEK);<span class="comment">// 3</span></span><br><span class="line">    System.out.println(year2 + <span class="string">", "</span> + month2 + <span class="string">", "</span> + day2 + <span class="string">", "</span> + dow2);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 写2017-3-18会报错，DateTimeParseException异常，可以传一个DateTimeFormatter自定义格式</span></span><br><span class="line">    LocalDate date3 = LocalDate.parse(<span class="string">"2017-03-18"</span>);</span><br><span class="line">    <span class="keyword">int</span> year3 = date3.get(ChronoField.YEAR);</span><br><span class="line">    <span class="keyword">int</span> month3 = date3.get(ChronoField.MONTH_OF_YEAR);</span><br><span class="line">    <span class="keyword">int</span> day3 = date3.get(ChronoField.DAY_OF_MONTH);</span><br><span class="line">    <span class="keyword">int</span> dow3 = date3.get(ChronoField.DAY_OF_WEEK);</span><br><span class="line">    System.out.println(year3 + <span class="string">", "</span> + month3 + <span class="string">", "</span> + day3 + <span class="string">", "</span> + dow3);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="LocalTime对象3种创建方式以及相关方法示例"><a href="#LocalTime对象3种创建方式以及相关方法示例" class="headerlink" title="LocalTime对象3种创建方式以及相关方法示例"></a>LocalTime对象3种创建方式以及相关方法示例</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">localTime</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    LocalTime time = LocalTime.of(<span class="number">11</span>, <span class="number">06</span>, <span class="number">23</span>);</span><br><span class="line">    <span class="keyword">int</span> hour = time.getHour();</span><br><span class="line">    <span class="keyword">int</span> minute = time.getMinute();</span><br><span class="line">    <span class="keyword">int</span> second = time.getSecond();</span><br><span class="line">    System.out.println(hour + <span class="string">":"</span> + minute + <span class="string">":"</span> + second);</span><br><span class="line"></span><br><span class="line">    LocalTime time2 = LocalTime.now();</span><br><span class="line">    <span class="keyword">int</span> hour2 = time2.get(ChronoField.HOUR_OF_DAY);</span><br><span class="line">    <span class="keyword">int</span> minute2 = time2.get(ChronoField.MINUTE_OF_HOUR);</span><br><span class="line">    <span class="keyword">int</span> second2 = time2.get(ChronoField.SECOND_OF_MINUTE);</span><br><span class="line">    System.out.println(hour2 + <span class="string">":"</span> + minute2 + <span class="string">":"</span> + second2);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 写13:5:43会报错，DateTimeParseException异常，可以传一个DateTimeFormatter自定义格式</span></span><br><span class="line">    LocalTime time3 = LocalTime.parse(<span class="string">"13:05:43"</span>);</span><br><span class="line">    <span class="keyword">int</span> hour3 = time3.get(ChronoField.HOUR_OF_DAY);</span><br><span class="line">    <span class="keyword">int</span> minute3 = time3.get(ChronoField.MINUTE_OF_HOUR);</span><br><span class="line">    <span class="keyword">int</span> second3 = time3.get(ChronoField.SECOND_OF_MINUTE);</span><br><span class="line">    System.out.println(hour3 + <span class="string">":"</span> + minute3 + <span class="string">":"</span> + second3);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="LocalDateTime对象3种创建方式以及相关方法示例"><a href="#LocalDateTime对象3种创建方式以及相关方法示例" class="headerlink" title="LocalDateTime对象3种创建方式以及相关方法示例"></a>LocalDateTime对象3种创建方式以及相关方法示例</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">localDateTime</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    LocalDate date = LocalDate.now();</span><br><span class="line">    LocalTime time = LocalTime.now();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建LocalDateTime</span></span><br><span class="line">    LocalDateTime dateTime = LocalDateTime.of(<span class="number">2017</span>, <span class="number">5</span>, <span class="number">3</span>, <span class="number">11</span>, <span class="number">53</span>, <span class="number">23</span>);</span><br><span class="line">    LocalDateTime dateTime2 = LocalDateTime.of(date, time);</span><br><span class="line">    LocalDateTime dateTime3 = date.atTime(<span class="number">11</span>, <span class="number">53</span>, <span class="number">23</span>);</span><br><span class="line">    LocalDateTime dateTime4 = date.atTime(time);</span><br><span class="line">    LocalDateTime dateTime6 = time.atDate(date);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 转化</span></span><br><span class="line">    LocalDate date2 = dateTime2.toLocalDate();</span><br><span class="line">    LocalTime time2 = dateTime2.toLocalTime();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="时间间隔对象"><a href="#时间间隔对象" class="headerlink" title="时间间隔对象"></a>时间间隔对象</h1><ul>
<li><code>Duration</code>：可以传2个localTime对象，localDateTime对象或者Instant对象</li>
<li><code>Period</code>：用年，月，日建模，可以传2个localDate对象</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">between</span><span class="params">()</span> </span>&#123;</span><br><span class="line"><span class="comment">//        Duration d1 = Duration.between(time1, time2);</span></span><br><span class="line"><span class="comment">//        Duration d2 = Duration.between(dateTime1, dateTime2);</span></span><br><span class="line"><span class="comment">//        Duration d3 = Duration.between(instant1, instant2);</span></span><br><span class="line">        Period tenDays = Period.between(LocalDate.of(<span class="number">2014</span>, <span class="number">3</span>, <span class="number">8</span>), LocalDate.of(<span class="number">2014</span>, <span class="number">3</span>, <span class="number">18</span>));</span><br><span class="line"></span><br><span class="line">        Duration threeMinutes = Duration.ofMinutes(<span class="number">3</span>);</span><br><span class="line">        Duration threeMinutes2 = Duration.of(<span class="number">3</span>, ChronoUnit.MINUTES);</span><br><span class="line">        Period tenDays2 = Period.ofDays(<span class="number">10</span>);</span><br><span class="line">        Period threeWeeks = Period.ofWeeks(<span class="number">3</span>);</span><br><span class="line">        Period twoYearsSixMonthsOneDay = Period.of(<span class="number">2</span>, <span class="number">6</span>, <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h1 id="修改日期时间对象"><a href="#修改日期时间对象" class="headerlink" title="修改日期时间对象"></a>修改日期时间对象</h1><h2 id="直接使用-with-来修改对应的年月日时分秒属性"><a href="#直接使用-with-来修改对应的年月日时分秒属性" class="headerlink" title="直接使用.with()来修改对应的年月日时分秒属性"></a>直接使用<code>.with()</code>来修改对应的年月日时分秒属性</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">editDateTime</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    LocalDate date1 = LocalDate.of(<span class="number">2014</span>, <span class="number">3</span>, <span class="number">18</span>);<span class="comment">// 2014-03-18</span></span><br><span class="line">    LocalDate date2 = date1.withYear(<span class="number">2011</span>);<span class="comment">// 2011-03-18</span></span><br><span class="line">    LocalDate date3 = date2.withDayOfMonth(<span class="number">25</span>);<span class="comment">// 2011-03-25</span></span><br><span class="line">    LocalDate date4 = date3.with(ChronoField.MONTH_OF_YEAR, <span class="number">9</span>);<span class="comment">// 2011-09-25</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="在现有对象上做相对修改"><a href="#在现有对象上做相对修改" class="headerlink" title="在现有对象上做相对修改"></a>在现有对象上做相对修改</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">editDateTime</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    LocalDate date1 = LocalDate.of(<span class="number">2014</span>, <span class="number">3</span>, <span class="number">18</span>);<span class="comment">// 2014-03-18</span></span><br><span class="line">    LocalDate date2 = date1.plusWeeks(<span class="number">1</span>);<span class="comment">// 2014-03-25</span></span><br><span class="line">    LocalDate date3 = date2.minusYears(<span class="number">3</span>);<span class="comment">// 2011-03-25</span></span><br><span class="line">    LocalDate date4 = date3.plus(<span class="number">6</span>, ChronoUnit.MONTHS);<span class="comment">// 2011-09-25</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="自定义修改日期"><a href="#自定义修改日期" class="headerlink" title="自定义修改日期"></a>自定义修改日期</h2><p>如果要对日期做一些复杂的修改，那么可以通过<code>TemporalAdjusters</code>接口实现复杂逻辑。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">adjust</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    LocalDate date1 = LocalDate.of(<span class="number">2017</span>, <span class="number">5</span>, <span class="number">8</span>);<span class="comment">// 2017-05-08</span></span><br><span class="line">    LocalDate date2 = date1.with(TemporalAdjusters.nextOrSame(DayOfWeek.SUNDAY));<span class="comment">// 2017-05-14</span></span><br><span class="line">    LocalDate date3 = date2.with(TemporalAdjusters.lastDayOfMonth());<span class="comment">// 2017-05-31</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>jdk为我们实现了一些常用的方法：</p>
<ul>
<li>dayOfWeekInMonth：创建一个新的日期，它的值为同一个月中每一周的第几天</li>
<li>firstDayOfMonth：创建一个新的日期，它的值为当月的第一天</li>
<li>firstDayOfNextMonth：创建一个新的日期，它的值为下月的第一天</li>
<li>firstDayOfNextYear：创建一个新的日期，它的值为明年的第一天</li>
<li>firstDayOfYear：创建一个新的日期，它的值为当年的第一天</li>
<li>firstInMonth：创建一个新的日期，它的值为同一个月中，第一个符合星期几要求的值</li>
<li>lastDayOfMonth：创建一个新的日期，它的值为当月的最后一天</li>
<li>lastDayOfNextMonth：创建一个新的日期，它的值为下月的最后一天</li>
<li>lastDayOfNextYear：创建一个新的日期，它的值为明年的最后一天</li>
<li>lastDayOfYear：创建一个新的日期，它的值为今年的最后一天</li>
<li>lastInMonth：创建一个新的日期，它的值为同一个月中，最后一个符合星期几要求的值</li>
<li>next/previous：创建一个新的日期，并将其值设定为日期调整后或者调整前，第一个符合指定星期几要求的日期</li>
<li>nextOrSame/previousOrSame：创建一个新的日期，并将其值设定为日期调整后或者调整前，第一个符合指定星期几要求的日期，如果该日期已经符合要求，直接返回该对象</li>
</ul>
<h1 id="自定义格式化输出日期"><a href="#自定义格式化输出日期" class="headerlink" title="自定义格式化输出日期"></a>自定义格式化输出日期</h1><p>新版本，我们拥有一个线程安全的类：<code>DateTimeFormatter</code><br>来看代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">formatPrint</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    LocalDate date = LocalDate.of(<span class="number">2014</span>, <span class="number">3</span>, <span class="number">18</span>);</span><br><span class="line">    String s1 = date.format(DateTimeFormatter.BASIC_ISO_DATE);<span class="comment">// 20140318</span></span><br><span class="line">    String s2 = date.format(DateTimeFormatter.ISO_LOCAL_DATE);<span class="comment">// 2014-03-18</span></span><br><span class="line"></span><br><span class="line">    LocalDate date1 = LocalDate.parse(<span class="string">"20140318"</span>, DateTimeFormatter.BASIC_ISO_DATE);</span><br><span class="line">    LocalDate date2 = LocalDate.parse(<span class="string">"2014-03-18"</span>, DateTimeFormatter.ISO_LOCAL_DATE);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 自定义格式</span></span><br><span class="line">    DateTimeFormatter formatter = DateTimeFormatter.ofPattern(<span class="string">"dd/MM/yyyy"</span>);</span><br><span class="line">    String formattedDate = date.format(formatter);</span><br><span class="line">    System.out.println(formattedDate);<span class="comment">// 18/03/2014</span></span><br><span class="line">    LocalDate date3 = LocalDate.parse(formattedDate, formatter);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 带时区的日期(本地化)</span></span><br><span class="line">    DateTimeFormatter italianFormatter = DateTimeFormatter.ofPattern(<span class="string">"d. MMMM yyyy"</span>, Locale.ITALIAN);</span><br><span class="line">    String formattedDate2 = date.format(italianFormatter); <span class="comment">// 18. marzo 2014</span></span><br><span class="line">    LocalDate date4 = LocalDate.parse(formattedDate2, italianFormatter);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java8</tag>
        <tag>时间日期API</tag>
      </tags>
  </entry>
  <entry>
    <title>Java8学习笔记之Stream API</title>
    <url>/2017/04/26/Java8%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%E4%B9%8BStream%20API/</url>
    <content><![CDATA[<p>Stream是Java8引入的一个重度使用lambda表达式的API。<br>Stream可以用流的方式处理数据集合，在Java8之前，我们处理这些集合，是需要迭代器的，比如iterator，这是外部迭代；而Stream是内部迭代，我们不用关心集合内部元素是如何迭代的，计算机会自动帮我们选择最适合的实现方式。</p>
<h1 id="如何创建一个流"><a href="#如何创建一个流" class="headerlink" title="如何创建一个流"></a>如何创建一个流</h1><ol>
<li>最常见的，有一个集合对象<code>List&lt;String&gt; strs = Arrays.asList(&quot;Java 8 &quot;, &quot;Lambdas &quot;, &quot;In &quot;, &quot;Action&quot;);</code>，直接调用<code>strs.stream()</code>就得到一个<code>Stream&lt;String&gt;</code>的流。<br>如果想使用并行流增加性能，请使用<code>strs.parallelStream()</code>，或<code>strs.stream().parallel()</code>。</li>
<li>由值创建：<code>Stream&lt;String&gt; stream = Stream.of(&quot;Java 8 &quot;, &quot;Lambdas &quot;, &quot;In &quot;, &quot;Action&quot;);</code><a id="more"></a></li>
<li>由数组创建：<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>[] numbers = &#123;<span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span>, <span class="number">7</span>, <span class="number">11</span>, <span class="number">13</span>&#125;;</span><br><span class="line"><span class="keyword">int</span> sum = Arrays.stream(numbers).sum();</span><br></pre></td></tr></table></figure></li>
<li>由文件创建：<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 统计文本文件中有多少个不同的单词</span></span><br><span class="line"><span class="keyword">long</span> uniqueWords = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">try</span> (Stream&lt;String&gt; lines = Files.lines(Paths.get(<span class="string">"data.txt"</span>), Charset.defaultCharset())) &#123;</span><br><span class="line">    uniqueWords = lines.flatMap(line -&gt; Arrays.stream(line.split(<span class="string">" "</span>)))</span><br><span class="line">            .distinct()</span><br><span class="line">            .count();</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>由函数生成流：<code>Stream.iterate()</code>和<code>Stream.generate()</code>可以生产无限流，即元素有无穷多个。一般来说，应该使用limit(n)来对这种流加以限制，以避免产生无穷多个元素。<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">iterator</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Stream.iterate(<span class="number">0</span>, n -&gt; n + <span class="number">2</span>).limit(<span class="number">10</span>).forEach(System.out::println);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">generate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Stream.generate(Math::random).limit(<span class="number">5</span>).forEach(System.out::println);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


</li>
</ol>
<h1 id="Stream常用方法"><a href="#Stream常用方法" class="headerlink" title="Stream常用方法"></a>Stream常用方法</h1><blockquote>
<p>Stream API 支持两种类型的操作：中间操作（如filter或map）和终端操作（如count、findFirst、forEach和reduce）。</p>
</blockquote>
<p>我用一个筛选菜单的需求作为示例。</p>
<h2 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Dish</span> </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> String name;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">boolean</span> vegetarian;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> calories;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> Type type;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="title">Dish</span><span class="params">(String name, <span class="keyword">boolean</span> vegetarian, <span class="keyword">int</span> calories, Type type)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">this</span>.name = name;</span><br><span class="line">            <span class="keyword">this</span>.vegetarian = vegetarian;</span><br><span class="line">            <span class="keyword">this</span>.calories = calories;</span><br><span class="line">            <span class="keyword">this</span>.type = type;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> name;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isVegetarian</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> vegetarian;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getCalories</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> calories;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> Type <span class="title">getType</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> type;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> name;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Type &#123;MEAT, FISH, OTHER&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> List&lt;Dish&gt; <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> Arrays.asList(</span><br><span class="line">                <span class="keyword">new</span> Dish(<span class="string">"pork"</span>, <span class="keyword">false</span>, <span class="number">800</span>, Type.MEAT),</span><br><span class="line">                <span class="keyword">new</span> Dish(<span class="string">"beef"</span>, <span class="keyword">false</span>, <span class="number">700</span>, Type.MEAT),</span><br><span class="line">                <span class="keyword">new</span> Dish(<span class="string">"chicken"</span>, <span class="keyword">false</span>, <span class="number">400</span>, Type.MEAT),</span><br><span class="line">                <span class="keyword">new</span> Dish(<span class="string">"french fries"</span>, <span class="keyword">true</span>, <span class="number">530</span>, Type.OTHER),</span><br><span class="line">                <span class="keyword">new</span> Dish(<span class="string">"rice"</span>, <span class="keyword">true</span>, <span class="number">350</span>, Type.OTHER),</span><br><span class="line">                <span class="keyword">new</span> Dish(<span class="string">"season fruit"</span>, <span class="keyword">true</span>, <span class="number">120</span>, Type.OTHER),</span><br><span class="line">                <span class="keyword">new</span> Dish(<span class="string">"pizza"</span>, <span class="keyword">true</span>, <span class="number">550</span>, Type.OTHER),</span><br><span class="line">                <span class="keyword">new</span> Dish(<span class="string">"prawns"</span>, <span class="keyword">false</span>, <span class="number">300</span>, Type.FISH),</span><br><span class="line">                <span class="keyword">new</span> Dish(<span class="string">"salmon"</span>, <span class="keyword">false</span>, <span class="number">450</span>, Type.FISH));</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="过滤筛选"><a href="#过滤筛选" class="headerlink" title="过滤筛选"></a>过滤筛选</h2><blockquote>
<p>谓词：返回boolean的函数</p>
</blockquote>
<ul>
<li>filter()：接受一个谓词，返回符合条件的元素集合<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">filter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Dish&gt; menu = init();</span><br><span class="line">    List&lt;Dish&gt; vegetarianMenu = menu.stream()</span><br><span class="line">            .filter(Dish::isVegetarian)</span><br><span class="line">            .collect(Collectors.toList());</span><br><span class="line">    Assert.assertEquals(<span class="number">4</span>, vegetarianMenu.size());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>distinct()：返回集合中各异的元素集合(去重)<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">distinct</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Integer&gt; numbers = Arrays.asList(<span class="number">5</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">1</span>, <span class="number">3</span>, <span class="number">3</span>, <span class="number">2</span>, <span class="number">4</span>);</span><br><span class="line">    numbers.stream().distinct().forEach(System.out::println);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>limit()：截取流中指定数量的元素，返回一个不超过给定长度的流。如果流是有序的，则最多会返回前n个元素。<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">``` java</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">limit</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        List&lt;Dish&gt; menu = init();</span><br><span class="line">        menu.stream()</span><br><span class="line">                .filter(d -&gt; d.getCalories() &gt; <span class="number">300</span>)</span><br><span class="line">                .limit(<span class="number">3</span>)</span><br><span class="line">                .forEach(System.out::println);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></li>
<li>skip()：跳过指定数量元素，返回一个扔掉了前n个元素的流。如果流中元素不足n个，则返回一个空流。<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">skip</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Dish&gt; menu = init();</span><br><span class="line">    menu.stream()</span><br><span class="line">            .filter(d -&gt; d.getCalories() &gt; <span class="number">300</span>)</span><br><span class="line">            .limit(<span class="number">3</span>)</span><br><span class="line">            .skip(<span class="number">2</span>)</span><br><span class="line">            .forEach(System.out::println);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="映射"><a href="#映射" class="headerlink" title="映射"></a>映射</h2><ul>
<li>map()：接受一个函数作为参数。这个函数会被应用到每个元素上，并将其映射成一个新的元素。<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">map</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Dish&gt; menu = init();</span><br><span class="line">    List&lt;String&gt; dishNames = menu.stream().map(m -&gt; m.getName()).collect(Collectors.toList());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>flatMap()：一个流中的每个值都换成另一个流，然后把所有的流连接起来成为一个流，即扁平化为一个流。<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">flatMap</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    String[] arrayOfWords = &#123;<span class="string">"Goodbye"</span>, <span class="string">"World"</span>&#125;;</span><br><span class="line">    List&lt;String&gt; words = Arrays.asList(arrayOfWords);</span><br><span class="line">    words.stream()</span><br><span class="line">            .map(w -&gt; w.split(<span class="string">""</span>))</span><br><span class="line">            .flatMap(Arrays::stream)</span><br><span class="line">            .distinct()</span><br><span class="line">            .forEach(System.out::println);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
上面例子中，<code>split()</code>得到的是String[] 而不是String，因此各个数组并不是分别映射成一个流，而是映射成流的内容。所有使用map(Arrays::stream)时生成的单个流都被合并起来，变为一个流。</li>
</ul>
<h2 id="匹配"><a href="#匹配" class="headerlink" title="匹配"></a>匹配</h2><p>匹配比较简单，返回一个boolean</p>
<ul>
<li>anyMatch()：至少匹配一个</li>
<li>allMatch()：全部匹配</li>
<li>noneMatch()：全部不匹配，和allMatch相反</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">anyMatch</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Dish&gt; menu = init();</span><br><span class="line">    Assert.assertEquals(<span class="keyword">true</span>, menu.stream().anyMatch(Dish::isVegetarian));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">allMatch</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Dish&gt; menu = init();</span><br><span class="line">    Assert.assertEquals(<span class="keyword">true</span>, menu.stream().allMatch(d -&gt; d.getCalories() &lt; <span class="number">1000</span>));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">noneMatch</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Dish&gt; menu = init();</span><br><span class="line">    Assert.assertEquals(<span class="keyword">true</span>, menu.stream().noneMatch(d -&gt; d.getCalories() &gt;= <span class="number">1000</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="查找"><a href="#查找" class="headerlink" title="查找"></a>查找</h2><p>查找有2个方法：<code>findFirst()</code>和<code>findAny()</code>，返回一个<code>Optional&lt;T&gt;</code>集合。<br>如果你不关心返回的元素是哪个，请使用<code>findAny()</code>，因为它在使用并行流时限制较少。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">findFirst</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Integer&gt; someNumbers = Arrays.asList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>);</span><br><span class="line">    Optional&lt;Integer&gt; firstSquareDivisibleByThree =</span><br><span class="line">            someNumbers.stream()</span><br><span class="line">                    .map(x -&gt; x * x)</span><br><span class="line">                    .filter(x -&gt; x % <span class="number">3</span> == <span class="number">0</span>)</span><br><span class="line">                    .findFirst(); <span class="comment">// 9</span></span><br><span class="line">    System.out.println(firstSquareDivisibleByThree.get());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">findAny</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Integer&gt; someNumbers = Arrays.asList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>);</span><br><span class="line">    Optional&lt;Integer&gt; firstSquareDivisibleByThree =</span><br><span class="line">            someNumbers.stream()</span><br><span class="line">                    .map(x -&gt; x * x)</span><br><span class="line">                    .filter(x -&gt; x % <span class="number">3</span> == <span class="number">0</span>)</span><br><span class="line">                    .findAny(); <span class="comment">// 9</span></span><br><span class="line">    System.out.println(firstSquareDivisibleByThree.get());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="归约"><a href="#归约" class="headerlink" title="归约"></a>归约</h2><p>归约在汇总结合内所有数据的时候使用。比如求 max，min，sum。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">reduce</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Integer&gt; numbers = Arrays.asList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>);</span><br><span class="line">    <span class="keyword">int</span> sum = numbers.stream().reduce(<span class="number">0</span>, Integer::sum);</span><br><span class="line">    System.out.println(sum);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="原始类型流特化"><a href="#原始类型流特化" class="headerlink" title="原始类型流特化"></a>原始类型流特化</h1><p>流在内部迭代的过程中，对基本类型会自动装箱和拆箱。为了避免不需要的装箱拆箱，Java8提供了<code>IntStream</code>、<code>DoubleStream</code>和<code>LongStream</code></p>
<ul>
<li>普通流转特化流：mapToInt(), mapToLong(), mapToDouble()</li>
<li>特化流转普通流：boxed()</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">boxedStream</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Dish&gt; menu = init();</span><br><span class="line">    <span class="comment">// 特化</span></span><br><span class="line">    IntStream intStream = menu.stream().mapToInt(Dish::getCalories);</span><br><span class="line">    <span class="comment">// 转回普通Stream</span></span><br><span class="line">    Stream&lt;Integer&gt; stream = intStream.boxed();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Java 8引入了两个可以用于IntStream和LongStream的静态方法，用于生成给定范围的数字流：</p>
<ul>
<li>range(min, max)：随机生成的数字不包含max，即(min, max)</li>
<li>rangeClosed(min, max)：随机生成的数字包含max，即(min, max]</li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java8</tag>
        <tag>Stream API</tag>
      </tags>
  </entry>
  <entry>
    <title>Java8学习笔记之Lambda表达式</title>
    <url>/2017/04/19/Java8%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%E4%B9%8BLambda%E8%A1%A8%E8%BE%BE%E5%BC%8F/</url>
    <content><![CDATA[<p><img src="http://upload-images.jianshu.io/upload_images/4059338-39a526e6dbd5707d.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Lambda.jpg"><br>使用Lambda表达式，我们可以很简洁地传递代码（通常是匿名函数）。</p>
<h1 id="结构"><a href="#结构" class="headerlink" title="结构"></a>结构</h1><p>Lambda表达式主要分为三部分：参数列表，箭头，Lambda 主体</p>
<a id="more"></a>
<h1 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h1><ul>
<li>(parameters) -&gt; expression</li>
<li>(parameters) -&gt; { statements; }</li>
</ul>
<p>如果表达式只有一行，用第一种，多行用第二种。</p>
<p>Java8中，标注了<code>@FunctionalInterface</code>，表明这个接口将是一个函数式接口，它里面只能有一个抽象方法。</p>
<h1 id="常用的函数式接口"><a href="#常用的函数式接口" class="headerlink" title="常用的函数式接口"></a>常用的函数式接口</h1><p>JDK已经为我们提供了很多常用的函数式接口：</p>
<ul>
<li>Predicate：<code>java.util.function.Predicate&lt;T&gt;</code>接口定义了一个名叫test的抽象方法，它接受泛型T对象，并返回一个boolean。在需要表示一个涉及类型T的布尔表达式时可以使用。</li>
<li>Consumer：<code>java.util.function.Consumer&lt;T&gt;</code>定义了一个名叫accept的抽象方法，它接受泛型T的对象，没有返回（void）。如果需要访问类型T的对象，并对其执行某些操作，就可以使用这个接口。</li>
<li>Supplier：<code>java.util.function.Supplier&lt;T&gt;</code>不接受对象，返回一个泛型对象T。在需要new一个对象实例时可以使用。</li>
<li>Function：<code>java.util.function.Function&lt;T, R&gt;</code>接口定义了一个叫作apply的方法，它接受一个泛型T的对象，并返回一个泛型R的对象。如果需要定义一个Lambda，将输入对象的信息映射到输出，就可以使用这个接口。</li>
</ul>
<h1 id="原始类型特化"><a href="#原始类型特化" class="headerlink" title="原始类型特化"></a>原始类型特化</h1><p>我们知道，泛型只能绑定到引用类型的对象。因此，在使用泛型绑定基本类型的时候，Java会为我们自动装箱和拆箱，但这是会消耗性能的。<br>如果输入和输出都是基本类型时，Java8为我们提供了新的函数式接口，以避免自动装箱拆箱。</p>
<p>简单列举一部分：</p>
<ul>
<li>Predicate：<code>IntPredicate</code>, <code>LongPredicate</code>, <code>DoublePredicate</code></li>
<li>Consumer：<code>IntConsumer</code>,<code>LongConsumer</code>, <code>DoubleConsumer</code></li>
<li>Supplier：<code>BooleanSupplier</code>, <code>IntSupplier</code>, <code>LongSupplier</code>, <code>DoubleSupplier</code></li>
<li>Function：<code>IntFunction&lt;R&gt;</code>,<code>LongToDoubleFunction</code>,<code>ToLongFunction&lt;T&gt;</code></li>
</ul>
<p>从命名可以轻易看出从什么类型转成什么类型，可以在<code>java.util.function</code>包下查看所有接口。</p>
<h1 id="使用局部变量"><a href="#使用局部变量" class="headerlink" title="使用局部变量"></a>使用局部变量</h1><p>在使用lambda时，主体代码块内允许使用的外部变量。但是，不允许改变外部变量。这些变量应该声明为<code>final</code>或者事实上是<code>final</code>的（即之后代码中不会改变）</p>
<h1 id="方法引用"><a href="#方法引用" class="headerlink" title="方法引用"></a>方法引用</h1><p>方法引用主要有三类：</p>
<ul>
<li>指向静态方法的方法引用<ul>
<li>Lambda: <code>(args) -&gt; ClassName.staticMethod(args)</code></li>
<li>方法引用：<code>ClassName :: staticMethod</code></li>
</ul>
</li>
<li>指向任意类型实例方法的方法引用<ul>
<li>Lambda: <code>(arg0, rest) -&gt; arg0.instanceMethod(rest)</code></li>
<li>方法引用：<code>ClassName :: instanceMethod</code>(arg0 是 ClassName 类型的)</li>
</ul>
</li>
<li>指向现有对象的实例方法的方法引用<ul>
<li>Lambda: <code>(args) -&gt; expr.instanceMethod(args)</code></li>
<li>方法引用：<code>expr :: intanceMethod</code></li>
</ul>
</li>
</ul>
<p>除此之外，还有构造函数引用：<code>ClassName :: new</code><br>比如用Map来将构造函数映射到字符串值：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> Map&lt;String, Function&lt;Integer, Fruit&gt;&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line"><span class="keyword">static</span> &#123;</span><br><span class="line">    map.put(<span class="string">"apple"</span>, Apple::<span class="keyword">new</span>);</span><br><span class="line">    map.put(<span class="string">"orange"</span>, Orange::<span class="keyword">new</span>);</span><br><span class="line">    <span class="comment">// etc...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Fruit <span class="title">giveMeFruit</span><span class="params">(String fruit, Integer weight)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> map.get(fruit.toLowerCase()).apply(weight);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="复合-Lambda-表达式"><a href="#复合-Lambda-表达式" class="headerlink" title="复合 Lambda 表达式"></a>复合 Lambda 表达式</h1><p>Comparator、Predicate和Function等函数式接口都有几个可以用来结Lambda表达式的默认方法。</p>
<h2 id="比较器复合"><a href="#比较器复合" class="headerlink" title="比较器复合"></a>比较器复合</h2><ol>
<li>普通排序<code>comparing()</code><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Comparator&lt;Apple&gt; c = Comparator.comparing(Apple::getWeight);</span><br></pre></td></tr></table></figure></li>
<li>逆序<code>reversed()</code><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">inventory.sort(comparing(Apple::getWeight).reversed());</span><br></pre></td></tr></table></figure></li>
<li>比较器链<code>thenComparing()</code><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">inventory.sort(comparing(Apple::getWeight).reversed()</span><br><span class="line">    .thenComparing(Apple::getCountry));</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="谓词复合"><a href="#谓词复合" class="headerlink" title="谓词复合"></a>谓词复合</h2><p>3个方法增强已有的Predicate接口：</p>
<ul>
<li><code>and</code>：与</li>
<li><code>or</code>：或</li>
<li><code>negate</code>：非</li>
</ul>
<p><strong>请注意，and和or方法是按照在表达式链中的位置，从左向右确定优先级的。</strong>因此，a.or(b).and(c)可以看作(a || b) &amp;&amp; c。</p>
<h2 id="函数复合"><a href="#函数复合" class="headerlink" title="函数复合"></a>函数复合</h2><p>Function接口有<code>andThen</code>和<code>compose</code>两个默认方法，它们都会返回Function的一个实例。</p>
<p>举个例子：<br>有2个函数，一个加1，一个乘2</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Function&lt;Integer, Integer&gt; f = x -&gt; x + <span class="number">1</span>; <span class="comment">// f(x)=x+1</span></span><br><span class="line">Function&lt;Integer, Integer&gt; g = x -&gt; x * <span class="number">2</span>; <span class="comment">// g(x)=2x</span></span><br></pre></td></tr></table></figure>
<ol>
<li><p><code>andThen()</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Function&lt;Integer, Integer&gt; h = f.andThen(g); <span class="comment">// g(f(x))</span></span><br><span class="line"><span class="keyword">int</span> result = h.apply(<span class="number">1</span>); <span class="comment">// 4</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>compose()</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Function&lt;Integer, Integer&gt; h = f.compose(g); <span class="comment">// f(g(x))</span></span><br><span class="line"><span class="keyword">int</span> result = h.apply(<span class="number">1</span>); <span class="comment">// 3</span></span><br></pre></td></tr></table></figure>
</li>
</ol>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Lambda表达式</tag>
        <tag>java8</tag>
      </tags>
  </entry>
  <entry>
    <title>Java8学习笔记之行为参数化</title>
    <url>/2017/04/17/Java8%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%E4%B9%8B%E8%A1%8C%E4%B8%BA%E5%8F%82%E6%95%B0%E5%8C%96/</url>
    <content><![CDATA[<p>用一个例子说明<code>行为参数化</code>带来的变化 - 从苹果仓库中筛选苹果</p>
<h1 id="版本1"><a href="#版本1" class="headerlink" title="版本1"></a>版本1</h1><p>从一个苹果集合中选出绿的苹果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> List&lt;Apple&gt; <span class="title">filterGreenApples</span><span class="params">(List&lt;Apple&gt; inventory)</span> </span>&#123;</span><br><span class="line">        List&lt;Apple&gt; result = <span class="keyword">new</span> ArrayList&lt;Apple&gt;();</span><br><span class="line">        <span class="keyword">for</span> (Apple apple : inventory) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="string">"green"</span>.equals(apple.getColor()) &#123;</span><br><span class="line">                result.add(apple);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<a id="more"></a>
<h1 id="版本2"><a href="#版本2" class="headerlink" title="版本2"></a>版本2</h1><p>这时，如果需求变了，要从集合中选出红苹果，我们会这样</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> List&lt;Apple&gt; <span class="title">filterApplesByColor</span><span class="params">(List&lt;Apple&gt; inventory, String color)</span> </span>&#123;</span><br><span class="line">        List&lt;Apple&gt; result = <span class="keyword">new</span> ArrayList&lt;Apple&gt;();</span><br><span class="line">        <span class="keyword">for</span> (Apple apple : inventory) &#123;</span><br><span class="line">            <span class="keyword">if</span> (apple.getColor().equals(color)) &#123;</span><br><span class="line">                result.add(apple);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>然后传入颜色参数来筛选</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;Apple&gt; apples = filterApplesByColor(inventory, <span class="string">"red"</span>);</span><br></pre></td></tr></table></figure>

<h1 id="版本3"><a href="#版本3" class="headerlink" title="版本3"></a>版本3</h1><p>但是，如果现在要选出重量超过150g的苹果呢？在方法参数列表中多加一个weight么？<br>你会发现我们所有的代码，只有if判断中的条件发生了变化，这违反了DRY原则(Don’t Repeat Yourself)。</p>
<p>所以，我们把整个具体行为作为参数来传递，这样，方法体本身的代码就可以复用了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义一个接口</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">ApplePredicate</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">test</span><span class="params">(Apple apple)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AppleHeavyWeightPredicate</span> <span class="keyword">implements</span> <span class="title">ApplePredicate</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">test</span><span class="params">(Apple apple)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> apple.getWeight() &gt; <span class="number">150</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AppleGreenColorPredicate</span> <span class="keyword">implements</span> <span class="title">ApplePredicate</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">test</span><span class="params">(Apple apple)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"green"</span>.equals(apple.getColor());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> List&lt;Apple&gt; <span class="title">filterApples</span><span class="params">(List&lt;Apple&gt; inventory, ApplePredicate p)</span> </span>&#123;</span><br><span class="line">    List&lt;Apple&gt; result = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    <span class="keyword">for</span> (Apple apple : inventory) &#123;</span><br><span class="line">        <span class="keyword">if</span> (p.test(apple)) &#123;</span><br><span class="line">            result.add(apple);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>现在，我们可以很灵活的调用了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;Apple&gt; redAndHeavyApples = filterApples(inventory, <span class="keyword">new</span> AppleHeavyWeightPredicate());</span><br></pre></td></tr></table></figure>

<h1 id="版本4"><a href="#版本4" class="headerlink" title="版本4"></a>版本4</h1><p>其实，接口的具体实现，我们只会用到一次。所以，我们可以改成匿名类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;Apple&gt; redApples = filterApples(inventory, <span class="keyword">new</span> ApplePredicate() &#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">test</span><span class="params">(Apple apple)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"red"</span>.equals(apple.getColor());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>
<p>现在，代码已经变得非常简洁和灵活了。</p>
<h1 id="版本5"><a href="#版本5" class="headerlink" title="版本5"></a>版本5</h1><p>从Java8开始，我们可以利用Lambda表达式，进一步改进代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;Apple&gt; result = filterApples(inventory, (Apple apple) -&gt; <span class="string">"red"</span>.equals(apple.getColor()));</span><br></pre></td></tr></table></figure>
<p>现在，调用方法，我们只要一行代码，而且代码的可读性非常好。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Lambda表达式</tag>
        <tag>java8</tag>
      </tags>
  </entry>
  <entry>
    <title>HTML转图片利器：wkhtmltox</title>
    <url>/2017/04/01/HTML%E8%BD%AC%E5%9B%BE%E7%89%87%E5%88%A9%E5%99%A8%EF%BC%9Awkhtmltox/</url>
    <content><![CDATA[<p>关于<code>wkhtmltox</code>，是一个可以把HTML转换为图片和pdf的工具。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/4059338-27a3b4979a3084c2.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="wkhtmltox.jpg"></p>
<p>不多介绍了，详见官网 <a href="https://wkhtmltopdf.org/" target="_blank" rel="noopener">https://wkhtmltopdf.org/</a></p>
<a id="more"></a>
<h1 id="安装步骤"><a href="#安装步骤" class="headerlink" title="安装步骤"></a>安装步骤</h1><ol>
<li><p>下载下来是tar.xz文件，首先解压：<br><code>tar -vxf wkhtmltox-0.12.4_linux-generic-amd64.tar.xz</code></p>
</li>
<li><p>解压得到一个目录wkhtmltox，把wkhtmltoimage和wkhtmltopdf复制到<code>/usr/bin</code>目录，更改所有者，并增加可执行属性</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo cp wkhtmltox/bin/wkhtmltopdf /usr/bin/</span><br><span class="line">sudo cp wkhtmltox/bin/wkhtmltoimage /usr/bin/</span><br><span class="line">sudo chown root:root /usr/bin/wkhtmltopdf</span><br><span class="line">sudo chown root:root /usr/bin/wkhtmltoimage</span><br><span class="line">sudo chmod +x /usr/bin/wkhtmltopdf</span><br><span class="line">sudo chmod +x /usr/bin/wkhtmltoimage</span><br></pre></td></tr></table></figure>
</li>
<li><p>注意事项</p>
</li>
</ol>
<p>有时候为了让程序可以执行这个命令，可能需要配置环境变量</p>
<ul>
<li>编辑配置文件 <code>vi .bashrc</code></li>
<li>添加环境变量 <code>export PATH=$PATH:/opt/wkhtmltox/bin</code></li>
<li>让修改生效 <code>source .bashrc</code>让修改生效</li>
</ul>
<p>如果网页上的中文，转成图片后变成乱码方块，请安装相关字体 :)</p>
<h1 id="关于使用"><a href="#关于使用" class="headerlink" title="关于使用"></a>关于使用</h1><p>执行<code>wkhtmltoimage www.bing.com bing.png</code>，就会在当前目录下生成了一张png图片。<br><img src="http://upload-images.jianshu.io/upload_images/4059338-b81b7a7129404297.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="bing.png"></p>
<p>这个命令还可以增加一些参数，比如常用的设置宽高，图片质量等参数<br>执行<code>wkhtmltoimage --crop-w 410 --width 410 --quality 50 www.bing.com bing2.png</code>看看</p>
<p><img src="http://upload-images.jianshu.io/upload_images/4059338-3c6b126b35acc5f8.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="bing2.png"></p>
<p>说明:</p>
<ul>
<li><code>--crop-w 410</code>：截图宽度410px</li>
<li><code>--width 410</code>：浏览器模拟宽度410px</li>
<li><code>--quality 50</code>：图片质量(这个值越大，图片质量越高，当然文件也会比较大)</li>
</ul>
<p>还有更多参数用法，请 <code>wkhtmltoimage -h</code>查看。<br>HTML转pdf同理，<code>wkhtmltopdf -h</code>。</p>
]]></content>
      <categories>
        <category>日常记录</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>wkhtmltox</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot中使用Redis实现缓存</title>
    <url>/2017/03/30/SpringBoot%E4%B8%AD%E4%BD%BF%E7%94%A8Redis%E5%AE%9E%E7%8E%B0%E7%BC%93%E5%AD%98/</url>
    <content><![CDATA[<p>Spring Data Redis为我们封装了Redis客户端的各种操作，简化使用。</p>
<ul>
<li>当Redis当做数据库或者消息队列来操作时，我们一般使用RedisTemplate来操作</li>
<li>当Redis作为缓存使用时，我们可以将它作为Spring Cache的实现，直接通过注解使用</li>
</ul>
<p>关于RedisTemplate的使用可参考：<a href="http://blog.didispace.com/springbootredis/" target="_blank" rel="noopener">http://blog.didispace.com/springbootredis/</a></p>
<h1 id="下面总结使用Redis作为缓存"><a href="#下面总结使用Redis作为缓存" class="headerlink" title="下面总结使用Redis作为缓存"></a>下面总结使用Redis作为缓存</h1><h2 id="引入依赖"><a href="#引入依赖" class="headerlink" title="引入依赖"></a>引入依赖</h2><p>SpringBoot从1.4版本开始，<code>spring-boot-starter-redis</code>依赖改名了。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-data-redis<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.5.2.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<a id="more"></a>
<h2 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h2><figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">redis:</span></span><br><span class="line">    <span class="attr">host:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span></span><br><span class="line">    <span class="attr">port:</span> <span class="number">6379</span></span><br><span class="line">    <span class="attr">timeout:</span> <span class="number">0</span></span><br><span class="line">    <span class="attr">database:</span> <span class="number">0</span></span><br><span class="line">    <span class="attr">pool:</span></span><br><span class="line">      <span class="attr">max-active:</span> <span class="number">8</span></span><br><span class="line">      <span class="attr">max-wait:</span> <span class="number">-1</span></span><br><span class="line">      <span class="attr">max-idle:</span> <span class="number">8</span></span><br><span class="line">      <span class="attr">min-idle:</span> <span class="number">0</span></span><br></pre></td></tr></table></figure>
<p>这样，SpringBoot将会自动配置redis，注入相关bean，我们就可以使用<code>@CacheConfig</code>，<code>@Cacheable</code>，<code>@CachePut</code>，<code>@CacheEvict</code>了。</p>
<h1 id="使用Cache注解时的问题"><a href="#使用Cache注解时的问题" class="headerlink" title="使用Cache注解时的问题"></a>使用Cache注解时的问题</h1><p>缓存对象集合中，缓存是以key-value形式保存的。当不指定缓存的key时，SpringBoot会使用<code>SimpleKeyGenerator</code>生成key。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleKeyGenerator</span> <span class="keyword">implements</span> <span class="title">KeyGenerator</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> Object <span class="title">generate</span><span class="params">(Object target, Method method, Object... params)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> generateKey(params);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Generate a key based on the specified parameters.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title">generateKey</span><span class="params">(Object... params)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (params.length == <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> SimpleKey.EMPTY;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (params.length == <span class="number">1</span>) &#123;</span><br><span class="line">			Object param = params[<span class="number">0</span>];</span><br><span class="line">			<span class="keyword">if</span> (param != <span class="keyword">null</span> &amp;&amp; !param.getClass().isArray()) &#123;</span><br><span class="line">				<span class="keyword">return</span> param;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> SimpleKey(params);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">SimpleKey</span><span class="params">(Object... elements)</span> </span>&#123;</span><br><span class="line">	Assert.notNull(elements, <span class="string">"Elements must not be null"</span>);</span><br><span class="line">	<span class="keyword">this</span>.params = <span class="keyword">new</span> Object[elements.length];</span><br><span class="line">	System.arraycopy(elements, <span class="number">0</span>, <span class="keyword">this</span>.params, <span class="number">0</span>, elements.length);</span><br><span class="line">	<span class="keyword">this</span>.hashCode = Arrays.deepHashCode(<span class="keyword">this</span>.params);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>查看源码可以发现，它是使用方法参数组合生成的一个key。<br><strong>此时有一个问题：</strong><br>如果2个方法，参数是一样的，但执行逻辑不同，那么将会导致执行第二个方法时命中第一个方法的缓存。<br>解决办法是在<code>@Cacheable</code>注解参数中指定key，或者自己实现一个<code>KeyGenerator</code>，在注解中指定KeyGenerator。<br>但是如果这样的情况很多，每一个都要指定key、KeyGenerator很麻烦。</p>
<p>Spring同样提供了方案：继承<code>CachingConfigurerSupport</code>并重写<code>keyGenerator()</code></p>
<p>下面贴出代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@EnableCaching</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RedisCacheConfig</span> <span class="keyword">extends</span> <span class="title">CachingConfigurerSupport</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> JedisConnectionFactory jedisConnectionFactory;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> RedisTemplate <span class="title">redisTemplate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        StringRedisTemplate redisTemplate = <span class="keyword">new</span> StringRedisTemplate(jedisConnectionFactory);</span><br><span class="line"></span><br><span class="line">        Jackson2JsonRedisSerializer jackson2JsonRedisSerializer = <span class="keyword">new</span> Jackson2JsonRedisSerializer&lt;&gt;(Object<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">        ObjectMapper om = <span class="keyword">new</span> ObjectMapper();</span><br><span class="line">        om.setVisibility(PropertyAccessor.ALL, JsonAutoDetect.Visibility.ANY);</span><br><span class="line">        om.enableDefaultTyping(ObjectMapper.DefaultTyping.NON_FINAL);</span><br><span class="line">        jackson2JsonRedisSerializer.setObjectMapper(om);</span><br><span class="line"></span><br><span class="line">        redisTemplate.setValueSerializer(jackson2JsonRedisSerializer);</span><br><span class="line">        redisTemplate.afterPropertiesSet();</span><br><span class="line">        <span class="keyword">return</span> redisTemplate;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> CacheManager <span class="title">cacheManager</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        String[] cacheNames = &#123;<span class="string">"app_default"</span>, <span class="string">"users"</span>, <span class="string">"blogs"</span>, <span class="string">"goods"</span>, <span class="string">"configs"</span>, <span class="string">"info"</span>&#125;;</span><br><span class="line">        RedisCacheManager redisCacheManager = <span class="keyword">new</span> RedisCacheManager(redisTemplate(), Arrays.asList(cacheNames));</span><br><span class="line">        redisCacheManager.setDefaultExpiration(<span class="number">86400</span>);</span><br><span class="line">        <span class="keyword">return</span> redisCacheManager;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Cache <span class="title">cache</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> cacheManager().getCache(<span class="string">"app_default"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> KeyGenerator <span class="title">keyGenerator</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> (target, method, objects) -&gt; &#123;</span><br><span class="line">            StringBuilder sb = <span class="keyword">new</span> StringBuilder();</span><br><span class="line">            sb.append(target.getClass().getName());</span><br><span class="line">            sb.append(<span class="string">"::"</span> + method.getName() + <span class="string">":"</span>);</span><br><span class="line">            <span class="keyword">for</span> (Object obj : objects) &#123;</span><br><span class="line">                sb.append(obj.toString());</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> sb.toString();</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>此时，缓存的key是包名+方法名+参数列表，这样就很难会冲突了。</p>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
        <tag>Redis</tag>
        <tag>Cache</tag>
      </tags>
  </entry>
  <entry>
    <title>手动安装redis-3.2.8的详细步骤</title>
    <url>/2017/03/21/%E6%89%8B%E5%8A%A8%E5%AE%89%E8%A3%85redis-3.2.8%E7%9A%84%E8%AF%A6%E7%BB%86%E6%AD%A5%E9%AA%A4/</url>
    <content><![CDATA[<p>CentOS6.7使用yum安装有时候没有比较新的版本，所以手动安装，下面记录一下步骤。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/4059338-13f399d148cd1ad3.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="redis"></p>
<h1 id="下载最新版本"><a href="#下载最新版本" class="headerlink" title="下载最新版本"></a>下载最新版本</h1><p>以3.2.8为例，附上地址：<a href="https://redis.io/" target="_blank" rel="noopener">redis-3.2.8.tar.gz</a></p>
<a id="more"></a>
<h1 id="解压，编译，安装redis"><a href="#解压，编译，安装redis" class="headerlink" title="解压，编译，安装redis"></a>解压，编译，安装redis</h1><ul>
<li>解压：<code>tar -zxvf redis-3.2.8.tar.gz</code></li>
<li>进入目录：<code>cd redis-3.2.8</code></li>
<li>编译：<code>make &amp;&amp; make install</code></li>
<li>创建相关目录：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkdir -p &#x2F;opt&#x2F;redis-3.2.8&#x2F;bin</span><br><span class="line">mkdir -p &#x2F;opt&#x2F;redis-3.2.8&#x2F;log</span><br><span class="line">mkdir -p &#x2F;opt&#x2F;redis-3.2.8&#x2F;pid</span><br><span class="line">mkdir -p &#x2F;opt&#x2F;redis-3.2.8&#x2F;db</span><br></pre></td></tr></table></figure></li>
<li>将编译后的可执行文件复制到自己的安装目录：<code>ln -s /usr/local/bin/redis-* /opt/redis-3.2.8/bin</code></li>
<li>复制配置文件到安装目录：<code>cp redis.conf /opt/redis-3.2.8/</code></li>
</ul>
<h1 id="配置redis"><a href="#配置redis" class="headerlink" title="配置redis"></a>配置redis</h1><ul>
<li>编辑redis.conf：<code>cd /opt/redis-3.2.8</code>，<code>vi redis.conf</code><ul>
<li>redis默认只允许本机连接，所以注释掉这行配置就可以远程访问：<code>\# bind 127.0.0.1</code></li>
<li>redis3.0版本增加了保护模式，需要我们设置密码，如果不想设置密码，就关闭保护模式：<code>protected-mode no</code></li>
<li>设置redis以守护线程方式启动：<code>daemonize yes</code></li>
<li>配置pid，log，db文件的保存地址：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pidfile &#x2F;opt&#x2F;redis-3.2.8&#x2F;pid&#x2F;redis.pid</span><br><span class="line">logfile &#x2F;opt&#x2F;redis-3.2.8&#x2F;log&#x2F;redis.log</span><br><span class="line">dir &#x2F;opt&#x2F;redis-3.2.8&#x2F;db</span><br></pre></td></tr></table></figure>
其他配置就默认即可，有需要再自行修改</li>
</ul>
</li>
<li>编写redis启动脚本：<code>vi /etc/init.d/redis</code><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/sh</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Simple Redis init.d script conceived to work on Linux systems</span></span><br><span class="line"><span class="comment"># as it does use of the /proc filesystem.</span></span><br><span class="line">PATH=/opt/redis-3.2.8/bin:/sbin:/usr/bin:/bin</span><br><span class="line">REDISPORT=6379</span><br><span class="line">EXEC=/opt/redis-3.2.8/bin/redis-server</span><br><span class="line">CLIEXEC=/opt/redis-3.2.8/bin/redis-cli</span><br><span class="line">PIDFILE=/opt/redis-3.2.8/pid/redis.pid</span><br><span class="line">CONF=<span class="string">"/opt/redis-3.2.8/redis.conf"</span></span><br><span class="line"><span class="keyword">case</span> <span class="string">"<span class="variable">$1</span>"</span> <span class="keyword">in</span></span><br><span class="line">    start)</span><br><span class="line">        <span class="keyword">if</span> [ -f <span class="variable">$PIDFILE</span> ]</span><br><span class="line">        <span class="keyword">then</span></span><br><span class="line">                <span class="built_in">echo</span> <span class="string">"<span class="variable">$PIDFILE</span> exists, process is already running or crashed"</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">                <span class="built_in">echo</span> <span class="string">"Starting Redis server..."</span></span><br><span class="line">                <span class="variable">$EXEC</span> <span class="variable">$CONF</span></span><br><span class="line">        <span class="keyword">fi</span></span><br><span class="line">        ;;</span><br><span class="line">    stop)</span><br><span class="line">        <span class="keyword">if</span> [ ! -f <span class="variable">$PIDFILE</span> ]</span><br><span class="line">        <span class="keyword">then</span></span><br><span class="line">                <span class="built_in">echo</span> <span class="string">"<span class="variable">$PIDFILE</span> does not exist, process is not running"</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">                PID=$(cat <span class="variable">$PIDFILE</span>)</span><br><span class="line">                <span class="built_in">echo</span> <span class="string">"Stopping ..."</span></span><br><span class="line">                <span class="variable">$CLIEXEC</span> -p <span class="variable">$REDISPORT</span> shutdown</span><br><span class="line">                <span class="keyword">while</span> [ -x /proc/<span class="variable">$&#123;PID&#125;</span> ]</span><br><span class="line">                <span class="keyword">do</span></span><br><span class="line">                    <span class="built_in">echo</span> <span class="string">"Waiting for Redis to shutdown ..."</span></span><br><span class="line">                    sleep 1</span><br><span class="line">                <span class="keyword">done</span></span><br><span class="line">                <span class="built_in">echo</span> <span class="string">"Redis stopped"</span></span><br><span class="line">        <span class="keyword">fi</span></span><br><span class="line">        ;;</span><br><span class="line">    *)</span><br><span class="line">        <span class="built_in">echo</span> <span class="string">"Please use start or stop as first argument"</span></span><br><span class="line">        ;;</span><br><span class="line"><span class="keyword">esac</span></span><br></pre></td></tr></table></figure></li>
<li>设置服务权限：<code>chmod a+x /etc/init.d/redis</code></li>
</ul>
<h1 id="相关使用"><a href="#相关使用" class="headerlink" title="相关使用"></a>相关使用</h1><ul>
<li>启动：<code>service redis start</code></li>
<li>关闭：<code>service redis stop</code></li>
<li>查看：<code>ps -ef | grep redis</code>，<code>netstat -anptu | grep 6379</code></li>
</ul>
]]></content>
      <categories>
        <category>NoSQL</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>谈谈Java中的代理</title>
    <url>/2017/02/17/%E8%B0%88%E8%B0%88Java%E4%B8%AD%E7%9A%84%E4%BB%A3%E7%90%86/</url>
    <content><![CDATA[<p>代理是Java常用的设计模式，代理类通过调用被代理类的相关方法，实现对相关方法增强。比如加入事务、日志、报警发邮件等操作。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/4059338-83f70e36399f4ad0.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="java.png"></p>
<a id="more"></a>
<h1 id="静态代理"><a href="#静态代理" class="headerlink" title="静态代理"></a>静态代理</h1><p>静态代理，就是由程序员手动编写代理类或者用工具生成代理类的代码，再进行编译生成class文件，实现代理。比如简单工厂模式。</p>
<p>用法：</p>
<ul>
<li>代理类和目标类都实现相同接口。</li>
<li>代理类持有目标类的引用。</li>
</ul>
<p>缺点：<br>静态代理要为每个目标类创建一个代理类，当需要代理的对象太多，那么代理类也变得很多。代理类违背了可重复代理只写一次的原则。</p>
<h1 id="动态代理"><a href="#动态代理" class="headerlink" title="动态代理"></a>动态代理</h1><p>为了解决静态代理的缺点，于是引入了动态代理。<br>它有一个好处，那就是不用写很多代理类，生成的代理类数量是固定的。<br>一般动态代理分为2种：</p>
<h2 id="JDK动态代理"><a href="#JDK动态代理" class="headerlink" title="JDK动态代理"></a>JDK动态代理</h2><p>JDK动态代理是JDK自带的，不依赖第三方框架。<br>它的实现原理，就是利用Java的反射机制，创建一个实现接口的代理类。</p>
<p>用法：</p>
<ul>
<li>被代理对象必须实现接口。</li>
<li>代理对象由代理工厂自动生成。</li>
</ul>
<p>下面贴个例子</p>
<p>接口类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Subject</span> </span>&#123;   </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span></span>;   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>实现类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RealSubject</span> <span class="keyword">implements</span> <span class="title">Subject</span> </span>&#123;   </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span> </span>&#123;   </span><br><span class="line">    System.out.println(<span class="string">"do 了 some thing ..."</span>);   </span><br><span class="line">  &#125;   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>代理工厂：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.lang.reflect.InvocationHandler;  </span><br><span class="line"><span class="keyword">import</span> java.lang.reflect.Method;  </span><br><span class="line"><span class="keyword">import</span> java.lang.reflect.Proxy;  </span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ProxyHandler</span> <span class="keyword">implements</span> <span class="title">InvocationHandler</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Object target;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//绑定委托对象，并返回代理类</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">bind</span><span class="params">(Object target)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.target = target;</span><br><span class="line">        <span class="comment">//绑定该类实现的所有接口，取得代理类 </span></span><br><span class="line">        <span class="keyword">return</span> Proxy.newProxyInstance(target.getClass().getClassLoader(),</span><br><span class="line">                target.getClass().getInterfaces(), <span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line">	</span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">invoke</span><span class="params">(Object proxy , Method method , Object[] args)</span><span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">        Object result = <span class="keyword">null</span>;</span><br><span class="line">        <span class="comment">//这里就可以进行所谓的AOP编程了</span></span><br><span class="line">        <span class="comment">//在调用具体函数方法前，执行功能处理</span></span><br><span class="line">        result = method.invoke(target, args);</span><br><span class="line">        <span class="comment">//在调用具体函数方法后，执行功能处理</span></span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>测试类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestProxy</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String args[])</span> </span>&#123;</span><br><span class="line">           ProxyHandler proxy = <span class="keyword">new</span> ProxyHandler();</span><br><span class="line">           <span class="comment">//绑定该类实现的所有接口</span></span><br><span class="line">           Subject sub = (Subject) proxy.bind(<span class="keyword">new</span> RealSubject());</span><br><span class="line">           sub.doSomething();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="CGLIB代理"><a href="#CGLIB代理" class="headerlink" title="CGLIB代理"></a>CGLIB代理</h2><p>使用CGLIB代理需要引入CGLIB库，它使用字节码技术实现代理。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.lang.reflect.Method;</span><br><span class="line"><span class="keyword">import</span> net.sf.cglib.proxy.Enhancer;</span><br><span class="line"><span class="keyword">import</span> net.sf.cglib.proxy.MethodInterceptor;</span><br><span class="line"><span class="keyword">import</span> net.sf.cglib.proxy.MethodProxy;</span><br><span class="line">      </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CGLibProxy</span> <span class="keyword">implements</span> <span class="title">MethodInterceptor</span> </span>&#123;    </span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> Object targetObject;<span class="comment">// CGLib需要代理的目标对象    </span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">createProxyObject</span><span class="params">(Object obj)</span> </span>&#123;    </span><br><span class="line">        <span class="keyword">this</span>.targetObject = obj;    </span><br><span class="line">        Enhancer enhancer = <span class="keyword">new</span> Enhancer();    </span><br><span class="line">        enhancer.setSuperclass(obj.getClass());    </span><br><span class="line">        enhancer.setCallback(<span class="keyword">this</span>);    </span><br><span class="line">        Object proxyObj = enhancer.create();    </span><br><span class="line">        <span class="keyword">return</span> proxyObj;<span class="comment">// 返回代理对象    </span></span><br><span class="line">    &#125;    </span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">intercept</span><span class="params">(Object proxy, Method method, Object[] args,    </span></span></span><br><span class="line"><span class="function"><span class="params">            MethodProxy methodProxy)</span> <span class="keyword">throws</span> Throwable </span>&#123;    </span><br><span class="line">        Object obj = <span class="keyword">null</span>;    </span><br><span class="line">        <span class="keyword">if</span> (<span class="string">"addUser"</span>.equals(method.getName())) &#123;<span class="comment">// 过滤方法    </span></span><br><span class="line">            checkPopedom();<span class="comment">// 检查权限    </span></span><br><span class="line">        &#125;    </span><br><span class="line">        obj = method.invoke(targetObject, args);    </span><br><span class="line">        <span class="keyword">return</span> obj;    </span><br><span class="line">    &#125;    </span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">checkPopedom</span><span class="params">()</span> </span>&#123;    </span><br><span class="line">        System.out.println(<span class="string">"检查权限  checkPopedom()!"</span>);    </span><br><span class="line">    &#125;    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">		Subject sub = (Subject) <span class="keyword">new</span> CGLibProxy().createProxyObject(<span class="keyword">new</span> RealSubject());</span><br><span class="line">		sub.doSomething();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="2种动态代理的区别"><a href="#2种动态代理的区别" class="headerlink" title="2种动态代理的区别"></a>2种动态代理的区别</h2><p>JDK动态代理是利用反射机制生成一个实现代理接口的匿名类，在调用具体方法前调用InvokeHandler来处理。<br>而CGLIB动态代理是利用asm开源包，加载代理对象类的class文件，修改其字节码生成子类来处理。</p>
<p>在 Spring 中，</p>
<ul>
<li>如果目标对象实现了接口，默认情况下会采用JDK动态代理实现AOP </li>
<li>如果目标对象实现了接口，可以强制使用CGLIB实现AOP </li>
<li>如果目标对象没有实现了接口，必须采用CGLIB库，Spring会自动在JDK动态代理和CGLIB之间转换</li>
</ul>
<p>如何强制使用CGLIB实现AOP？</p>
<ul>
<li>添加CGLIB依赖</li>
<li>在Spring配置文件中加入<code>&lt;aop:aspectj-autoproxy proxy-target-class=&quot;true&quot;/&gt;</code></li>
<li>如果是SpringBoot，在配置文件设置<code>spring.aop.proxy-target-class=true</code></li>
</ul>
<p>JDK动态代理和CGLIB字节码生成的区别？</p>
<ul>
<li>JDK动态代理只能对实现了接口的类生成代理，而不能针对未实现接口的类</li>
<li>CGLIB是针对类实现代理，主要是对指定的类生成一个子类，覆盖其中的方法</li>
<li>因为是继承，所以该类或方法最好不要声明成final</li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>动态代理</tag>
      </tags>
  </entry>
  <entry>
    <title>理解JVM（一）：JVM内存结构</title>
    <url>/2017/02/16/%E7%90%86%E8%A7%A3JVM%EF%BC%88%E4%B8%80%EF%BC%89%EF%BC%9AJVM%E5%86%85%E5%AD%98%E7%BB%93%E6%9E%84/</url>
    <content><![CDATA[<p><strong>在<code>JVM</code>中，内存主要被分为5类</strong><br><img src="http://upload-images.jianshu.io/upload_images/4059338-8a05e9cb9a61a962.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="JVM内存结构.jpg"></p>
<a id="more"></a>
<h2 id="堆Heap"><a href="#堆Heap" class="headerlink" title="堆Heap"></a>堆<code>Heap</code></h2><ul>
<li>虚拟机内存管理中最大的一块内存空间。</li>
<li>存放关键字<code>new</code>创建的对象实例和数组。</li>
<li>堆内存被所有线程共享。</li>
<li>这块内存区由<code>JVM</code>（Java虚拟机）自己管理。当使用new创建对象时，不必指定分配空间的大小，<code>JVM</code>会动态自动分配一块区域；在程序执行过程中，没有指向此对象的引用时，此对象就被标记为可被回收状态，将由<code>GC</code>（垃圾回收器）在一个不确定的时间自动回收，释放所占的内存空间。</li>
<li>从内存回收的角度看，垃圾收集器大都基于分代收集算法，所以堆一般分为<code>新生代</code>和<code>老年代</code>，更细致可划分为：<code>Eden空间</code>，<code>From Survivor空间</code>，<code>To Survivor空间</code>。</li>
</ul>
<h2 id="虚拟机栈VM-Stack"><a href="#虚拟机栈VM-Stack" class="headerlink" title="虚拟机栈VM Stack"></a>虚拟机栈<code>VM Stack</code></h2><ul>
<li>存放8种基本类型的数据和对象引用（不是对象）。</li>
<li>每个线程有自己的单独的栈。</li>
<li>先进后出，后进先出。</li>
<li>因为主要存放基本类型数据变量，所以分配空间比堆快。当超出变量的作用域，将由编译器立即释放空间。</li>
</ul>
<h2 id="本地方法栈Native-Method-Stack"><a href="#本地方法栈Native-Method-Stack" class="headerlink" title="本地方法栈Native Method Stack"></a>本地方法栈<code>Native Method Stack</code></h2><ul>
<li>与<code>虚拟机栈</code>类似，<code>虚拟机栈</code>为虚拟机调用Java方法服务，<code>本地方法栈</code>为虚拟机调用Native方法服务。</li>
<li>在HotSpot虚拟机实现中，<code>虚拟机栈</code>和<code>本地方法栈</code>被合并为一个区域。</li>
</ul>
<h2 id="程序计数器-Program-Counter-Register"><a href="#程序计数器-Program-Counter-Register" class="headerlink" title="程序计数器 Program Counter Register"></a>程序计数器 <code>Program Counter Register</code></h2><p>一块较小的内存空间，可看作是当前线程所执行的字节码的 行号指示器。</p>
<p>通过改变计数器的值来选取下一条需要执行的字节码指令。（分支、循环、跳转、异常处理、线程恢复等）基础功能都依赖与其完成。</p>
<p>特点：</p>
<ul>
<li>线程私有：因为 Java 虚拟机的多线程是通过 线程轮流切换 并 分配处理器执行时间 来实现的，在某一时刻，只会执行一条线程。因此，为了线程切换后能恢复到正确的执行位置，每条线程都需要有一个独立的程序计数器。</li>
<li>无内存溢出：如果线程正在执行的是一个 Java 方法，这个计数器记录的是正在 执行的虚拟机字节码指令的地址；如果正在执行的是 Native 方法，这个计数器值则为空（Undefined）。此内存区域是唯一一个在 Java 虚拟机程序规范中没有规定任何 OutOfMemoryError 情况的区域。</li>
</ul>
<h2 id="方法区Method-Area"><a href="#方法区Method-Area" class="headerlink" title="方法区Method Area"></a>方法区<code>Method Area</code></h2><ul>
<li>跟堆一样，被所有的线程共享。</li>
<li>是一个内存逻辑区域，用于存储已被虚<br>拟机加载的类信息、常量、静态变量、即时编译器编译后的代码等数据。</li>
</ul>
<h4 id="运行时常量池Runtime-Constant-Pool"><a href="#运行时常量池Runtime-Constant-Pool" class="headerlink" title="运行时常量池Runtime Constant Pool"></a>运行时常量池<code>Runtime Constant Pool</code></h4><ul>
<li>方法区的一部分。</li>
<li>Class文件中除了有类的版本、字段、方法、接口等描述信息外，还有一项信息是常量池，用于存放编译期生成的各种字面量和符号引用，这部分内容将在类加载后进入方法区的运行时常量池中存放。</li>
</ul>
<h1 id="直接内存Direct-Memory"><a href="#直接内存Direct-Memory" class="headerlink" title="直接内存Direct Memory"></a>直接内存<code>Direct Memory</code></h1><p>直接内存不是虚拟机运行时数据区的一部分，也不是Java虚拟机规范中定义的内存区域。<br>但是这部分内存也被频繁地使用，而且也可能导致OutOfMemoryError异常出现。</p>
<p>在JDK 1.4中新加入了NIO（New Input/Output）类，引入了一种基于通道（Channel）与缓<br>冲区（Buffer）的I/O方式，它可以使用Native函数库直接分配堆外内存，然后通过一个存储<br>在Java堆中的DirectByteBuffer对象作为这块内存的引用进行操作。这样能在一些场景中显著<br>提高性能，因为避免了在Java堆和Native堆中来回复制数据。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringMVC统一异常处理总结</title>
    <url>/2017/02/15/SpringMVC%E7%BB%9F%E4%B8%80%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p>在一个Spring MVC项目中，使用统一异常处理，可以使维护代码变得容易。下面总结一下常用的3种方法。<br><img src="http://upload-images.jianshu.io/upload_images/4059338-24a47cdd2121897b.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="try-catch"></p>
<h1 id="实现HandlerExceptionResolver接口"><a href="#实现HandlerExceptionResolver接口" class="headerlink" title="实现HandlerExceptionResolver接口"></a>实现<code>HandlerExceptionResolver</code>接口</h1><p>实现<code>HandlerExceptionResolver</code>接口，实现<code>resolveException()</code>方法，根据传入的异常类型做出处理。</p>
<a id="more"></a>
<h1 id="继承AbstractHandlerExceptionResolver类"><a href="#继承AbstractHandlerExceptionResolver类" class="headerlink" title="继承AbstractHandlerExceptionResolver类"></a>继承<code>AbstractHandlerExceptionResolver</code>类</h1><p>继承<code>AbstractHandlerExceptionResolver</code>类，和第一种方式类似，因为<code>AbstractHandlerExceptionResolver</code>实现了<code>HandlerExceptionResolver</code>接口。<br>所以，我们继承之后也是重写<code>resolveException()</code>方法，再处理各种异常。</p>
<h1 id="使用注解-ControllerAdvice处理"><a href="#使用注解-ControllerAdvice处理" class="headerlink" title="使用注解@ControllerAdvice处理"></a>使用注解<code>@ControllerAdvice</code>处理</h1><p>推荐使用这种方法，比较直观。下面上代码：</p>
<p>首先是自定义异常类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ResourceDoesNotExistException</span> <span class="keyword">extends</span> <span class="title">RuntimeException</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">7833283455112352655L</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ResourceDoesNotExistException</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ResourceDoesNotExistException</span><span class="params">(String message)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(message);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ResourceDoesNotExistException</span><span class="params">(String message, Throwable cause)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(message, cause);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ResourceDoesNotExistException</span><span class="params">(Throwable cause)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(cause);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="title">ResourceDoesNotExistException</span><span class="params">(String message, Throwable cause, <span class="keyword">boolean</span> enableSuppression, <span class="keyword">boolean</span> writableStackTrace)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(message, cause, enableSuppression, writableStackTrace);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后是全局异常统一处理类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GlobalExceptionHandler</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ExceptionHandler</span>(value = OtherException<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">    <span class="title">public</span> <span class="title">ModelAndView</span> <span class="title">defaultErrorHandler</span>(<span class="title">HttpServletRequest</span> <span class="title">req</span>, <span class="title">Exception</span> <span class="title">ex</span>) </span>&#123;</span><br><span class="line">        <span class="comment">// 其他异常处理逻辑...</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ExceptionHandler</span>(value = ResourceDoesNotExistException<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">    <span class="title">public</span> <span class="title">ModelAndView</span> <span class="title">notFoundErrorHandler</span>(<span class="title">HttpServletRequest</span> <span class="title">req</span>, <span class="title">ResourceDoesNotExistException</span> <span class="title">ex</span>) </span>&#123;</span><br><span class="line">        ModelAndView mav = <span class="keyword">new</span> ModelAndView();</span><br><span class="line">        mav.setViewName(<span class="string">"404"</span>);</span><br><span class="line">        <span class="keyword">return</span> mav;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>添加<code>@ControllerAdvice</code>注解的类是集中处理异常的地方，可以同时存在多个这样的类，用来做更细粒度的划分。<br>在这个类中，我们可以对每一种异常编写一种处理逻辑，在方法上使用<code>@ExceptionHandler</code>注解修饰，传入指定的异常类型即可。<br>如果是RESTful风格，不返回视图，也可使用<code>@RestControllerAdvice</code>。</p>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Task定时任务的配置和使用</title>
    <url>/2017/02/14/Spring%20Task%E5%AE%9A%E6%97%B6%E4%BB%BB%E5%8A%A1%E7%9A%84%E9%85%8D%E7%BD%AE%E5%92%8C%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>记录下<code>Spring</code>自带的定时任务用法。</p>
<h1 id="spring中使用定时任务"><a href="#spring中使用定时任务" class="headerlink" title="spring中使用定时任务"></a>spring中使用定时任务</h1><h2 id="基于xml配置文件使用定时任务"><a href="#基于xml配置文件使用定时任务" class="headerlink" title="基于xml配置文件使用定时任务"></a>基于xml配置文件使用定时任务</h2><p>首先配置spring开启定时任务</p>
<a id="more"></a>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span>  </span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span>   </span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:p</span>=<span class="string">"http://www.springframework.org/schema/p"</span>  </span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:task</span>=<span class="string">"http://www.springframework.org/schema/task"</span>  </span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span>  </span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:aop</span>=<span class="string">"http://www.springframework.org/schema/aop"</span>   </span></span><br><span class="line"><span class="tag">    <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans   </span></span></span><br><span class="line"><span class="tag"><span class="string">    http://www.springframework.org/schema/beans/spring-beans-4.0.xsd  </span></span></span><br><span class="line"><span class="tag"><span class="string">    http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.0.xsd    </span></span></span><br><span class="line"><span class="tag"><span class="string">    http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-4.0.xsd    </span></span></span><br><span class="line"><span class="tag"><span class="string">    http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd    </span></span></span><br><span class="line"><span class="tag"><span class="string">    http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.0.xsd    </span></span></span><br><span class="line"><span class="tag"><span class="string">    http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-4.0.xsd"</span>&gt;</span>  </span><br><span class="line">  </span><br><span class="line">    <span class="tag">&lt;<span class="name">task:annotation-driven</span> /&gt;</span> <span class="comment">&lt;!-- 定时器开关--&gt;</span>  </span><br><span class="line">  </span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"myTask"</span> <span class="attr">class</span>=<span class="string">"com.spring.task.MyTask"</span>&gt;</span><span class="tag">&lt;/<span class="name">bean</span>&gt;</span>  </span><br><span class="line">  </span><br><span class="line">    <span class="tag">&lt;<span class="name">task:scheduled-tasks</span>&gt;</span>  </span><br><span class="line">        <span class="comment">&lt;!-- 这里表示的是每隔五秒执行一次 --&gt;</span>  </span><br><span class="line">        <span class="tag">&lt;<span class="name">task:scheduled</span> <span class="attr">ref</span>=<span class="string">"myTask"</span> <span class="attr">method</span>=<span class="string">"show"</span> <span class="attr">cron</span>=<span class="string">"*/5 * * * * ?"</span> /&gt;</span>  </span><br><span class="line">        <span class="tag">&lt;<span class="name">task:scheduled</span> <span class="attr">ref</span>=<span class="string">"myTask"</span> <span class="attr">method</span>=<span class="string">"print"</span> <span class="attr">cron</span>=<span class="string">"*/10 * * * * ?"</span>/&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;/<span class="name">task:scheduled-tasks</span>&gt;</span>  </span><br><span class="line">      </span><br><span class="line">    <span class="comment">&lt;!-- 自动扫描的包名 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"com.spring.task"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>定义自己的任务执行逻辑</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.spring.task;  </span><br><span class="line">  </span><br><span class="line"><span class="comment">/** </span></span><br><span class="line"><span class="comment"> * 定义任务 </span></span><br><span class="line"><span class="comment"> */</span>  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyTask</span> </span>&#123;  </span><br><span class="line">      </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">        System.out.println(<span class="string">"show method 1"</span>);  </span><br><span class="line">    &#125;  </span><br><span class="line">      </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">        System.out.println(<span class="string">"print method 1"</span>);  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="基于注解使用定时任务"><a href="#基于注解使用定时任务" class="headerlink" title="基于注解使用定时任务"></a>基于注解使用定时任务</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.spring.task;  </span><br><span class="line"><span class="keyword">import</span> org.springframework.scheduling.annotation.Scheduled;  </span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Component;  </span><br><span class="line">  </span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 基于注解的定时器  </span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyTask2</span> </span>&#123;  </span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 定时计算。每天凌晨 01:00 执行一次</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Scheduled</span>(cron = <span class="string">"0 0 1 * * *"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"show method 2"</span>);  </span><br><span class="line">    &#125;  </span><br><span class="line">      </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 启动时执行一次，之后每隔2秒执行一次  </span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Scheduled</span>(fixedRate = <span class="number">1000</span>*<span class="number">2</span>)   </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">        System.out.println(<span class="string">"print method 2"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这样，当项目启动，定时任务就会按照规则按时执行了。</p>
<h1 id="Spring-Boot中使用定时任务"><a href="#Spring-Boot中使用定时任务" class="headerlink" title="Spring Boot中使用定时任务"></a>Spring Boot中使用定时任务</h1><p>Spring Boot中使用更加方便。</p>
<h2 id="引入springboot-starter包"><a href="#引入springboot-starter包" class="headerlink" title="引入springboot starter包"></a>引入<code>springboot starter</code>包</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="在程序入口启动类添加-EnableScheduling，开启定时任务功能"><a href="#在程序入口启动类添加-EnableScheduling，开启定时任务功能" class="headerlink" title="在程序入口启动类添加@EnableScheduling，开启定时任务功能"></a>在程序入口启动类添加<code>@EnableScheduling</code>，开启定时任务功能</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@EnableScheduling</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Application</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(Application<span class="class">.<span class="keyword">class</span>, <span class="title">args</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="定义定时任务逻辑"><a href="#定义定时任务逻辑" class="headerlink" title="定义定时任务逻辑"></a>定义定时任务逻辑</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyTask3</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> count=<span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Scheduled</span>(cron=<span class="string">"*/6 * * * * ?"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">process</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"this is scheduler task runing  "</span>+(count++));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="任务执行规则说明"><a href="#任务执行规则说明" class="headerlink" title="任务执行规则说明"></a>任务执行规则说明</h1><h2 id="注解参数说明"><a href="#注解参数说明" class="headerlink" title="注解参数说明"></a>注解参数说明</h2><p>先来看看<code>@Scheduled</code>注解的源码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target</span>(&#123;ElementType.METHOD, ElementType.ANNOTATION_TYPE&#125;)</span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Repeatable</span>(Schedules<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> @<span class="title">interface</span> <span class="title">Scheduled</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="function">String <span class="title">cron</span><span class="params">()</span> <span class="keyword">default</span> ""</span>;</span><br><span class="line"></span><br><span class="line">	<span class="function">String <span class="title">zone</span><span class="params">()</span> <span class="keyword">default</span> ""</span>;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">long</span> <span class="title">fixedDelay</span><span class="params">()</span> <span class="keyword">default</span> -1</span>;</span><br><span class="line"></span><br><span class="line">	<span class="function">String <span class="title">fixedDelayString</span><span class="params">()</span> <span class="keyword">default</span> ""</span>;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">long</span> <span class="title">fixedRate</span><span class="params">()</span> <span class="keyword">default</span> -1</span>;</span><br><span class="line"></span><br><span class="line">	<span class="function">String <span class="title">fixedRateString</span><span class="params">()</span> <span class="keyword">default</span> ""</span>;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">long</span> <span class="title">initialDelay</span><span class="params">()</span> <span class="keyword">default</span> -1</span>;</span><br><span class="line"></span><br><span class="line">	<span class="function">String <span class="title">initialDelayString</span><span class="params">()</span> <span class="keyword">default</span> ""</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看出，注解中可以传8种参数：</p>
<ul>
<li>cron：指定cron表达式</li>
<li>zone：默认使用服务器默认时区。可以设置为<code>java.util.TimeZone</code>中的zoneId</li>
<li>fixedDelay：从上一个任务完成开始到下一个任务开始的间隔，单位毫秒</li>
<li>fixedDelayString：同上，时间值是<code>String</code>类型</li>
<li>fixedRate：从上一个任务开始到下一个任务开始的间隔，单位毫秒</li>
<li>fixedRateString：同上，时间值是<code>String</code>类型</li>
<li>initialDelay：任务首次执行延迟的时间，单位毫秒</li>
<li>initialDelayString：同上，时间值是<code>String</code>类型</li>
</ul>
<h2 id="cron表达式的用法"><a href="#cron表达式的用法" class="headerlink" title="cron表达式的用法"></a>cron表达式的用法</h2><p>Cron表达式是一个字符串，字符串以5或6个空格隔开，分为6或7个域，每一个域代表一个含义，Cron有如下两种语法格式： </p>
<ul>
<li>Seconds Minutes Hours DayofMonth Month DayofWeek Year </li>
<li>Seconds Minutes Hours DayofMonth Month DayofWeek</li>
</ul>
<p>每一个域可出现的字符如下： </p>
<ul>
<li>Seconds: 可出现”, - * /“四个字符，有效范围为0-59的整数 </li>
<li>Minutes: 可出现”, - * /“四个字符，有效范围为0-59的整数 </li>
<li>Hours: 可出现”, - * /“四个字符，有效范围为0-23的整数 </li>
<li>DayofMonth: 可出现”, - * / ? L W C”八个字符，有效范围为0-31的整数 </li>
<li>Month: 可出现”, - * /“四个字符，有效范围为1-12的整数或JAN-DEC </li>
<li>DayofWeek: 可出现”, - * / ? L C #”四个字符，有效范围为1-7的整数或SUN-SAT两个范围。1表示星期天，2表示星期一， 依次类推 </li>
<li>Year: 可出现”, - * /“四个字符，有效范围为1970-2099年</li>
</ul>
<p>每一个域都使用数字，但还可以出现如下特殊字符，它们的含义是： </p>
<ol>
<li><code>*</code>：表示匹配该域的任意值，假如在Minutes域使用*, 即表示每分钟都会触发事件。</li>
<li><code>?</code>：只能用在DayofMonth和DayofWeek两个域。它也匹配域的任意值，但实际不会。因为DayofMonth和 DayofWeek会相互影响。例如想在每月的20日触发调度，不管20日到底是星期几，则只能使用如下写法： 13 13 15 20 * ?, 其中最后一位只能用？，而不能使用<em>，如果使用</em>表示不管星期几都会触发，实际上并不是这样。 </li>
<li><code>-</code>：表示范围，例如在Minutes域使用5-20，表示从5分到20分钟每分钟触发一次。</li>
<li><code>/</code>：表示起始时间开始触发，然后每隔固定时间触发一次，例如在Minutes域使用5/20,则意味着5分钟触发一次，而25，45等分别触发一次。</li>
<li><code>,</code>：表示列出枚举值值。例如：在Minutes域使用5,20，则意味着在5和20分每分钟触发一次。 </li>
<li><code>L</code>：表示最后，只能出现在DayofWeek和DayofMonth域，如果在DayofWeek域使用5L,意味着在最后的一个星期四触发。</li>
<li><code>W</code>：表示有效工作日(周一到周五),只能出现在DayofMonth域，系统将在离指定日期的最近的有效工作日触发事件。例如：在 DayofMonth使用5W，如果5日是星期六，则将在最近的工作日：星期五，即4日触发。如果5日是星期天，则在6日(周一)触发；如果5日在星期一 到星期五中的一天，则就在5日触发。另外一点，W的最近寻找不会跨过月份。</li>
<li><code>LW</code>：这两个字符可以连用，表示在某个月最后一个工作日，即最后一个星期五。</li>
<li><code>#</code>：用于确定每个月第几个星期几，只能出现在DayofMonth域。例如在4#2，表示某月的第二个星期三。</li>
</ol>
<hr>
<p>参考 <a href="http://www.cnblogs.com/liuyitian/p/4108391.html" target="_blank" rel="noopener">cron表达式例子</a></p>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>使用SpringBoot Actuator监控应用</title>
    <url>/2017/02/13/%E4%BD%BF%E7%94%A8SpringBoot%20Actuator%E7%9B%91%E6%8E%A7%E5%BA%94%E7%94%A8/</url>
    <content><![CDATA[<p>Actuator是Spring Boot提供的对应用系统的自省和监控的集成功能，可以对应用系统进行配置查看、相关功能统计等。</p>
<h1 id="使用Actuator"><a href="#使用Actuator" class="headerlink" title="使用Actuator"></a>使用Actuator</h1><p>引入依赖即可</p>
<ul>
<li><p><code>Maven</code>：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">     <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">     <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-actuator<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>Gradle</code>：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">compile(&#39;org.springframework.boot:spring-boot-starter-actuator&#39;)</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h1 id="Endpoints"><a href="#Endpoints" class="headerlink" title="Endpoints"></a>Endpoints</h1><p>列举一些主要的endpoints</p>
<a id="more"></a>
<p><img src="http://img.blog.csdn.net/20170213112756666" alt="endpoints"></p>
<h1 id="配置文件属性介绍"><a href="#配置文件属性介绍" class="headerlink" title="配置文件属性介绍"></a>配置文件属性介绍</h1><h3 id="地址和端口的配置"><a href="#地址和端口的配置" class="headerlink" title="地址和端口的配置"></a>地址和端口的配置</h3><ul>
<li><code>management.port</code>：指定访问这些监控方法的端口，与逻辑接口端口分离。如果不想将这些暴露在http中，可以设置 management.port = -1</li>
<li><code>management.address</code>：指定地址，比如只能通过本机监控，可以设置 management.address = 127.0.0.1</li>
</ul>
<h3 id="敏感信息访问限制"><a href="#敏感信息访问限制" class="headerlink" title="敏感信息访问限制"></a>敏感信息访问限制</h3><p>根据上面表格，鉴权为<code>false</code>的，表示不敏感，可以随意访问，否则就是做了一些保护，不能随意访问。</p>
<blockquote>
<p>endpoints.mappings.sensitive=false</p>
</blockquote>
<p>这样需要对每一个都设置，比较麻烦。敏感方法默认是需要用户拥有<code>ACTUATOR</code>角色，因此，也可以设置关闭安全限制：</p>
<blockquote>
<p>management.security.enabled=false</p>
</blockquote>
<p>或者配合<code>Spring Security</code>做细粒度控制。</p>
<h3 id="自定义系统信息"><a href="#自定义系统信息" class="headerlink" title="自定义系统信息"></a>自定义系统信息</h3><p>可以通过访问<code>/info</code>获取信息，需要在配置文件设置</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">info:</span></span><br><span class="line">  <span class="attr">aaa:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">xxx</span></span><br><span class="line">    <span class="attr">email:</span> <span class="string">xxx@qq.com</span></span><br><span class="line">  <span class="attr">bbb:</span></span><br><span class="line">    <span class="attr">age:</span> <span class="number">25</span></span><br><span class="line">    <span class="attr">hobbies:</span> <span class="string">running</span></span><br><span class="line">  <span class="attr">build:</span></span><br><span class="line">    <span class="attr">artifact:</span> <span class="string">"@project.artifactId@"</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">"@project.name@"</span></span><br><span class="line">    <span class="attr">version:</span> <span class="string">"@project.version@"</span></span><br></pre></td></tr></table></figure>
<p>此时访问<a href="localhost:8080/info">localhost:8080/info</a>返回一下信息<br><img src="http://img.blog.csdn.net/20170213115530800?watermark/2/text/aHR0cDovL2Jsb2cuY3Nkbi5uZXQvdTAxMTg1MTQ3OA==/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70/gravity/SouthEast" alt="这里写图片描述"></p>
<p>如果使用<code>maven</code>，可以访问pom.xml文件的信息，用法如下:</p>
<blockquote>
<p>// 获取pom.xml中project节点下artifactId属性<br>artifact: “@project.artifactId@”</p>
</blockquote>
<h3 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h3><p><code>/shutdown</code>这个需要post方式，通过请求来关闭应用。<br>这个操作比较敏感，要想真正生效，需要以下配置:</p>
<blockquote>
<p>endpoints.shutdown.enabled: true</p>
</blockquote>
<ul>
<li>我们可以通过实现<code>HealthIndicator</code>接口，编写自己的<code>/health</code>方法逻辑。也可以增加自定义监控方法。</li>
<li>查看详细介绍，请移步 <a href="http://docs.spring.io/spring-boot/docs/current/reference/htmlsingle/#production-ready" target="_blank" rel="noopener">官方文档</a></li>
</ul>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>在SpringBoot中使用Logback管理日志</title>
    <url>/2017/02/11/%E5%9C%A8SpringBoot%E4%B8%AD%E4%BD%BF%E7%94%A8Logback%E7%AE%A1%E7%90%86%E6%97%A5%E5%BF%97/</url>
    <content><![CDATA[<h1 id="SpringBoot的默认日志配置"><a href="#SpringBoot的默认日志配置" class="headerlink" title="SpringBoot的默认日志配置"></a>SpringBoot的默认日志配置</h1><p><code>SpringBoot</code>开箱即用，默认帮你配置了日志框架，使用 <code>Commons Logging</code> ，但是默认配置也提供了对常用日志的支持，如： Java Util Logging ， Log4J , Log4J2 和 Logback。每种Logger都可以通过配置使用控制台或者文件输出日志内容。</p>
<h2 id="控制台输出"><a href="#控制台输出" class="headerlink" title="控制台输出"></a>控制台输出</h2><p>关于log日志，首先说说的5个日志级别 LEVEL：<br>从高到低分别是<code>ERROR</code>、<code>WARN</code>、<code>INFO</code>、<code>DEBUG</code>、<code>TRACE</code><br>低级别的会输出高级别信息，高级别不会输出低级别信息。例如：等级设为<code>ERROR</code>的话，<code>WARN</code>、<code>INFO</code>、<code>DEBUG</code>的信息是不会输出的。<br>在<code>SpringBoot</code>中默认配置了<code>ERROR</code> 、<code>WARN</code>和<code>INFO</code>级别的日志输出到控制台。<br>Logback中没有<code>FATAL</code>级别，它会被当作<code>ERROR</code>级别来处理。</p>
<a id="more"></a>
<h2 id="我们可以通过两种方式切换至-DEBUG-级别："><a href="#我们可以通过两种方式切换至-DEBUG-级别：" class="headerlink" title="我们可以通过两种方式切换至 DEBUG 级别："></a>我们可以通过两种方式切换至 DEBUG 级别：</h2><ul>
<li>在运行命令后加入 –debug 标志，如： $ java -jar myapp.jar –debug</li>
<li>在 <code>application.properties</code> 中配置 debug=true ，该属性置为true的时候，核心Logger（包含嵌入式容器、hibernate、spring）会输出更多内容，但是你自己应用的日志并不会输出为DEBUG级别。</li>
</ul>
<h2 id="多彩输出"><a href="#多彩输出" class="headerlink" title="多彩输出"></a>多彩输出</h2><p><code>SpringBoot</code>从1.4.0版本开始支持彩色日志输出了。<br>如果你的终端支持ANSI，设置彩色输出会让日志更具可读性。通过在 <code>application.properties</code> 中设置 <code>spring.output.ansi.enabled</code> 参数来支持。</p>
<ul>
<li><code>NEVER</code>：禁用ANSI-colored输出（默认项）</li>
<li><code>DETECT</code>：会检查终端是否支持ANSI，是的话就采用彩色输出（推荐项）</li>
<li><code>ALWAYS</code>：总是使用ANSI-colored格式输出，若终端不支持的时候，会有很多干扰信息，不推荐使用</li>
</ul>
<h2 id="文件输出"><a href="#文件输出" class="headerlink" title="文件输出"></a>文件输出</h2><p><code>SpringBoot</code>默认配置只会输出到控制台，并不会记录到文件中，但是我们通常生产环境使用时都需要以文件方式记录。</p>
<p>若要增加文件输出，需要在 <code>application.properties</code> 中配置 <code>logging.file</code> 或 <code>logging.path</code> 属性。</p>
<ul>
<li><code>logging.file</code>:设置文件，可以是绝对路径，也可以是相对路径。如： logging.file=my.log</li>
<li><code>logging.path</code>:设置目录，会在该目录下创建spring.log文件，并写入日志内容，如： logging.path=/var/log</li>
</ul>
<p><strong>日志文件会在10Mb大小的时候被截断，产生新的日志文件，默认级别为：ERROR、WARN、INFO</strong></p>
<h2 id="级别控制"><a href="#级别控制" class="headerlink" title="级别控制"></a>级别控制</h2><p>在<code>SpringBoot</code>中只需要在 <code>application.properties</code> 中进行配置完成日志记录的级别控制。</p>
<p>配置格式： logging.level.*=LEVEL</p>
<ul>
<li><code>logging.level</code> ：日志级别控制前缀， * 为包名或Logger名</li>
<li><code>LEVEL</code> ：选项TRACE, DEBUG, INFO, WARN, ERROR, FATAL, OFF</li>
</ul>
<p>举例：</p>
<ul>
<li>logging.level.com.controller=DEBUG ： com.controller包下所有class以<code>DEBUG</code>级别输出</li>
<li>logging.level.root=WARN ：root日志以<code>WARN</code>级别输出</li>
</ul>
<h1 id="自定义日志配置"><a href="#自定义日志配置" class="headerlink" title="自定义日志配置"></a>自定义日志配置</h1><p>如果不想使用默认配置，只要加入自己的日志配置文件就行了。<br>由于日志服务一般都在<code>ApplicationContext</code>创建前就初始化了，它并不是必须通过<code>Spring</code>的配置文件控制。因此通过系统属性和传统的<code>Spring Boot</code>外部配置文件依然可以很好的支持日志控制和管理。</p>
<p>根据不同的日志系统，你可以按如下规则组织配置文件名，就能被正确加载：</p>
<ul>
<li>Logback： logback-spring.xml , logback-spring.groovy , logback.xml , logback.groovy</li>
<li>Log4j： log4j-spring.properties , log4j-spring.xml , log4j.properties , log4j.xml</li>
<li>Log4j2： log4j2-spring.xml , log4j2.xml</li>
<li>JDK (Java Util Logging)： logging.properties</li>
</ul>
<p><code>SpringBoot</code>官方推荐优先使用带有 <code>-spring</code> 的文件名作为你的日志配置（如使用 logback-spring.xml ，而不是 logback.xml ）</p>
<h2 id="自定义输出格式"><a href="#自定义输出格式" class="headerlink" title="自定义输出格式"></a>自定义输出格式</h2><p>在<code>SpringBoot</code>中可以通过在 <code>application.properties</code> 配置如下参数控制输出格式：</p>
<ul>
<li><code>logging.pattern.console</code>：定义输出到控制台的样式（不支持JDK Logger）</li>
<li><code>logging.pattern.file</code>：定义输出到文件的样式（不支持JDK Logger）</li>
</ul>
<p>也可以直接在日志配置文件中定义这些格式，而不是在应用配置文件中。</p>
<h2 id="常用的Logback配置文件模板"><a href="#常用的Logback配置文件模板" class="headerlink" title="常用的Logback配置文件模板"></a>常用的Logback配置文件模板</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">	<span class="comment">&lt;!--定义日志文件的存储地址 勿在 LogBack 的配置中使用相对路径--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"LOG_HOME"</span> <span class="attr">value</span>=<span class="string">"d:/logs"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- 彩色日志依赖的渲染类 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">conversionRule</span> <span class="attr">conversionWord</span>=<span class="string">"clr"</span> <span class="attr">converterClass</span>=<span class="string">"org.springframework.boot.logging.logback.ColorConverter"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">conversionRule</span> <span class="attr">conversionWord</span>=<span class="string">"wex"</span></span></span><br><span class="line"><span class="tag">                    <span class="attr">converterClass</span>=<span class="string">"org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">conversionRule</span> <span class="attr">conversionWord</span>=<span class="string">"wEx"</span></span></span><br><span class="line"><span class="tag">                    <span class="attr">converterClass</span>=<span class="string">"org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 彩色日志格式 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"CONSOLE_LOG_PATTERN"</span></span></span><br><span class="line"><span class="tag">              <span class="attr">value</span>=<span class="string">"$&#123;CONSOLE_LOG_PATTERN:-%clr(%d&#123;yyyy-MM-dd HH:mm:ss.SSS&#125;)&#123;faint&#125; %clr($&#123;LOG_LEVEL_PATTERN:-%5p&#125;) %clr($&#123;PID:- &#125;)&#123;magenta&#125; %clr(--)&#123;faint&#125; %clr([%15.15t])&#123;faint&#125; %clr(%-40.40logger&#123;39&#125;)&#123;cyan&#125; %clr(:)&#123;faint&#125; %m%n$&#123;LOG_EXCEPTION_CONVERSION_WORD:-%wEx&#125;&#125;"</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- Console 输出设置 --&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">appender</span> <span class="attr">name</span>=<span class="string">"console"</span> <span class="attr">class</span>=<span class="string">"ch.qos.logback.core.ConsoleAppender"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">encoder</span>&gt;</span></span><br><span class="line">			<span class="comment">&lt;!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符--&gt;</span></span><br><span class="line">			<span class="comment">&lt;!--&lt;pattern&gt;%d&#123;yyyy-MM-dd HH:mm:ss.SSS&#125; [%thread] %-5level %logger&#123;36&#125; - %msg%n&lt;/pattern&gt;--&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">pattern</span>&gt;</span>$&#123;CONSOLE_LOG_PATTERN&#125;<span class="tag">&lt;/<span class="name">pattern</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">charset</span>&gt;</span>utf8<span class="tag">&lt;/<span class="name">charset</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">encoder</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">appender</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- 按照每天生成日志文件 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">appender</span> <span class="attr">name</span>=<span class="string">"file"</span> <span class="attr">class</span>=<span class="string">"ch.qos.logback.core.rolling.RollingFileAppender"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">rollingPolicy</span> <span class="attr">class</span>=<span class="string">"ch.qos.logback.core.rolling.TimeBasedRollingPolicy"</span>&gt;</span></span><br><span class="line">            <span class="comment">&lt;!--日志文件输出的文件名--&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">fileNamePattern</span>&gt;</span>$&#123;LOG_HOME&#125;/mixedSys.%d&#123;yyyy-MM-dd&#125;.log<span class="tag">&lt;/<span class="name">fileNamePattern</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">rollingPolicy</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">encoder</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">pattern</span>&gt;</span>%d&#123;yyyy-MM-dd HH:mm:ss.SSS&#125; [%thread] %-5level %logger&#123;36&#125; - %msg%n<span class="tag">&lt;/<span class="name">pattern</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">encoder</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">appender</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- 日志logger(包.类)的输出级别 --&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">logger</span> <span class="attr">name</span>=<span class="string">"org.springframework.web.servlet.mvc.method.annotation.ExceptionHandlerExceptionResolver"</span> <span class="attr">level</span>=<span class="string">"DEBUG"</span> &gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">"console"</span> /&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">"file"</span> /&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">logger</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">logger</span> <span class="attr">name</span>=<span class="string">"org.springframework.boot"</span> <span class="attr">level</span>=<span class="string">"DEBUG"</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 为 Hibernate sql 定制 --&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- &lt;logger name="org.hibernate.type.descriptor.sql.BasicBinder"  level="TRACE" /&gt;</span></span><br><span class="line"><span class="comment">    &lt;logger name="org.hibernate.type.descriptor.sql.BasicExtractor"  level="DEBUG" /&gt;</span></span><br><span class="line"><span class="comment">    &lt;logger name="org.hibernate.SQL" level="DEBUG" /&gt;</span></span><br><span class="line"><span class="comment">    &lt;logger name="org.hibernate.engine.QueryParameters" level="DEBUG" /&gt;</span></span><br><span class="line"><span class="comment">    &lt;logger name="org.hibernate.engine.query.HQLQueryPlan" level="DEBUG" /&gt;  --&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- 日志输出级别 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">root</span> <span class="attr">level</span>=<span class="string">"INFO"</span>&gt;</span> <span class="comment">&lt;!-- 此时debug级别的信息会被过滤 --&gt;</span>  </span><br><span class="line">        <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">"console"</span> /&gt;</span>   </span><br><span class="line">        <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">"file"</span> /&gt;</span>   </span><br><span class="line">    <span class="tag">&lt;/<span class="name">root</span>&gt;</span> </span><br><span class="line">    </span><br><span class="line">	<span class="comment">&lt;!--日志异步到数据库 --&gt;</span>    </span><br><span class="line">    <span class="comment">&lt;!-- &lt;appender name="DB" class="ch.qos.logback.classic.db.DBAppender"&gt;  </span></span><br><span class="line"><span class="comment">        日志异步到数据库   </span></span><br><span class="line"><span class="comment">        &lt;connectionSource class="ch.qos.logback.core.db.DriverManagerConnectionSource"&gt;  </span></span><br><span class="line"><span class="comment">            连接池   </span></span><br><span class="line"><span class="comment">            &lt;dataSource class="com.mchange.v2.c3p0.ComboPooledDataSource"&gt;  </span></span><br><span class="line"><span class="comment">              &lt;driverClass&gt;com.mysql.jdbc.Driver&lt;/driverClass&gt;  </span></span><br><span class="line"><span class="comment">              &lt;url&gt;jdbc:mysql://127.0.0.1:3306/databaseName&lt;/url&gt;  </span></span><br><span class="line"><span class="comment">              &lt;user&gt;root&lt;/user&gt;  </span></span><br><span class="line"><span class="comment">              &lt;password&gt;root&lt;/password&gt;  </span></span><br><span class="line"><span class="comment">            &lt;/dataSource&gt;  </span></span><br><span class="line"><span class="comment">        &lt;/connectionSource&gt;  </span></span><br><span class="line"><span class="comment">    &lt;/appender&gt; --&gt;</span></span><br><span class="line">	</span><br><span class="line"><span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
        <tag>Logback</tag>
      </tags>
  </entry>
  <entry>
    <title>用SpringMVC搭建web应用</title>
    <url>/2016/12/13/%E7%94%A8SpringMVC%E6%90%AD%E5%BB%BAweb%E5%BA%94%E7%94%A8/</url>
    <content><![CDATA[<p><img src="http://upload-images.jianshu.io/upload_images/4059338-fe41c5b216f5ff66.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="SpringMVC"></p>
<p>在使用<code>SpringMVC</code>时，最重要的2个类就是<code>DispatcherServlet</code>和<code>ContextLoaderListener</code>。<code>DispatcherServlet</code>加载包含Web组件的bean，如控制器、视图解析器以及处理器映射，<code>ContextLoaderListener</code>加载应用中的其他bean(通常是驱动应用后端的中间层和数据层组件)。</p>
<a id="more"></a>
<h1 id="Servlet-3-0之后"><a href="#Servlet-3-0之后" class="headerlink" title="Servlet 3.0之后"></a>Servlet 3.0之后</h1><p>servlet3.0规范出来后，spring3.2有了一种简便的搭建方式。<br>直接继承<code>AbstractAnnotationConfigDispatcherServletInitializer</code>即可。<br>这个类会自动创建DispatcherServlet和ContextLoaderListener。这种方式不再需要web.xml，非常方便。</p>
<h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>在Servlet 3.0环境中，容器会在类路径中查找实现javax.servlet.ServletContainerInitializer接口的类，如果能发现的话，就会用它来配置Servlet容器。<br>Spring提供了这个接口的实现，名为SpringServletContainerInitializer，这个类反过来又会查找实现WebApplicationInitializer的类并将配置的任务交给它们来完成。<br>Spring 3.2引入了一个便利的WebApplicationInitializer基础实现，也就是AbstractAnnotationConfigDispatcherServletInitializer。<br>所以我们只要继承AbstractAnnotationConfigDispatcherServletInitializer（同时也就实现了WebApplicationInitializer），在部署到Servlet 3.0容器中的时候，容器会自动发现它，并用它来配置Servlet上下文。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyWebInitializer</span> <span class="keyword">extends</span> <span class="title">AbstractAnnotationConfigDispatcherServletInitializer</span> </span>&#123;</span><br><span class="line">    <span class="keyword">protected</span> Class&lt;?&gt;[] getRootConfigClasses() &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Class&lt;?&gt;[]&#123;RootConfig<span class="class">.<span class="keyword">class</span>&#125;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 指定配置类</span></span><br><span class="line">    <span class="keyword">protected</span> Class&lt;?&gt;[] getServletConfigClasses() &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Class&lt;?&gt;[]&#123;WebConfig<span class="class">.<span class="keyword">class</span>&#125;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将DispatcherServlet映射到"/"</span></span><br><span class="line">    <span class="keyword">protected</span> String[] getServletMappings() &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> String[]&#123;<span class="string">"/"</span>&#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="Servlet-3-0之前"><a href="#Servlet-3-0之前" class="headerlink" title="Servlet 3.0之前"></a>Servlet 3.0之前</h1><p>如果你要部署在不支持servlet3.0的容器，比如tomcat6和以下版本，那就只能通过web.xml来配置了。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">web-app</span> <span class="attr">version</span>=<span class="string">"2.5"</span> </span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns</span>=<span class="string">"http://java.sun.com/xml/ns/javaee"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://java.sun.com/xml/ns/javaee</span></span></span><br><span class="line"><span class="tag"><span class="string">      http://java.sun.com/xml/ns/javaee/web-app_2_5.xsd"</span>&gt;</span></span><br><span class="line">  </span><br><span class="line">	<span class="comment">&lt;!-- 配置根上下文 --&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">context-param</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">param-name</span>&gt;</span>contextConfigLocation<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">param-value</span>&gt;</span>classpath:applicationContext.xml<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">context-param</span>&gt;</span></span><br><span class="line">	</span><br><span class="line">	<span class="comment">&lt;!-- 注册Spring监听器 --&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">listener</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">listener-class</span>&gt;</span>org.springframework.web.context.ContextLoaderListener<span class="tag">&lt;/<span class="name">listener-class</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">listener</span>&gt;</span> </span><br><span class="line">    </span><br><span class="line">	<span class="comment">&lt;!-- SpringMVC前端控制器 --&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">servlet</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>springMVC<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">servlet-class</span>&gt;</span>org.springframework.web.servlet.DispatcherServlet<span class="tag">&lt;/<span class="name">servlet-class</span>&gt;</span></span><br><span class="line">		<span class="comment">&lt;!-- contextConfigLocation配置springmvc加载的配置文件（配置处理器、映射器、适配器等等） </span></span><br><span class="line"><span class="comment">		如果不配置contextConfigLocation，默认加载的是/WEB-INF/servlet名称- serlvet.xml（springmvc-servlet.xml） --&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">init-param</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">param-name</span>&gt;</span>contextConfigLocation<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">param-value</span>&gt;</span>classpath:spring-mvc.xml<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">init-param</span>&gt;</span></span><br><span class="line">		<span class="comment">&lt;!-- 指定servlet加载顺序，整数越小优先越高 --&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">load-on-startup</span>&gt;</span>1<span class="tag">&lt;/<span class="name">load-on-startup</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">servlet</span>&gt;</span></span><br><span class="line"></span><br><span class="line">	<span class="tag">&lt;<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>springMVC<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">	</span><br><span class="line"><span class="tag">&lt;/<span class="name">web-app</span>&gt;</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>解决Linux下DNS配置重启失效问题</title>
    <url>/2016/12/02/%E8%A7%A3%E5%86%B3Linux%E4%B8%8BDNS%E9%85%8D%E7%BD%AE%E9%87%8D%E5%90%AF%E5%A4%B1%E6%95%88%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<p><img src="http://upload-images.jianshu.io/upload_images/4059338-31dffaf4f06dcb24.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="DNS"></p>
<p>有时候能<code>ping</code>通ip地址，却<code>ping</code>不通域名，这就是<code>dns</code>没有配置的缘故。<br>但是DNS配置文件 <code>/etc/resolv.conf</code> 每次重启就会失效。</p>
<h1 id="那么问题来了，怎么解决每次都要配置的问题呢？"><a href="#那么问题来了，怎么解决每次都要配置的问题呢？" class="headerlink" title="那么问题来了，怎么解决每次都要配置的问题呢？"></a>那么问题来了，怎么解决每次都要配置的问题呢？</h1><h1 id="Ubuntu系统下"><a href="#Ubuntu系统下" class="headerlink" title="Ubuntu系统下"></a>Ubuntu系统下</h1><p>打开这个配置文件，发现有注释提示：</p>
<blockquote>
<p># Dynamic resolv.conf(5) file for glibc resolver(3) generated by resolvconf(8)# DO NOT EDIT THIS FILE BY HAND — YOUR CHANGES WILL BE OVERWRITTEN</p>
</blockquote>
<p>原来系统已经提示我们不要手动改此文件，因为这里每次重启会被覆盖。</p>
<h2 id="方法1"><a href="#方法1" class="headerlink" title="方法1"></a>方法1</h2><ul>
<li>执行<code>sudo vim /etc/network/interfaces</code></li>
<li>添加一行DNS配置，比如<code>dns-nameservers 8.8.8.8</code></li>
</ul>
<h2 id="方法2"><a href="#方法2" class="headerlink" title="方法2"></a>方法2</h2><ul>
<li>执行<code>sudo vim /etc/resolvconf/resolv.conf.d/base</code></li>
<li>添加DNS配置，比如<code>nameserver 8.8.8.8</code></li>
<li>如果有多个DNS就添加多行，一行一个</li>
<li>保存后执行<code>resolvconf -u</code><br>此时，再打开<code>/etc/resolv.conf</code>会发现刚才添加的DNS配置了。</li>
</ul>
<hr>
<h1 id="CentOS系统下"><a href="#CentOS系统下" class="headerlink" title="CentOS系统下"></a>CentOS系统下</h1><p>直接编辑<code>/etc/resolv.conf</code>，重启之后一样会失效。</p>
<h2 id="方法：直接将DNS配置写入网卡中"><a href="#方法：直接将DNS配置写入网卡中" class="headerlink" title="方法：直接将DNS配置写入网卡中"></a>方法：直接将DNS配置写入网卡中</h2><ul>
<li>执行 <code>cd /etc/sysconfig/network-scripts/</code></li>
<li>编辑网卡配置文件 <code>vi ifcfg-eth0</code>，在后面加入DNS配置<blockquote>
<p>DNS1=223.5.5.5<br>DNS1=223.6.6.6</p>
</blockquote>
</li>
<li>重启网卡<code>service network restart</code>即可<br>此时，再打开/etc/resolv.conf会发现刚才添加的DNS配置了。<br>以后在重启就没问题了 ^_^</li>
</ul>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker安装与基础命令</title>
    <url>/2016/11/22/Docker%E5%AE%89%E8%A3%85%E4%B8%8E%E5%9F%BA%E7%A1%80%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<p><img src="http://upload-images.jianshu.io/upload_images/4059338-0771491bb3c9125a.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="docker"></p>
<h1 id="Docker安装"><a href="#Docker安装" class="headerlink" title="Docker安装"></a>Docker安装</h1><p>我是在Ubuntu 16上安装的docker，linux安装docker只需要一个命令：</p>
<blockquote>
<p>sudo apt-get install docker.io</p>
</blockquote>
<a id="more"></a>
<p>运行完后，可以在终端输入docker看到以下信息证明我们安装成功了<br>注：提示权限问题就添加sudo</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker</span><br><span class="line">Usage: docker [OPTIONS] COMMAND [arg...]</span><br><span class="line">       docker daemon [ --<span class="built_in">help</span> | ... ]</span><br><span class="line">       docker [ --<span class="built_in">help</span> | -v | --version ]</span><br><span class="line"></span><br><span class="line">A self-sufficient runtime <span class="keyword">for</span> containers.</span><br><span class="line"></span><br><span class="line">Options:</span><br><span class="line"></span><br><span class="line">  --config=~/.docker              Location of client config files</span><br><span class="line">  -D, --debug                     Enable debug mode</span><br><span class="line">  -H, --host=[]                   Daemon socket(s) to connect to</span><br><span class="line">  -h, --<span class="built_in">help</span>                      Print usage</span><br><span class="line">  -l, --<span class="built_in">log</span>-level=info            Set the logging level</span><br><span class="line">  --tls                           Use TLS; implied by --tlsverify</span><br><span class="line">  --tlscacert=~/.docker/ca.pem    Trust certs signed only by this CA</span><br><span class="line">  --tlscert=~/.docker/cert.pem    Path to TLS certificate file</span><br><span class="line">  --tlskey=~/.docker/key.pem      Path to TLS key file</span><br><span class="line">  --tlsverify                     Use TLS and verify the remote</span><br><span class="line">  -v, --version                   Print version information and quit</span><br><span class="line"></span><br><span class="line">Commands:</span><br><span class="line">    attach    Attach to a running container</span><br><span class="line">    build     Build an image from a Dockerfile</span><br><span class="line">    commit    Create a new image from a container<span class="string">'s changes</span></span><br><span class="line"><span class="string">    cp        Copy files/folders between a container and the local filesystem</span></span><br><span class="line"><span class="string">    create    Create a new container</span></span><br><span class="line"><span class="string">    diff      Inspect changes on a container'</span>s filesystem</span><br><span class="line">    events    Get real time events from the server</span><br><span class="line">    <span class="built_in">exec</span>      Run a <span class="built_in">command</span> <span class="keyword">in</span> a running container</span><br><span class="line">    <span class="built_in">export</span>    Export a container<span class="string">'s filesystem as a tar archive</span></span><br><span class="line"><span class="string">    history   Show the history of an image</span></span><br><span class="line"><span class="string">    images    List images</span></span><br><span class="line"><span class="string">    import    Import the contents from a tarball to create a filesystem image</span></span><br><span class="line"><span class="string">    info      Display system-wide information</span></span><br><span class="line"><span class="string">    inspect   Return low-level information on a container or image</span></span><br><span class="line"><span class="string">    kill      Kill a running container</span></span><br><span class="line"><span class="string">    load      Load an image from a tar archive or STDIN</span></span><br><span class="line"><span class="string">    login     Register or log in to a Docker registry</span></span><br><span class="line"><span class="string">    logout    Log out from a Docker registry</span></span><br><span class="line"><span class="string">    logs      Fetch the logs of a container</span></span><br><span class="line"><span class="string">    network   Manage Docker networks</span></span><br><span class="line"><span class="string">    pause     Pause all processes within a container</span></span><br><span class="line"><span class="string">    port      List port mappings or a specific mapping for the CONTAINER</span></span><br><span class="line"><span class="string">    ps        List containers</span></span><br><span class="line"><span class="string">    pull      Pull an image or a repository from a registry</span></span><br><span class="line"><span class="string">    push      Push an image or a repository to a registry</span></span><br><span class="line"><span class="string">    rename    Rename a container</span></span><br><span class="line"><span class="string">    restart   Restart a container</span></span><br><span class="line"><span class="string">    rm        Remove one or more containers</span></span><br><span class="line"><span class="string">    rmi       Remove one or more images</span></span><br><span class="line"><span class="string">    run       Run a command in a new container</span></span><br><span class="line"><span class="string">    save      Save an image(s) to a tar archive</span></span><br><span class="line"><span class="string">    search    Search the Docker Hub for images</span></span><br><span class="line"><span class="string">    start     Start one or more stopped containers</span></span><br><span class="line"><span class="string">    stats     Display a live stream of container(s) resource usage statistics</span></span><br><span class="line"><span class="string">    stop      Stop a running container</span></span><br><span class="line"><span class="string">    tag       Tag an image into a repository</span></span><br><span class="line"><span class="string">    top       Display the running processes of a container</span></span><br><span class="line"><span class="string">    unpause   Unpause all processes within a container</span></span><br><span class="line"><span class="string">    update    Update resources of one or more containers</span></span><br><span class="line"><span class="string">    version   Show the Docker version information</span></span><br><span class="line"><span class="string">    volume    Manage Docker volumes</span></span><br><span class="line"><span class="string">    wait      Block until a container stops, then print its exit code</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">Run '</span>docker COMMAND --<span class="built_in">help</span><span class="string">' for more information on a command.</span></span><br></pre></td></tr></table></figure>

<p>安装完以后，也可运行以下命令查看版本信息：</p>
<blockquote>
<p>docker -v<br>docker version<br>docker info</p>
</blockquote>
<p>当利用 docker run 来创建容器时，Docker 在后台运行的标准操作包括：</p>
<ul>
<li>检查本地是否存在指定的镜像，不存在就从公有仓库下载</li>
<li>利用镜像创建并启动一个容器</li>
<li>分配一个文件系统，并在只读的镜像层外面挂载一层可读写层</li>
<li>从宿主主机配置的网桥接口中桥接一个虚拟接口到容器中去</li>
<li>从地址池配置一个 ip 地址给容器</li>
<li>执行用户指定的应用程序</li>
<li>执行完毕后容器被终止</li>
</ul>
]]></content>
      <categories>
        <category>日常记录</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux虚拟机设置静态IP</title>
    <url>/2016/11/15/Linux%E8%99%9A%E6%8B%9F%E6%9C%BA%E8%AE%BE%E7%BD%AE%E9%9D%99%E6%80%81IP/</url>
    <content><![CDATA[<p>照着网上的方法试了，总有几个步骤不对，反复试了几次，终于成功了，做下笔记。</p>
<h1 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h1><p>我是win7系统（这个无所谓），本地VMWare装了Ubuntu 16.04 LTS  64位虚拟机。想在win7用xshell操作本地虚拟机。</p>
<h1 id="设置方法"><a href="#设置方法" class="headerlink" title="设置方法"></a>设置方法</h1><h2 id="配置VMware网络环境"><a href="#配置VMware网络环境" class="headerlink" title="配置VMware网络环境"></a>配置VMware网络环境</h2><p>VMware在默认安装完成之后，会创建三个虚拟的网络环境：</p>
<ul>
<li>VMnet0 ：桥接网络</li>
<li>VMnet1 ：Host-only</li>
<li>VMnet8 ：NAT</li>
</ul>
<p>其中，NAT表示VMWware内安装的Ubuntu将会在一个子网中，VMware通过网络地址转换，通过物理机的IP上网。<br>我们选择NAT方式实现Ubuntu的静态IP地址配置。</p>
<a id="more"></a>
<ol>
<li>打开VMware，在顶部依次选择：编辑 &gt; 虚拟网路编辑器，打开虚拟网路编辑器：<br><img src="http://img.blog.csdn.net/20161115104554327" alt="这里写图片描述"></li>
<li>去掉VMnet0和VMnet1，只保留VMnet8。</li>
<li>然后，去掉如下图中的“使用本地DHCP服务奖IP地址分配给虚拟机”：<br><img src="http://img.blog.csdn.net/20161115104659176" alt="这里写图片描述"></li>
<li>这里的子网IP为：192.168.8.0，子网掩码为：255.255.255.0，因此，在Ubuntu中，设置IP地址的时候，可以设置为192.168.8.x，x可以为1~255。</li>
<li>选择“NAT设置”，打开NAT设置面板：<br><img src="http://img.blog.csdn.net/20161115104851753" alt="这里写图片描述"><br>查看自己的网关地址，例如，此处的网关应该为：192.168.8.2。</li>
<li>最后，在VMWare的虚拟机管理界面，选择Ubuntu的“编辑虚拟机设置”，打开Ubuntu这个虚拟的设置界面。</li>
</ol>
<p>选择网络适配器，然后确定网络连接选中的是“自定义”中的VMnet8(NAT模式)：<br><img src="http://img.blog.csdn.net/20161115104937850" alt="这里写图片描述"></p>
<p>VMWare设置完毕。</p>
<h2 id="Ubuntu网络设置"><a href="#Ubuntu网络设置" class="headerlink" title="Ubuntu网络设置"></a>Ubuntu网络设置</h2><ol>
<li><p>在Ubuntu桌面的右上角，点击网络图标，然后选择“Edit Connections”：<br><img src="http://img.blog.csdn.net/20161115105137304" alt="这里写图片描述"></p>
</li>
<li><p>点击Edit按钮，在IPv4Settings选项卡中，Method选择<code>Manual</code>，点击add，编辑ip地址<br><img src="http://img.blog.csdn.net/20161115105455524" alt="这里写图片描述"><br>设置的IP地址为： IP： 192.168.8.100 子网掩码： 255.255.255.0 网关： 192.168.8.2<br>然后保存。</p>
</li>
<li><p>最后，点击Ubuntu桌面右上角的网络图标，选择<code>Disconnect</code>，断开连接。然后再打开该菜单，选择<code>Connect</code>，即可连接上网。 </p>
</li>
</ol>
<h2 id="终端配置"><a href="#终端配置" class="headerlink" title="终端配置"></a>终端配置</h2><h3 id="打开Terminal，配置静态ip"><a href="#打开Terminal，配置静态ip" class="headerlink" title="打开Terminal，配置静态ip"></a>打开Terminal，配置静态ip</h3><blockquote>
<p>sudo vi  /etc/network/interfaces</p>
</blockquote>
<p>内容改为如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">auto lo </span><br><span class="line">iface lo inet loopback</span><br><span class="line"></span><br><span class="line">auto ens33 </span><br><span class="line">iface ens33 inet static </span><br><span class="line">address 192.168.8.100 </span><br><span class="line">netmask 255.255.255.0 </span><br><span class="line">gateway 192.168.8.2</span><br></pre></td></tr></table></figure>
<p>这边说明一下，ubuntu15开始，网卡名<code>eth0</code>改成了<code>ens33</code>。</p>
<h3 id="配置dns"><a href="#配置dns" class="headerlink" title="配置dns"></a>配置dns</h3><blockquote>
<p>sudo vi /etc/resolv.conf</p>
</blockquote>
<p>在里面填入DNS，比如阿里的dns：223.5.5.5</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">nameserver 223.5.5.5</span><br></pre></td></tr></table></figure>
<p>设置完毕。</p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>有的网上说要执行<code>sudo /etc/init.d/networking restart</code>重启网络。<br>但是我们之前vmware设置选择自定义的nat模式，所以执行以上命令会出错，只要vmware改为<code>桥接模式直接连接物理网络</code>，勾选<code>复制物理网络连接状态</code>，在执行就可以。</p>
<p>但是这样会导致虚拟机连不到网络，所以我最后又将连接模式改为自定义NAT模式，发现这时候，主机可以ssh连接本地虚拟机了。</p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Ubuntu</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis入门</title>
    <url>/2016/11/11/Redis%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<p>redis官方不支持windows系统，所以我将redis装在linux下。<br>我用的Ubuntu 16 64位系统。</p>
<h1 id="安装Redis"><a href="#安装Redis" class="headerlink" title="安装Redis"></a>安装Redis</h1><blockquote>
<p>sudo apt-get install redis-server</p>
</blockquote>
<p>非常方便，一句话搞定。</p>
<h1 id="配置Redis"><a href="#配置Redis" class="headerlink" title="配置Redis"></a>配置Redis</h1><p>由于要远程连接Redis，所以要修改Redis的默认配置文件。<br>修改<code>/etc/redis/redis.conf</code>，把<code>bind 127.0.0.1</code>注释掉即可</p>
<blockquote>
<p> # bind 127.0.0.1</p>
</blockquote>
<h1 id="启动和关闭Redis服务"><a href="#启动和关闭Redis服务" class="headerlink" title="启动和关闭Redis服务"></a>启动和关闭Redis服务</h1><p>如果是用<code>apt-get</code>或者<code>yum install</code>安装的redis，可以直接通过下面的命令停止/启动/重启：</p>
<blockquote>
<p>/etc/init.d/redis-server stop<br>/etc/init.d/redis-server start<br>/etc/init.d/redis-server restart</p>
</blockquote>
<a id="more"></a>
<p>如果是通过源码安装的redis，则可以通过redis的客户端程序<code>redis-cli</code>的<code>shutdown</code>命令来重启redis：</p>
<blockquote>
<p>redis-cli -h 127.0.0.1 -p 6379 shutdown</p>
</blockquote>
<p>如果上述方式都没有成功停止redis，则可以使用终极武器 <code>kill -9</code>直接杀进程。</p>
<h1 id="Redis数据类型"><a href="#Redis数据类型" class="headerlink" title="Redis数据类型"></a>Redis数据类型</h1><p>redis有5种数据类型：</p>
<ul>
<li><code>string</code>（字符串）</li>
<li><code>hash</code>（哈希）</li>
<li><code>list</code>（列表）</li>
<li><code>set</code>（集合）</li>
<li><code>zset</code>（sorted set：有序集合）</li>
</ul>
<h2 id="string"><a href="#string" class="headerlink" title="string"></a>string</h2><p>string是redis最基本的类型，你可以理解成与Memcached一模一样的类型，一个key对应一个value。<br>string类型是二进制安全的。意思是redis的string可以包含任何数据。比如jpg图片或者序列化的对象 。<br>string类型是Redis最基本的数据类型，一个键最大能存储512MB。</p>
<h2 id="hash"><a href="#hash" class="headerlink" title="hash"></a>hash</h2><p>Redis hash 是一个键值对集合。<br>Redis hash是一个string类型的field和value的映射表，hash特别适合用于存储对象。</p>
<h2 id="list"><a href="#list" class="headerlink" title="list"></a>list</h2><p>Redis 列表是简单的字符串列表，按照插入顺序排序。你可以添加一个元素到列表的头部（左边）或者尾部（右边）。</p>
<h2 id="set"><a href="#set" class="headerlink" title="set"></a>set</h2><p>Redis的Set是string类型的无序集合。<br>集合是通过哈希表实现的，所以添加，删除，查找的复杂度都是O(1)。</p>
<h2 id="zset"><a href="#zset" class="headerlink" title="zset"></a>zset</h2><p>Redis zset 和 set 一样也是string类型元素的集合,且不允许重复的成员。<br>不同的是每个元素都会关联一个double类型的分数。redis正是通过分数来为集合中的成员进行从小到大的排序。<br>zset的成员是唯一的,但分数(score)却可以重复。</p>
<h1 id="Java操作Redis"><a href="#Java操作Redis" class="headerlink" title="Java操作Redis"></a>Java操作Redis</h1><h2 id="引入redis驱动包"><a href="#引入redis驱动包" class="headerlink" title="引入redis驱动包"></a>引入redis驱动包</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>redis.clients<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jedis<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.9.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h2 id="创建连接"><a href="#创建连接" class="headerlink" title="创建连接"></a>创建连接</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Jedis jedis = <span class="keyword">new</span> Jedis(<span class="string">"192.168.1.185"</span>);</span><br><span class="line">System.out.println(<span class="string">"Connection to server sucessfully"</span>);</span><br><span class="line"><span class="comment">//查看服务是否运行</span></span><br><span class="line">System.out.println(<span class="string">"Server is running: "</span> + jedis.ping());</span><br></pre></td></tr></table></figure>
<p>看到如下输出，说明连接成功</p>
<blockquote>
<p>Connection to server sucessfully<br>Server is running: PONG</p>
</blockquote>
]]></content>
      <categories>
        <category>NoSQL</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTP协议学习笔记</title>
    <url>/2016/11/08/HTTP%E5%8D%8F%E8%AE%AE%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p><img src="http://upload-images.jianshu.io/upload_images/4059338-6885f7d9242a214b.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="http"></p>
<h1 id="HTTP基本概念"><a href="#HTTP基本概念" class="headerlink" title="HTTP基本概念"></a>HTTP基本概念</h1><ul>
<li>HTTP是Hyper Text Transfer Protocol（超文本传输协议）的缩写。</li>
<li>HTTP是一个应用层协议，由请求和响应构成，是一个标准的客户端服务器模型。</li>
<li>HTTP是一个无状态的协议。</li>
</ul>
<h1 id="在TCP-IP协议栈中的位置"><a href="#在TCP-IP协议栈中的位置" class="headerlink" title="在TCP/IP协议栈中的位置"></a>在TCP/IP协议栈中的位置</h1><p>HTTP协议通常承载于TCP协议之上，有时也承载于TLS或SSL协议层之上，这个时候，就成了我们常说的HTTPS。如下图所示：<br><img src="http://img.blog.csdn.net/20161108133850318" alt="这里写图片描述"></p>
<a id="more"></a>
<p><strong>默认HTTP的端口号为80，HTTPS的端口号为443。</strong></p>
<h1 id="HTTP的请求响应模型"><a href="#HTTP的请求响应模型" class="headerlink" title="HTTP的请求响应模型"></a>HTTP的请求响应模型</h1><p>HTTP协议永远都是客户端发起请求，服务器回送响应。见下图：<br><img src="http://img.blog.csdn.net/20161108134113119" alt="这里写图片描述"></p>
<p>这样就限制了使用HTTP协议，无法实现在客户端没有发起请求的时候，服务器将消息推送给客户端。HTML5定义了WebSocket协议，它实现了浏览器与服务器全双工通信(full-duplex)，可以借助它实现服务器向客户端推送消息。</p>
<h1 id="HTTP工作流程"><a href="#HTTP工作流程" class="headerlink" title="HTTP工作流程"></a>HTTP工作流程</h1><p>一次HTTP操作称为一个事务，其工作过程可分为四步：</p>
<ol>
<li>首先客户机与服务器需要建立连接。只要单击某个超级链接，HTTP的工作开始。</li>
<li>建立连接后，客户机发送一个请求给服务器，请求方式的格式为：统一资源标识符（URL）、协议版本号，后边是MIME信息包括请求修饰符、客户机信息和可能的内容。</li>
<li>服务器接到请求后，给予相应的响应信息，其格式为一个状态行，包括信息的协议版本号、一个成功或错误的代码，后边是MIME信息包括服务器信息、实体信息和可能的内容。</li>
<li>客户端接收服务器所返回的信息通过浏览器显示在用户的显示屏上，然后客户机与服务器断开连接。<br>如果在以上过程中的某一步出现错误，那么产生错误的信息将返回到客户端，有显示屏输出。对于用户来说，这些过程是由HTTP自己完成的，用户只要用鼠标点击，等待信息显示就可以了。</li>
</ol>
<h1 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h1><p>HTTP支持几种不同的的请求命令，成为 HTTP方法。每一个请求报文中都包含一个方法，告诉服务器执行什么动作。</p>
<table>
<thead>
<tr>
<th align="left">常见http方法</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">GET</td>
<td align="left">向特定的资源发出请求，获取指定资源</td>
</tr>
<tr>
<td align="left">POST</td>
<td align="left">发送客户端数据到服务器（如表单提交）</td>
</tr>
<tr>
<td align="left">DELETE</td>
<td align="left">从服务器删除指定资源</td>
</tr>
<tr>
<td align="left">PUT</td>
<td align="left">向服务器指定资源上传最新数据（完整替换数据）</td>
</tr>
<tr>
<td align="left">PATCH</td>
<td align="left">向服务器指定资源上传最新数据（局部更新数据）</td>
</tr>
</tbody></table>
<p>此外，还有<code>head</code>，<code>trace</code>，<code>options</code>等。</p>
<h1 id="状态码"><a href="#状态码" class="headerlink" title="状态码"></a>状态码</h1><p>当服务器收到请求，返回的响应报文中会带有一个3位数字的状态码，告诉客户端请求是否成功。状态码分5类：</p>
<table>
<thead>
<tr>
<th align="center">整体范围</th>
<th align="center">已定义范围</th>
<th align="center">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">100-199</td>
<td align="center">100-101</td>
<td align="center">信息提示</td>
</tr>
<tr>
<td align="center">200-299</td>
<td align="center">200-206</td>
<td align="center">成功</td>
</tr>
<tr>
<td align="center">300-399</td>
<td align="center">300-305</td>
<td align="center">重定向</td>
</tr>
<tr>
<td align="center">400-499</td>
<td align="center">400-415</td>
<td align="center">客户端错误</td>
</tr>
<tr>
<td align="center">500-599</td>
<td align="center">500-505</td>
<td align="center">服务器错误</td>
</tr>
</tbody></table>
<p>常见的状态：<code>200</code>表示成功； <code>302</code>表示重定向；<code>404</code>表示找不到资源。</p>
<h1 id="HTTP报文"><a href="#HTTP报文" class="headerlink" title="HTTP报文"></a>HTTP报文</h1><h2 id="报文格式"><a href="#报文格式" class="headerlink" title="报文格式"></a>报文格式</h2><p>报文是具有固定格式的数据块，由3部分组成：</p>
<ul>
<li>对报文进行描述的起始行（start line）</li>
<li>包含属性的首部块（header）</li>
<li>可选的，包含数据的主体部分（body）</li>
</ul>
<p>请求报文格式</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;method&gt; &lt;request-URL&gt; &lt;version&gt;</span><br><span class="line">&lt;headers&gt;</span><br><span class="line"></span><br><span class="line">&lt;entity-body&gt;</span><br></pre></td></tr></table></figure>
<p>响应报文格式</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;version&gt; &lt;status&gt; &lt;reason-phrase&gt;</span><br><span class="line">&lt;headers&gt;</span><br><span class="line"></span><br><span class="line">&lt;entity-body&gt;</span><br></pre></td></tr></table></figure>
<p>只有起始行不同，主体是可选的。下图是一个报文例子：<br><img src="http://img.blog.csdn.net/20161108144346350" alt="这里写图片描述"></p>
<h2 id="Content-Type-字段"><a href="#Content-Type-字段" class="headerlink" title="Content-Type 字段"></a>Content-Type 字段</h2><p>头信息必须是 ASCII 码，后面的数据可以是任何格式。因此，服务器回应的时候，必须告诉客户端，数据是什么格式，所以有了<code>Content-Type</code>这个字段。<br>常见的类型:</p>
<ul>
<li>text/plain</li>
<li>text/html</li>
<li>text/css</li>
<li>image/jpeg</li>
<li>image/png</li>
<li>video/mp4</li>
<li>audio/mp4</li>
<li>application/javascript</li>
<li>application/json</li>
</ul>
<p>这些数据类型总称为<code>MIME type</code>，每个值包括一级类型和二级类型，之间用斜杠分隔。<br>除了预定义的类型，厂商也可以自定义类型：</p>
<blockquote>
<p>application/vnd.debian.binary-package</p>
</blockquote>
<p>上面的类型表明，发送的是Debian系统的二进制数据包。<br>MIME type还可以在尾部使用分号，添加参数。</p>
<blockquote>
<p>Content-Type: text/html; charset=utf-8</p>
</blockquote>
<p>上面的类型表明，发送的是网页，而且编码是UTF-8。<br>客户端请求的时候，可以使用Accept字段声明自己可以接受哪些数据格式。</p>
<blockquote>
<p>Accept: <em>/</em></p>
</blockquote>
<p>上面代码中，客户端声明自己可以接受任何格式的数据。<br>MIME type不仅用在HTTP协议，还可以用在其他地方，比如HTML网页。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;meta http-equiv&#x3D;&quot;Content-Type&quot; content&#x3D;&quot;text&#x2F;html; charset&#x3D;UTF-8&quot; &#x2F;&gt;</span><br><span class="line">&lt;!-- 等同于 --&gt;</span><br><span class="line">&lt;meta charset&#x3D;&quot;utf-8&quot; &#x2F;&gt;</span><br></pre></td></tr></table></figure>
<h2 id="Content-Encoding-字段"><a href="#Content-Encoding-字段" class="headerlink" title="Content-Encoding 字段"></a>Content-Encoding 字段</h2><p>由于发送的数据可以是任何格式，因此可以把数据压缩后再发送。Content-Encoding字段说明数据的压缩方法。</p>
<ul>
<li>Content-Encoding: gzip</li>
<li>Content-Encoding: compress</li>
<li>Content-Encoding: deflate</li>
</ul>
<p>客户端在请求时，用Accept-Encoding字段说明自己可以接受哪些压缩方法。</p>
<blockquote>
<p>Accept-Encoding: gzip, deflate</p>
</blockquote>
<p>参考资料：<br>《HTTP权威指南》<br><a href="http://www.ruanyifeng.com/blog/2016/08/http.html" target="_blank" rel="noopener">阮一峰-http入门</a></p>
]]></content>
      <categories>
        <category>日常记录</category>
      </categories>
      <tags>
        <tag>Http</tag>
      </tags>
  </entry>
  <entry>
    <title>OkHttp的基本使用——替代Apache HttpClient</title>
    <url>/2016/11/03/OkHttp%E7%9A%84%E5%9F%BA%E6%9C%AC%E4%BD%BF%E7%94%A8%E2%80%94%E2%80%94%E6%9B%BF%E4%BB%A3Apache%20HttpClient/</url>
    <content><![CDATA[<p>http是现在主流应用使用的网络请求方式, 用来交换数据和内容, 有效的使用HTTP可以使你的APP 变的更快和减少流量的使用。</p>
<p>OkHttp 是一个很棒HTTP客户端:</p>
<ul>
<li>支持SPDY, 可以合并多个到同一个主机的请求</li>
<li>使用连接池技术减少请求的延迟(如果SPDY是可用的话)</li>
<li>使用GZIP压缩减少传输的数据量</li>
<li>缓存响应避免重复的网络请求</li>
</ul>
<p>OkHttp可以替换Apache的HttpClient<br>OkHttp支持2.3和以上版本，对于java，需要jdk1.7 ，OkHttp需要依赖Okio包</p>
<a id="more"></a>
<hr>
<p>下面上demo</p>
<h2 id="使用get方式请求，获取响应"><a href="#使用get方式请求，获取响应" class="headerlink" title="使用get方式请求，获取响应"></a>使用get方式请求，获取响应</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> okhttp3.OkHttpClient;</span><br><span class="line"><span class="keyword">import</span> okhttp3.Request;</span><br><span class="line"><span class="keyword">import</span> okhttp3.Response;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GetExample</span> </span>&#123;</span><br><span class="line">  OkHttpClient client = <span class="keyword">new</span> OkHttpClient();</span><br><span class="line"></span><br><span class="line">  <span class="function">String <span class="title">run</span><span class="params">(String url)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    Request request = <span class="keyword">new</span> Request.Builder()</span><br><span class="line">        .url(url)</span><br><span class="line">        .build();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> (Response response = client.newCall(request).execute()) &#123;</span><br><span class="line">      <span class="keyword">return</span> response.body().string();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    GetExample example = <span class="keyword">new</span> GetExample();</span><br><span class="line">    String response = example.run(<span class="string">"https://raw.github.com/square/okhttp/master/README.md"</span>);</span><br><span class="line">    System.out.println(response);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="使用post向服务器发送请求"><a href="#使用post向服务器发送请求" class="headerlink" title="使用post向服务器发送请求"></a>使用post向服务器发送请求</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> okhttp3.MediaType;</span><br><span class="line"><span class="keyword">import</span> okhttp3.OkHttpClient;</span><br><span class="line"><span class="keyword">import</span> okhttp3.Request;</span><br><span class="line"><span class="keyword">import</span> okhttp3.RequestBody;</span><br><span class="line"><span class="keyword">import</span> okhttp3.Response;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PostExample</span> </span>&#123;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> MediaType JSON</span><br><span class="line">      = MediaType.parse(<span class="string">"application/json; charset=utf-8"</span>);</span><br><span class="line"></span><br><span class="line">  OkHttpClient client = <span class="keyword">new</span> OkHttpClient();</span><br><span class="line"></span><br><span class="line">  <span class="function">String <span class="title">post</span><span class="params">(String url, String json)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    RequestBody body = RequestBody.create(JSON, json);</span><br><span class="line">    Request request = <span class="keyword">new</span> Request.Builder()</span><br><span class="line">        .url(url)</span><br><span class="line">        .post(body)</span><br><span class="line">        .build();</span><br><span class="line">    <span class="keyword">try</span> (Response response = client.newCall(request).execute()) &#123;</span><br><span class="line">      <span class="keyword">return</span> response.body().string();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function">String <span class="title">bowlingJson</span><span class="params">(String player1, String player2)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"&#123;'winCondition':'HIGH_SCORE',"</span></span><br><span class="line">        + <span class="string">"'name':'Bowling',"</span></span><br><span class="line">        + <span class="string">"'round':4,"</span></span><br><span class="line">        + <span class="string">"'lastSaved':1367702411696,"</span></span><br><span class="line">        + <span class="string">"'dateStarted':1367702378785,"</span></span><br><span class="line">        + <span class="string">"'players':["</span></span><br><span class="line">        + <span class="string">"&#123;'name':'"</span> + player1 + <span class="string">"','history':[10,8,6,7,8],'color':-13388315,'total':39&#125;,"</span></span><br><span class="line">        + <span class="string">"&#123;'name':'"</span> + player2 + <span class="string">"','history':[6,10,5,10,10],'color':-48060,'total':41&#125;"</span></span><br><span class="line">        + <span class="string">"]&#125;"</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    PostExample example = <span class="keyword">new</span> PostExample();</span><br><span class="line">    String json = example.bowlingJson(<span class="string">"Jesse"</span>, <span class="string">"Jake"</span>);</span><br><span class="line">    String response = example.post(<span class="string">"http://www.roundsapp.com/post"</span>, json);</span><br><span class="line">    System.out.println(response);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>日常记录</category>
      </categories>
      <tags>
        <tag>Http</tag>
        <tag>OkHttp</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Hexo搭建个人博客</title>
    <url>/2016/10/27/%E4%BD%BF%E7%94%A8Hexo%E6%90%AD%E5%BB%BA%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2/</url>
    <content><![CDATA[<p>最近闲着没事，用<code>hexo</code>搭建了一个个人博客，部署在 <a href="https://peijie-sh.github.io/" target="_blank" rel="noopener">github</a> 和 <a href="https://peijie-sh.github.io/" target="_blank" rel="noopener">oschina</a>上，欢迎大家来我博客踩踩~ ^_^<br><img src="http://upload-images.jianshu.io/upload_images/4059338-127b161e567ad4d7.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="hexo.jpg"></p>
<p>下面总结一下搭建过程：</p>
<h1 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建"></a>环境搭建</h1><p>hexo是一个简单地、轻量地、基于Node的一个静态博客框架，可以方便的生成静态网页托管在github和Heroku上<br>引用Hexo作者 @tommy351 的话：</p>
<blockquote>
<p>快速、简单且功能强大的 Node.js 博客框架。A fast, simple &amp; powerful blog framework, powered by Node.js.</p>
</blockquote>
<p>所以，第一步，就是下载node的安装包并安装，附上<a href="https://nodejs.org/en/download/" target="_blank" rel="noopener">node下载地址</a>。<br>安装好node，会默认一起安装好npm包管理器，这可是一个神器啊。<br>可以打开终端cmd，执行一下命令确认安装完毕。</p>
<blockquote>
<p>node -v<br>npm -version</p>
</blockquote>
<p>另外一个，就是git了。git也没什么好说的，官网下载，选择自己操作系统的版本，一路next就装好了。</p>
<a id="more"></a>
<h1 id="安装hexo"><a href="#安装hexo" class="headerlink" title="安装hexo"></a>安装hexo</h1><p>在<code>git</code>终端或者<code>cmd</code>执行安装<code>hexo</code></p>
<blockquote>
<p>npm install hexo-cli -g</p>
</blockquote>
<p>以windows系统为例，在你想要安装的盘新建一个目录，比如<code>d:/blog</code>,在此目录下右键打开<code>git</code>的终端(就是<code>Git Bash Here</code>),然后执行</p>
<blockquote>
<p>hexo init</p>
</blockquote>
<p>初始化博客目录，然后执行</p>
<blockquote>
<p>npm install</p>
</blockquote>
<p>会下载需要的库。之后执行</p>
<blockquote>
<p>hexo server</p>
</blockquote>
<p>简便语法：<code>hexo s</code><br>启动本地服务器，在浏览器打开<code>localhost:4000</code>就可以看到效果了。<br>关闭本地服务：<code>ctrl + c</code><br>有不明白可以看<a href="https://hexo.io/" target="_blank" rel="noopener">hexo官网</a>,官网讲的很详细了。</p>
<h1 id="创建Github仓库"><a href="#创建Github仓库" class="headerlink" title="创建Github仓库"></a>创建Github仓库</h1><p>本地博客搭建完，只要部署在github上就ok啦。</p>
<ul>
<li>登录github，没有的自己注册。</li>
<li>创建一个Repository，名字必须为【你的用户名.github.io】。</li>
</ul>
<h1 id="配置博客部署信息"><a href="#配置博客部署信息" class="headerlink" title="配置博客部署信息"></a>配置博客部署信息</h1><p>博客的根目录<code>d:/blog</code>下有一个配置文件<code>_config.yml</code>，这里可以配置你的github的地址等信息，配置完，执行</p>
<blockquote>
<p>hexo g</p>
</blockquote>
<p>生成静态文件，然后</p>
<blockquote>
<p>hexo d</p>
</blockquote>
<p>就部署到github上了，然后在浏览器打开 【你的用户名.github.io】就可以了。</p>
<h1 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h1><p>这个过程你会遇到许多问题，这边放上几篇参考，可以解决你的困惑。</p>
<ul>
<li><a href="http://www.jianshu.com/p/465830080ea9" target="_blank" rel="noopener">http://www.jianshu.com/p/465830080ea9</a></li>
<li><a href="http://www.tuicool.com/articles/ueI7naV" target="_blank" rel="noopener">http://www.tuicool.com/articles/ueI7naV</a></li>
<li>这些不能解决你的问题，请自行google</li>
</ul>
<h1 id="DIY博客"><a href="#DIY博客" class="headerlink" title="DIY博客"></a>DIY博客</h1><p><code>hexo</code>默认主题是<code>landscape</code>，不喜欢，可以自己替换主题，这当然需要自己配置许多东西，我用的是<code>NexT</code>主题，放上一点参考吧<a href="http://theme-next.iissnan.com/getting-started.html" target="_blank" rel="noopener">NexT官网</a>。</p>
]]></content>
      <categories>
        <category>日常记录</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>Node.Js</tag>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>VIM编辑器的基本使用</title>
    <url>/2016/10/26/VIM%E7%BC%96%E8%BE%91%E5%99%A8%E7%9A%84%E5%9F%BA%E6%9C%AC%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<h1 id="VIM的运行模式"><a href="#VIM的运行模式" class="headerlink" title="VIM的运行模式"></a>VIM的运行模式</h1><ul>
<li>编辑模式：等待编辑命令输入</li>
<li>插入模式：编辑模式下，输入 i 进入插入模式，插入文本信息，<code>esc</code>退出</li>
<li>命令模式：在编辑模式下，输入 “：” 进行命令模式</li>
</ul>
<h1 id="VIM-常用命令"><a href="#VIM-常用命令" class="headerlink" title="VIM 常用命令"></a>VIM 常用命令</h1><ul>
<li>：q 直接退出vi</li>
<li>：wq保存后退出vi ，并可以新建文件</li>
<li>：q! 强制退出</li>
<li>：w file 将当前内容保存成某个文件</li>
<li>：set number 在编辑文件显示行号</li>
<li>：set nonumber  在编辑文件不显示行号</li>
</ul>
<a id="more"></a>
<h1 id="VIM命令大全"><a href="#VIM命令大全" class="headerlink" title="VIM命令大全"></a>VIM命令大全</h1><p><img src="http://img.blog.csdn.net/20161115114043493" alt="这里写图片描述"></p>
<h2 id="进入vi的命令"><a href="#进入vi的命令" class="headerlink" title="进入vi的命令"></a>进入vi的命令</h2><ul>
<li>vi filename :打开或新建文件，并将光标置于第一行首</li>
<li>vi +n filename ：打开文件，并将光标置于第n行首</li>
<li>vi + filename ：打开文件，并将光标置于最后一行首</li>
<li>vi +/pattern filename：打开文件，并将光标置于第一个与pattern匹配的串处</li>
<li>vi -r filename ：在上次正用vi编辑时发生系统崩溃，恢复filename</li>
<li>vi filename….filename ：打开多个文件，依次进行编辑</li>
</ul>
<h2 id="移动光标类命令"><a href="#移动光标类命令" class="headerlink" title="移动光标类命令"></a>移动光标类命令</h2><ul>
<li>h ：光标左移一个字符</li>
<li>l ：光标右移一个字符</li>
<li>space：光标右移一个字符</li>
<li>Backspace：光标左移一个字符</li>
<li>k或Ctrl+p：光标上移一行</li>
<li>j或Ctrl+n ：光标下移一行 </li>
<li>Enter ：光标下移一行</li>
<li>w或W ：光标右移一个字至字首 </li>
<li>b或B ：光标左移一个字至字首 </li>
<li>e或E ：光标右移一个字至字尾 </li>
<li>) ：光标移至句尾 </li>
<li>( ：光标移至句首 </li>
<li>}：光标移至段落开头 </li>
<li>{：光标移至段落结尾 </li>
<li>nG：光标移至第n行首</li>
<li>n+：光标下移n行</li>
<li>n-：光标上移n行</li>
<li>n$：光标移至第n行尾</li>
<li>H ：光标移至屏幕顶行</li>
<li>M ：光标移至屏幕中间行</li>
<li>L ：光标移至屏幕最后行</li>
<li>0：（注意是数字零）光标移至当前行首</li>
<li>$：光标移至当前行尾 </li>
</ul>
<h2 id="屏幕翻滚类命令"><a href="#屏幕翻滚类命令" class="headerlink" title="屏幕翻滚类命令"></a>屏幕翻滚类命令</h2><ul>
<li>Ctrl+u：向文件首翻半屏</li>
<li>Ctrl+d：向文件尾翻半屏</li>
<li>Ctrl+f：向文件尾翻一屏</li>
<li>Ctrl＋b；向文件首翻一屏</li>
<li>nz：将第n行滚至屏幕顶部，不指定n时将当前行滚至屏幕顶部。</li>
</ul>
<h2 id="插入文本类命令"><a href="#插入文本类命令" class="headerlink" title="插入文本类命令"></a>插入文本类命令</h2><ul>
<li>i ：在光标前 </li>
<li>I ：在当前行首 </li>
<li>a：光标后 </li>
<li>A：在当前行尾 </li>
<li>o：在当前行之下新开一行</li>
<li>O：在当前行之上新开一行</li>
<li>r：替换当前字符 </li>
<li>R：替换当前字符及其后的字符，直至按ESC键</li>
<li>s：从当前光标位置处开始，以输入的文本替代指定数目的字符</li>
<li>S：删除指定数目的行，并以所输入文本代替之</li>
<li>ncw或nCW：修改指定数目的字</li>
<li>nCC：修改指定数目的行</li>
</ul>
<h2 id="删除命令"><a href="#删除命令" class="headerlink" title="删除命令"></a>删除命令</h2><ul>
<li>ndw或ndW：删除光标处开始及其后的n-1个字</li>
<li>do：删至行首 </li>
<li>d$：删至行尾 </li>
<li>ndd：删除当前行及其后n-1行</li>
<li>x或X：删除一个字符，x删除光标后的，而X删除光标前的</li>
<li>Ctrl+u：删除输入方式下所输入的文本</li>
</ul>
<h2 id="搜索及替换命令"><a href="#搜索及替换命令" class="headerlink" title="搜索及替换命令"></a>搜索及替换命令</h2><ul>
<li>/pattern：从光标开始处向文件尾搜索pattern</li>
<li>?pattern：从光标开始处向文件首搜索pattern</li>
<li>n：在同一方向重复上一次搜索命令</li>
<li>N：在反方向上重复上一次搜索命令</li>
<li>：s/p1/p2/g：将当前行中所有p1均用p2替代</li>
<li>：n1,n2s/p1/p2/g：将第n1至n2行中所有p1均用p2替代</li>
<li>：g/p1/s//p2/g：将文件中所有p1均用p2替换</li>
</ul>
<h2 id="选项设置"><a href="#选项设置" class="headerlink" title="选项设置"></a>选项设置</h2><ul>
<li>all：列出所有选项设置情况</li>
<li>term：设置终端类型</li>
<li>ignorance：在搜索中忽略大小写</li>
<li>list：显示制表位(Ctrl+I)和行尾标志（$)</li>
<li>number：显示行号</li>
<li>report：显示由面向行的命令修改过的数目</li>
<li>terse：显示简短的警告信息</li>
<li>warn：在转到别的文件时若没保存当前文件则显示NO write信息</li>
<li>nomagic：允许在搜索模式中，使用前面不带“\”的特殊字符</li>
<li>nowrapscan：禁止vi在搜索到达文件两端时，又从另一端开始</li>
<li>mesg：允许vi显示其他用户用write写到自己终端上的信息</li>
</ul>
<h2 id="最后行方式命令"><a href="#最后行方式命令" class="headerlink" title="最后行方式命令"></a>最后行方式命令</h2><ul>
<li>：n1,n2 co n3：将n1行到n2行之间的内容拷贝到第n3行下</li>
<li>：n1,n2 m n3：将n1行到n2行之间的内容移至到第n3行下</li>
<li>：n1,n2 d ：将n1行到n2行之间的内容删除</li>
<li>：w ：保存当前文件</li>
<li>：e filename：打开文件filename进行编辑</li>
<li>：x：保存当前文件并退出</li>
<li>：q：退出vi</li>
<li>：q!：不保存文件并退出vi</li>
<li>：!command：执行shell命令command</li>
<li>：n1,n2 w!command：将文件中n1行至n2行的内容作为command的输入并执行之，若不指定n1，n2，则表示将整个文件内容作为command的输入</li>
<li>：r!command：将命令command的输出结果放到当前行</li>
</ul>
<hr>
<p><a href="http://www.cnblogs.com/mfc-itblog/p/5895395.html" target="_blank" rel="noopener">更多使用参考</a></p>
]]></content>
      <categories>
        <category>日常记录</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Vim</tag>
      </tags>
  </entry>
  <entry>
    <title>Oracle入门</title>
    <url>/2016/10/25/Oracle%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<p><img src="http://upload-images.jianshu.io/upload_images/4059338-7bf55c1ff662d501.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="oracle.png"></p>
<p>今天学习了Oracle的基本使用，做下总结：</p>
<h1 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a>基本操作</h1><p>以Oracle 11g为例，安装好以后，普通账户都是未解锁的，不能使用，只能使用超级管理员登录。</p>
<blockquote>
<p>sqlplus / as sysdba</p>
</blockquote>
<p>这样就用超级管理员登录了，无需密码。<br>然后解锁普通用户之后，就可以使用使用普通用户了。<br>举例：</p>
<a id="more"></a>

<blockquote>
<p>– – 解锁用户 scott<br>alter user scott account unlock;<br>– – 设置密码为 tiger<br>alter user scott identified by tiger;<br>– – 退出登录<br>exit<br>– – 使用普通用户 scott 登录<br>sqlplus scott/tiger</p>
</blockquote>
<ul>
<li><p>普通用户修改密码，需要验证旧密码，然后根据提示修改即可。</p>
<blockquote>
<p>password</p>
</blockquote>
</li>
<li><p>查询当前用户是谁         </p>
<blockquote>
<p>show user;</p>
</blockquote>
</li>
<li><p>查询scott用户下的所有对象（表），使用tab表，tab表每个用户都有</p>
<blockquote>
<p>select * from tab;</p>
</blockquote>
</li>
<li><p>设置显示的列宽（字符型varchar2、日期型date），10个宽度位，a表示字符型，大小写均可</p>
<blockquote>
<p>column ename format a12;</p>
</blockquote>
</li>
<li><p>设置显示的列宽（数值型number），9表示数字型，一个9表示一个数字位，四个9表示四个数字位，只能用9</p>
<blockquote>
<p>column empno format 9999;</p>
</blockquote>
</li>
<li><p>设置一页显示60条记录的高度</p>
<blockquote>
<p>set pagesize 60;</p>
</blockquote>
</li>
<li><p>使用/杠，执行最近一次的SQL语句</p>
<blockquote>
<p>/</p>
</blockquote>
</li>
<li><p>清屏，属于SQL*PLUS工具中的命令</p>
<blockquote>
<p>host cls;</p>
</blockquote>
</li>
<li><p>查询emp表的结构</p>
<blockquote>
<p>desc emp;</p>
</blockquote>
</li>
<li><p>使用dual哑表或者伪表，使用字符串连接符号||，输出”hello world”，在oracle中from是必须写的</p>
<blockquote>
<p>select ‘hello’ || ‘ world’ “结果” from dual;</p>
</blockquote>
</li>
<li><p>使用sysdate，显示系统当前时间，在默认情况下，oracle只显示日期，而不显示时间，格式：26-4月-15</p>
<blockquote>
<p>select sysdate from dual;</p>
</blockquote>
</li>
<li><p>使用spool命令，保存SQL语句到硬盘文件e:/oracle-day01.sql，并创建sql文件</p>
<blockquote>
<p>spool e:/oracle-day01.sql;</p>
</blockquote>
</li>
<li><p>使用spool off命令，保存SQL语句到硬盘文件e:/oracle-day01.sql，并创建sql文件，结束语句</p>
<blockquote>
<p>spool off;</p>
</blockquote>
</li>
<li><p>使用<code>@</code>命令，将硬盘文件e:/crm.sql，读到orcl实例中，并执行文件中的sql语句</p>
<blockquote>
<p>@ e:/crm.sql; </p>
</blockquote>
</li>
<li><p>使用<code>--</code>符号，设置单行注释</p>
</li>
<li><p>使用<code>/* */</code>符号，设置多行注释</p>
</li>
</ul>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>SQL语句的特点</p>
<ol>
<li>是SQL92/99的ANSI官方标准，只要按照该标准来写，在任何的关系型数据库中都可以直接执行</li>
<li>SQL语句的关健字不能简写，例如：select，where，from</li>
<li>大小写不敏感，提倡大写 </li>
<li>能够对表数据进行增删改查操作</li>
<li>必须以分号结束</li>
<li>通常称做语句</li>
</ol>
<p>SQLPLUS命令的特点</p>
<ol>
<li>是oracle自带的一款工具，在该工具中执行的命令叫SQLPLUS命令</li>
<li>SQLPLUS工具的命令中的关健字可以简写，也可以不简写，例如：col ename for a10;</li>
<li>大小写不敏感，提倡大写</li>
<li>不能够对表数据进行增删改查操作，只能完成显示格式控制，例如：设置显示列宽，清屏，记录执行结果</li>
<li>可以不用分号结束，也可以用分号结束，个人提倡不管SQL或SQLPLUS，都以分号结束</li>
<li>通常称做命令，是SQLPLUS工具中的命令</li>
</ol>
<p><strong>注意：SQLPLUS命令是SQLPLUS工具中特有的语句</strong></p>
<p>单引号出现的地方如下：</p>
<ol>
<li>字符串型，例如：’hello’ || ‘ world’</li>
<li>日期型，例如’25-4月-15’</li>
</ol>
<p>双引号出现的地方如下：</p>
<ol>
<li>列别名，例如：sal<em>12 “年 薪”，或 sal</em>12 年薪，个人提倡用<code>&quot;&quot;</code>双引号作列别名</li>
</ol>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>Oracle</tag>
      </tags>
  </entry>
  <entry>
    <title>Maven学习笔记（3） --- 生命周期</title>
    <url>/2016/10/25/Maven%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%EF%BC%883%EF%BC%89%20---%20%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F/</url>
    <content><![CDATA[<h1 id="Maven的3个生命周期："><a href="#Maven的3个生命周期：" class="headerlink" title="Maven的3个生命周期："></a>Maven的3个生命周期：</h1><p>Maven的生命周期就是对所有的构建过程进行抽象和统一。包含了项目的清理、初始化、编译、测试、打包、集成测试、验证、部署和站点生成等几乎所有的构建步骤。</p>
<p>Maven的生命周期是抽象的，即生命周期不做任何实际的工作，实际任务由插件完成，类似于设计模式中的模板方法。<br>每个生命周期包含一些阶段（phase），阶段是有顺序的，后面的阶段依赖于前面的阶段。</p>
<ul>
<li>clean生命周期：清理项目，phase如下。<ul>
<li><code>pre-clean</code>：执行清理前需要完成的工作</li>
<li><code>clean</code>：清理上一次构建生成的文件</li>
<li><code>post-clean</code>：执行清理后需要完成的工作</li>
</ul>
</li>
</ul>
<a id="more"></a>    
<ul>
<li><p>default生命周期：构建项目，phase如下。</p>
<ul>
<li><code>validate</code>：验证工程是否正确，所有需要的资源是否可用。</li>
<li><code>initialize</code>：初始化</li>
<li><code>generate-sources</code>：</li>
<li><code>process-sources</code>：处理资源文件 【src/main/resources】</li>
<li><code>generate-resources</code>：</li>
<li><code>process-resources</code>：</li>
<li><code>compile</code>：编译项目的源代码。【src/main/java】</li>
<li><code>process-classes</code>：处理编译后的class文件</li>
<li><code>generate-test-sources</code>：</li>
<li><code>process-test-sources</code>：处理测试资源文件【src/test/resources】</li>
<li><code>generate-test-resources</code>：</li>
<li><code>process-test-resources</code>：</li>
<li><code>test-compile</code>：编译测试文件【src/test/java】</li>
<li><code>process-test-classes</code>：处理编译后的测试class文件</li>
<li><code>test</code>：使用单元测试框架来测试已编译的源代码。测试代码不会被打包或布署。</li>
<li><code>pre-package</code>：打包之前要做的准备工作</li>
<li><code>package</code>：把已编译的代码打包成可发布的格式，比如jar。</li>
<li><code>pre-integration-test</code>：</li>
<li><code>integration-test</code>：如有需要，将包处理和发布到一个能够进行集成测试的环境。</li>
<li><code>post-integration-test</code>：</li>
<li><code>verify</code>：运行所有检查，验证包是否有效且达到质量标准。</li>
<li><code>install</code>：把包安装到maven本地仓库，可以被其他工程作为依赖来使用。</li>
<li><code>deploy</code>：在集成或者发布环境下执行，将最终版本的包拷贝到远程的repository，使得其他的开发者或者工程可以共享。</li>
</ul>
</li>
<li><p>site生命周期：建立和发布项目站点，phase如下。</p>
<ul>
<li><code>pre-site</code>：生成项目站点之前需要完成的工作</li>
<li><code>site</code>：生成项目站点文档</li>
<li><code>post-site</code>：生成项目站点之后需要完成的工作</li>
<li><code>site-deploy</code>：将项目站点发布到服务器</li>
</ul>
</li>
</ul>
<h1 id="命令行与生命周期"><a href="#命令行与生命周期" class="headerlink" title="命令行与生命周期"></a>命令行与生命周期</h1><ul>
<li><code>mvn clean</code> ：调用clean生命周期的clean阶段，实际执行pre-clean和clean阶段。</li>
<li><code>mvn test</code>：调用default生命周期的test阶段，实际执行test以及之前所有阶段。</li>
<li><code>mvn clean install</code>：调用clean生命周期的clean阶段和default的install阶段，实际执行pre-clean和clean，install以及之前所有阶段。</li>
<li><code>mvn clean deploy site-deploy</code>：调用clean生命周期的clean阶段，default生命周期的deploy阶段和site生命周期的site-deploy阶段。</li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Maven</tag>
      </tags>
  </entry>
  <entry>
    <title>Maven学习笔记（2） --- 依赖</title>
    <url>/2016/10/25/Maven%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%EF%BC%882%EF%BC%89%20---%20%E4%BE%9D%E8%B5%96/</url>
    <content><![CDATA[<h1 id="maven依赖"><a href="#maven依赖" class="headerlink" title="maven依赖"></a>maven依赖</h1><p>在maven项目中，我们会在pom.xml文件中引入我们需要用到的依赖，一般用<code>groupId</code>,<code>artifactId</code>,<code>version</code>就够了，如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-core<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">version</span>&gt;</span>4.1.7.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<a id="more"></a>
<p>有时，也会指定依赖范围，比如：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">version</span>&gt;</span>4.12<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>maven有3套classpath，编译classpath，运行classpath，测试classpath。</p>
<h1 id="依赖范围"><a href="#依赖范围" class="headerlink" title="依赖范围"></a>依赖范围</h1><p>依赖范围有以下几种：</p>
<ul>
<li><code>compile</code> 编译依赖范围。默认是这种范围。对3种classpath都有效。典型例子是<code>spring-core</code>，在编译，运行，测试时都需要。 </li>
<li><code>test</code> 测试依赖范围。只对测试classpath有效。典型例子是<code>junit</code>，它只在测试期间需要。</li>
<li><code>provided</code> 已提供依赖范围。对编译classpath和测试classpath有效，运行classpath无效。典型例子是<code>servlet-api</code>，运行的时候容器会提供，所以运行时不需要。</li>
<li><code>runtime</code> 运行时依赖范围。对运行classpath和测试classpath有效。典型例子是<code>jdbc</code>驱动实现，编译的时候只要有jdk的jdbc接口即可。</li>
<li><code>system</code> 系统依赖范围。和<code>provided</code>一样，对3种classpath都有效。但是，使用时必须通过systemPath元素显式指定依赖文件的路径。限制比较多，一般不使用。</li>
<li><code>import</code> 导入依赖范围。对3种classpath都没实际影响。</li>
</ul>
<h1 id="依赖机制"><a href="#依赖机制" class="headerlink" title="依赖机制"></a>依赖机制</h1><ol>
<li>依赖会传播：A依赖B，B依赖C，那么maven解析的时候会找到B，发现B依赖C，又去把C引入，然后在引入B</li>
<li>有相同依赖的时候，优先选路径最近的：比如A-&gt;B-&gt;C-&gt;X(1.0), A-&gt;D-&gt;X(2.0),这时候有2个X，会冲突，maven会选择最近的，也就是X(2.0)</li>
<li>在依赖路径长度相同时，优先选先声明的：比如A-&gt;B-&gt;Y(1.0), A-&gt;C-&gt;Y(2.0),在maven2.0.9之后，maven会选在pom中先声明的那个。即如果C的依赖声明在B之前，就选Y(2.0) </li>
</ol>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Maven</tag>
      </tags>
  </entry>
  <entry>
    <title>Maven学习笔记（1） --- 仓库</title>
    <url>/2016/10/25/Maven%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%EF%BC%881%EF%BC%89%20---%20%E4%BB%93%E5%BA%93/</url>
    <content><![CDATA[<h1 id="maven坐标"><a href="#maven坐标" class="headerlink" title="maven坐标"></a>maven坐标</h1><p>maven的构件（jar包）是根据坐标来确定其在仓库的保存位置的。<br>maven的坐标是通过以下元素来定义的：</p>
<ul>
<li>groupId : 一般是当前项目的公司名和项目组</li>
<li>artifactId : 项目名称</li>
<li>version : 项目版本</li>
<li>packaging : 打包方式，默认是<code>jar</code>,也可以是<code>war</code></li>
<li>classifier : 定义构建输出的一些附属构件，不常用</li>
</ul>
<p>形如</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.company.group<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>MyApp<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">packaging</span>&gt;</span>war<span class="tag">&lt;/<span class="name">packaging</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br></pre></td></tr></table></figure>

<a id="more"></a>
<h1 id="maven仓库"><a href="#maven仓库" class="headerlink" title="maven仓库"></a>maven仓库</h1><p>顾名思义，仓库就是存放依赖（jar包）的地方。<br>maven仓库分为<code>本地仓库</code>，<code>中央仓库</code>，<code>远程仓库</code>，和<code>私服</code>4种。</p>
<h2 id="本地仓库"><a href="#本地仓库" class="headerlink" title="本地仓库"></a>本地仓库</h2><p>通俗的说，本地仓库就是我们本地电脑中的一个文件夹，用来存放jar包的仓库。<br>本地仓库的默认位置：无论是Windows还是Linux，在用户的目录下都有一个.m2/repository/的仓库目录。<br>可通过修改maven安装目录下的settings.xml文件来修改本地仓库位置:<code>&lt;localRepository&gt;D:\repo&lt;/localRepository&gt;</code></p>
<h2 id="中央仓库"><a href="#中央仓库" class="headerlink" title="中央仓库"></a>中央仓库</h2><p>中央仓库是默认的远程仓库，它包含了绝大多数流行的开源Java构件，以及源码、作者信息、SCM、信息、许可证信息等。id是central，url地址是<a href="http://repo1.maven.org/maven2" target="_blank" rel="noopener">http://repo1.maven.org/maven2</a></p>
<h2 id="远程仓库"><a href="#远程仓库" class="headerlink" title="远程仓库"></a>远程仓库</h2><p>在很多情况下，默认的中央仓库无法满足项目的需求，可能项目需要的构件存在于另外一个远程仓库中，如:JBoss，Maven仓库。这时，可以在POM中配置该仓库，比如：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span>&gt;</span>  </span><br><span class="line">  ...  </span><br><span class="line">  <span class="tag">&lt;<span class="name">repositories</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">repository</span>&gt;</span>  </span><br><span class="line">      <span class="tag">&lt;<span class="name">id</span>&gt;</span>jboss<span class="tag">&lt;/<span class="name">id</span>&gt;</span>  </span><br><span class="line">      <span class="tag">&lt;<span class="name">name</span>&gt;</span>JBoss Repository<span class="tag">&lt;/<span class="name">name</span>&gt;</span>  </span><br><span class="line">      <span class="tag">&lt;<span class="name">url</span>&gt;</span>http://repository.jboss.com/maven2/<span class="tag">&lt;/<span class="name">url</span>&gt;</span>  </span><br><span class="line">      <span class="tag">&lt;<span class="name">releases</span>&gt;</span></span><br><span class="line">	    <span class="comment">&lt;!-- 更新频率never,always,interval,daily --&gt;</span>   </span><br><span class="line">        <span class="tag">&lt;<span class="name">updatePolicy</span>&gt;</span>daily<span class="tag">&lt;/<span class="name">updatePolicy</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">enabled</span>&gt;</span>true<span class="tag">&lt;/<span class="name">enabled</span>&gt;</span></span><br><span class="line">          <span class="comment">&lt;!-- 检查和检验文件的策略,fail,warn,ignore --&gt;</span>   </span><br><span class="line">        <span class="tag">&lt;<span class="name">checksumPolicy</span>&gt;</span>warn<span class="tag">&lt;/<span class="name">checksumPolicy</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">releases</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;/<span class="name">repository</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">snapshots</span>&gt;</span>  </span><br><span class="line">      <span class="tag">&lt;<span class="name">enabled</span>&gt;</span>false<span class="tag">&lt;/<span class="name">enabled</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;/<span class="name">snapshots</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">layout</span>&gt;</span>default<span class="tag">&lt;/<span class="name">layout</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;/<span class="name">repositories</span>&gt;</span>  </span><br><span class="line">  ...  </span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>在repositories元素下，用repository子元素声明一个或者多个远程仓库。该例中声明了一个id为jboss，名称为JBoss Repository的仓库。任何一个仓库声明的id必须是唯一的，尤其需要注意的是，maven自带的中央仓库使用的id为<code>central</code>，如果其他的仓库声明也使用该id，就会覆盖中央仓库的配置。该配置中的url值指向了仓库的地址，一般来说，该地址都基于http协议，maven用户都可以在浏览器中打开仓库地址浏览构件。<br>该例配置中的releases和snapshots元素比较重要，它们用来控制Maven对于发布版构件和快照版构件的下载。该例中releases的enabled值为true，表示开启JBoss仓库的发布版本下载支持，而snapshots的enabled值为false，表示关闭JBoss仓库的快照版本的下载支持。该例中的layout元素值default表示仓库的布局是Maven2及Maven3的默认布局，而不是Maven1的布局。</p>
<h3 id="远程仓库的认证"><a href="#远程仓库的认证" class="headerlink" title="远程仓库的认证"></a>远程仓库的认证</h3><p>远程仓库默认无须认证就可访问，有时出于安全需要，可配置认证访问。<br>配置认证信息和配置仓库信息不同，仓库信息可以直接配置在POM文件中，但是认证信息必须配置在settings.xml文件中。这是因为POM往往是被提交到代码仓库中供所有成员访问的，而settings.xml一般只放在本机。因此，settings.xml中配置认证信息更为安全。<br>假设为id为my-proj的仓库配置认证信息，编辑settings.xml文件如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">settings</span>&gt;</span>  </span><br><span class="line">  ...  </span><br><span class="line">  <span class="tag">&lt;<span class="name">servers</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">server</span>&gt;</span>  </span><br><span class="line">      <span class="tag">&lt;<span class="name">id</span>&gt;</span>my-proj<span class="tag">&lt;/<span class="name">id</span>&gt;</span>  </span><br><span class="line">      <span class="tag">&lt;<span class="name">username</span>&gt;</span>repo-user<span class="tag">&lt;/<span class="name">username</span>&gt;</span>  </span><br><span class="line">      <span class="tag">&lt;<span class="name">password</span>&gt;</span>repo-pwd<span class="tag">&lt;/<span class="name">password</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;/<span class="name">server</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;/<span class="name">servers</span>&gt;</span>  </span><br><span class="line">  ...  </span><br><span class="line"><span class="tag">&lt;/<span class="name">settings</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>**  注：settings.xml中server元素的id必须与POM中需要认证的repository元素的id完全一致。 **</p>
<h3 id="私服"><a href="#私服" class="headerlink" title="私服"></a>私服</h3><p>私服是架设在局域网的特殊远程仓库。一般maven自己的中央仓库存放了主流的jar包，但是有时候自己的编写的jar包就没办法在中央仓库找到了，这时候就需要配置一个远程仓库，就是所谓的“私服”，就可以将我们自己的jar包存放到远程仓库中，这样当maven需要下载jar包的时候，就可以先请求私服，如果私服上找不到，就会从maven的中央仓库再下载，之后也会缓存在私服上供以后使用。</p>
<h2 id="镜像"><a href="#镜像" class="headerlink" title="镜像"></a>镜像</h2><p>这里另外说一下镜像。国内访问国外的仓库，你懂的，所以有必要配置镜像。在maven安装目录下的settings.xml配置文件中，可以配置镜像，在<code>&lt;Mirrors&gt;</code>元素中，可配置一个或多个<code>&lt;mirror&gt;</code>镜像。比如：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">mirror</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>nexus-osc<span class="tag">&lt;/<span class="name">id</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">mirrorOf</span>&gt;</span>central<span class="tag">&lt;/<span class="name">mirrorOf</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>Nexus osc<span class="tag">&lt;/<span class="name">name</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">url</span>&gt;</span>http://maven.oschina.net/content/groups/public/<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mirror</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><code>mirrorOf</code>中写的是哪个仓库的镜像，例子中表明是中央仓库的镜像，这样，一切往中央仓库的请求，都会转发到去请求这个镜像的地址。所以，<code>mirrorOf</code>中不能乱写。<br>镜像也可以配置基于id的仓库认证。<br><code>mirrorOf</code>中可以写<code>*</code>,表示对一切的请求都会转到这个镜像。</p>
<p>** 为了满足复杂的规则，maven支持高级的镜像配置： **</p>
<ol>
<li><code>&lt;mirrorOf&gt;*&lt;mirrorOf&gt;</code>: 匹配所有仓库</li>
<li><code>&lt;mirrorOf&gt;external:*&lt;mirrorOf&gt;</code>: 匹配所有不在本机上的远程仓库</li>
<li><code>&lt;mirrorOf&gt;repo1,repo2&lt;mirrorOf&gt;</code>: 匹配repo1和repo2，多个仓库用逗号隔开</li>
<li><code>&lt;mirrorOf&gt;*,!repo3&lt;mirrorOf&gt;</code>: 匹配所有仓库，除了repo3</li>
</ol>
<p><strong><code>*</code>匹配所有，这个镜像建议放在最后，否则maven循环匹配，匹配到第一个就是*，满足，直接就结束了，其他镜像就不生效了。</strong></p>
<h2 id="仓库搜索"><a href="#仓库搜索" class="headerlink" title="仓库搜索"></a>仓库搜索</h2><p>在工作中，我们需要知道摸个依赖的坐标来使用，可以使用仓库搜索。<br>附上几个常用的搜索地址：</p>
<ul>
<li><a href="https://repository.sonatype.org/" target="_blank" rel="noopener">https://repository.sonatype.org/</a></li>
<li><a href="http://mvnbrowser.com/" target="_blank" rel="noopener">http://mvnbrowser.com/</a></li>
<li><a href="http://mvnrepository.com/" target="_blank" rel="noopener">http://mvnrepository.com/</a> </li>
</ul>
<h1 id="最后总结："><a href="#最后总结：" class="headerlink" title="最后总结："></a>最后总结：</h1><p>在pom中加入依赖声明后，maven会优先根据坐标去我们本地仓库查找，如果找到就用了。如果找不到，就会默认去中央仓库查找，然后下载到本地，然后使用。如果我们配置了私服或者其他远程仓库，就会从远程仓库查找，下载。如果我们有配置对应的镜像，就会从镜像下载使用了。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Maven</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringMVC重定向传参数的实现</title>
    <url>/2016/10/23/SpringMVC%E9%87%8D%E5%AE%9A%E5%90%91%E4%BC%A0%E5%8F%82%E6%95%B0%E7%9A%84%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<p><img src="http://upload-images.jianshu.io/upload_images/4059338-e1cf893b6354d07f.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="SpringMVC"></p>
<p>在spring的一个controller中要把参数传到页面，只要配置视图解析器，把参数添加到Model中，在页面用el表达式就可以取到。但是，这样使用的是forward方式，浏览器的地址栏是不变的，如果这时候浏览器F5刷新，就会造成表单重复提交的情况。所以，我们可以使用重定向的方式，改变浏览器的地址栏，防止表单因为刷新重复提交。</p>
<a id="more"></a>
<blockquote>
<p>jsp文件：</p>
</blockquote>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">%@</span> <span class="attr">page</span> <span class="attr">language</span>=<span class="string">"java"</span> <span class="attr">contentType</span>=<span class="string">"text/html; charset=UTF-8"</span></span></span><br><span class="line"><span class="tag">    <span class="attr">pageEncoding</span>=<span class="string">"UTF-8"</span>%&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">%@</span> <span class="attr">taglib</span> <span class="attr">prefix</span>=<span class="string">"c"</span> <span class="attr">uri</span>=<span class="string">"http://java.sun.com/jsp/jstl/core"</span> %&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">html</span> <span class="meta-keyword">PUBLIC</span> <span class="meta-string">"-//W3C//DTD HTML 4.01 Transitional//EN"</span> <span class="meta-string">"http://www.w3.org/TR/html4/loose.dtd"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"Content-Type"</span> <span class="attr">content</span>=<span class="string">"text/html; charset=UTF-8"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">title</span>&gt;</span>login<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">	</span><br><span class="line">	<span class="tag">&lt;<span class="name">form</span> <span class="attr">id</span>=<span class="string">"form1"</span> <span class="attr">action</span>=<span class="string">"/demo/user/login"</span> <span class="attr">method</span>=<span class="string">"post"</span>&gt;</span></span><br><span class="line">		账号:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"name"</span> /&gt;</span><span class="tag">&lt;/<span class="name">br</span>&gt;</span></span><br><span class="line">		密码:<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"password"</span> <span class="attr">name</span>=<span class="string">"password"</span> /&gt;</span><span class="tag">&lt;/<span class="name">br</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"submit"</span> <span class="attr">value</span>=<span class="string">"submit"</span>/&gt;</span></span><br><span class="line">		</span><br><span class="line">	<span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>controller：</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo.controller;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Map;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.ui.Model;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestParam;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> lpj</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2016年7月10日</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/user"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DemoController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@RequestMapping</span>(<span class="string">"/login"</span>)</span><br><span class="line">	<span class="function"><span class="keyword">public</span> String <span class="title">login</span><span class="params">(@RequestParam Map&lt;String, String&gt; user, Model model)</span> </span>&#123;</span><br><span class="line">		System.out.println(<span class="string">"用户提交了一次表单"</span>);</span><br><span class="line">		String username;</span><br><span class="line">		<span class="keyword">if</span> (user.get(<span class="string">"name"</span>).isEmpty()) &#123;</span><br><span class="line">			username = <span class="string">"Tom"</span>;</span><br><span class="line">		&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">			username = user.get(<span class="string">"name"</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		model.addAttribute(<span class="string">"msg"</span>, username);</span><br><span class="line"><span class="comment">//		  return "home";//此方式跳转，页面刷新会重复提交表单</span></span><br><span class="line">		<span class="keyword">return</span> <span class="string">"redirect:/home.jsp"</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<p>由于重定向相当于2次请求，所以无法把参数加在model中传过去。在上面例子中，页面获取不到msg参数。要想获取参数，可以手动拼url，把参数带在后面。<br>Spring 3.1 提供了一个很好用的类：RedirectAttributes。 使用这个类，我们可以把参数随着重定向传到页面，不需自己拼url了。<br>把上面方法参数中的Model换成RedirectAttributes，参数就自动跟在url后了。<br><img src="http://img.blog.csdn.net/20160710205655347?watermark/2/text/aHR0cDovL2Jsb2cuY3Nkbi5uZXQv/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70/gravity/Center" alt="img-1"></p>
<hr>
<p>但是，这样页面不能用el获取到，还要另外处理，所以，我们还有一种方式，不拼url，用el获取参数，就像普通转发一样。<br>还是使用RedirectAttributes，但是这次不用addAttribute方法，spring为我们准备了新方法，addFlashAttribute（）。<br>这个方法原理是放到session中，session在跳到页面后马上移除对象。所以你刷新一下后这个值就会丢失。<br>controller代码改为如下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.demo.controller;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Map;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.ui.Model;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.ModelAttribute;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestParam;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.servlet.mvc.support.RedirectAttributes;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> lpj</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2016年7月10日</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/user"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DemoController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@RequestMapping</span>(<span class="string">"/login"</span>)</span><br><span class="line"><span class="comment">//	public String login(@RequestParam Map&lt;String, String&gt; user, Model model) &#123;</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> String <span class="title">login</span><span class="params">(@RequestParam Map&lt;String, String&gt; user, RedirectAttributes model)</span> </span>&#123;</span><br><span class="line">		System.out.println(<span class="string">"用户提交了一次表单"</span>);</span><br><span class="line">		String username;</span><br><span class="line">		<span class="keyword">if</span> (user.get(<span class="string">"name"</span>).isEmpty()) &#123;</span><br><span class="line">			username = <span class="string">"Tom"</span>;</span><br><span class="line">		&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">			username = user.get(<span class="string">"name"</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		model.addFlashAttribute(<span class="string">"msg"</span>, username);</span><br><span class="line"><span class="comment">//		return "home";//此方式跳转，页面刷新会重复提交表单</span></span><br><span class="line">		<span class="keyword">return</span> <span class="string">"redirect:/user/toHome"</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="meta">@RequestMapping</span>(<span class="string">"/toHome"</span>)</span><br><span class="line">	<span class="function"><span class="keyword">public</span> String <span class="title">home</span><span class="params">(@ModelAttribute(<span class="string">"msg"</span>)</span> String msg, Model model) </span>&#123;</span><br><span class="line">		System.out.println(<span class="string">"拿到重定向得到的参数msg:"</span> + msg);</span><br><span class="line">		model.addAttribute(<span class="string">"msg"</span>, msg);</span><br><span class="line">		<span class="keyword">return</span> <span class="string">"home"</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这边我们使用@ModelAttribute注解，获取之前addFlashAttribute添加的数据，之后就可以正常使用啦。<br>需要例子代码的可以点此下载：<a href="http://download.csdn.net/detail/u011851478/9572366" target="_blank" rel="noopener">demo</a></p>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot简单入门</title>
    <url>/2016/10/21/SpringBoot%E7%AE%80%E5%8D%95%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<p>今天学学springboot，springboot是spring4新出的，目的在于减少配置，加快开发速度，springboot中内嵌了tomcat等。<br>来看一个简单的hello world 的小demo。<br><img src="http://upload-images.jianshu.io/upload_images/4059338-afe9c33c9297d869.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="著名的Hello World"></p>
<h1 id="新建一个maven项目"><a href="#新建一个maven项目" class="headerlink" title="新建一个maven项目"></a>新建一个maven项目</h1><blockquote>
<p>pom.xml</p>
</blockquote>
<a id="more"></a>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span> <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span> <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lpj<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>springboot<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">  </span><br><span class="line">  <span class="comment">&lt;!-- 继承springboot --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.3.5.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line">  </span><br><span class="line">  <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">  	<span class="comment">&lt;!-- 添加springWeb支持 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line">  </span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>我用的是目前比较新的版本，要求jdk1.8</p>
<h1 id="下面我们来输出hello-world"><a href="#下面我们来输出hello-world" class="headerlink" title="下面我们来输出hello world"></a>下面我们来输出hello world</h1><p>新建一个DemoController：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> controller;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.SpringApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.autoconfigure.EnableAutoConfiguration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.ResponseBody;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> lpj</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2016年6月10日</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="meta">@EnableAutoConfiguration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DemoController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@RequestMapping</span>(<span class="string">"/"</span>)</span><br><span class="line">    <span class="meta">@ResponseBody</span></span><br><span class="line">    <span class="function">String <span class="title">home</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Hello World!"</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        SpringApplication.run(DemoController<span class="class">.<span class="keyword">class</span>, <span class="title">args</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>@EnableAutoConfiguration是开启默认配置参数，就是之前我们pom中spring-boot-starter-parent自带的，tomcat也内嵌了，<br>所以直接run as Java Application，就ok了。如图<br><img src="http://img.blog.csdn.net/20160610205646914?watermark/2/text/aHR0cDovL2Jsb2cuY3Nkbi5uZXQv/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70/gravity/Center" alt="这里写图片描述"></p>
<p>然后打开浏览器，输入localhost:8080，回车，就看到结果了</p>
<p>PS：</p>
<ul>
<li>在resource目录下，我们可以添加一个叫 banner.txt的文件，这样当项目启动的时候，会自动加载这个文件，在控制台输出banner.txt文件中的内容。当然，如果不想看到banner，也可以关闭，方法如下：</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;  </span><br><span class="line"><span class="comment">//        SpringApplication.run(DemoController.class, args);  </span></span><br><span class="line">        SpringApplication application = <span class="keyword">new</span> SpringApplication(DemoController<span class="class">.<span class="keyword">class</span>)</span>;  </span><br><span class="line">        application.setShowBanner(<span class="keyword">false</span>);  </span><br><span class="line">        application.run(args);  </span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>或者使用链式语法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;  </span><br><span class="line">        <span class="comment">// SpringApplication.run(DemoController.class, args);  </span></span><br><span class="line">        <span class="comment">// SpringApplication application = new SpringApplication(DemoController.class);  </span></span><br><span class="line">        <span class="comment">// application.setShowBanner(false);  </span></span><br><span class="line">        <span class="comment">// application.run(args);  </span></span><br><span class="line">        <span class="keyword">new</span> SpringApplicationBuilder().showBanner(<span class="keyword">false</span>).sources(DemoController<span class="class">.<span class="keyword">class</span>).<span class="title">run</span>(<span class="title">args</span>)</span>;  </span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>有一点需要注意，如果run方法和controller分开放，项目入口文件【SpringApplication.run方法所在的那个类】所在的包名，必须要是controller，service，entity等等的包名的上级目录，否则会导致项目启动后无法正确显示显示页面。因为SpringApplication.run()启动时，会扫面同目录以及子目录下的@Controller，@Service，@Repository，@Componet等注解标识的类，如果不在目录不是父子关系，会识别不到，导致问题出现。</li>
</ul>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>Hibernate Validator学习笔记</title>
    <url>/2016/10/21/Hibernate%20Validator%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p>Hibernate Validator 是 Bean Validation 的参考实现 。Hibernate Validator 提供了 <code>JSR 303</code> 规范中所有内置 constraint 的实现，除此之外还有一些附加的 constraint。<br>在日常开发中，Hibernate Validator经常用来验证bean的字段，基于注解，方便快捷高效。</p>
<a id="more"></a>
<h1 id="Bean-Validation-中内置的-constraint"><a href="#Bean-Validation-中内置的-constraint" class="headerlink" title="Bean Validation 中内置的 constraint"></a>Bean Validation 中内置的 constraint</h1><table>
<thead>
<tr>
<th>注解</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>@Valid</td>
<td>被注释的元素是一个对象，需要检查此对象的所有字段值</td>
</tr>
<tr>
<td>@Null</td>
<td>被注释的元素必须为 null</td>
</tr>
<tr>
<td>@NotNull</td>
<td>被注释的元素必须不为 null</td>
</tr>
<tr>
<td>@AssertTrue</td>
<td>被注释的元素必须为 true</td>
</tr>
<tr>
<td>@AssertFalse</td>
<td>被注释的元素必须为 false</td>
</tr>
<tr>
<td>@Min(value)</td>
<td>被注释的元素必须是一个数字，其值必须大于等于指定的最小值</td>
</tr>
<tr>
<td>@Max(value)</td>
<td>被注释的元素必须是一个数字，其值必须小于等于指定的最大值</td>
</tr>
<tr>
<td>@DecimalMin(value)</td>
<td>被注释的元素必须是一个数字，其值必须大于等于指定的最小值</td>
</tr>
<tr>
<td>@DecimalMax(value)</td>
<td>被注释的元素必须是一个数字，其值必须小于等于指定的最大值</td>
</tr>
<tr>
<td>@Size(max, min)</td>
<td>被注释的元素的大小必须在指定的范围内</td>
</tr>
<tr>
<td>@Digits (integer, fraction)</td>
<td>被注释的元素必须是一个数字，其值必须在可接受的范围内</td>
</tr>
<tr>
<td>@Past</td>
<td>被注释的元素必须是一个过去的日期</td>
</tr>
<tr>
<td>@Future</td>
<td>被注释的元素必须是一个将来的日期</td>
</tr>
<tr>
<td>@Pattern(value)</td>
<td>被注释的元素必须符合指定的正则表达式</td>
</tr>
</tbody></table>
<h1 id="Hibernate-Validator-附加的-constraint"><a href="#Hibernate-Validator-附加的-constraint" class="headerlink" title="Hibernate Validator 附加的 constraint"></a>Hibernate Validator 附加的 constraint</h1><table>
<thead>
<tr>
<th>注解</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>@Email</td>
<td>被注释的元素必须是电子邮箱地址</td>
</tr>
<tr>
<td>@Length(min=, max=)</td>
<td>被注释的字符串的大小必须在指定的范围内</td>
</tr>
<tr>
<td>@NotEmpty</td>
<td>被注释的字符串的必须非空</td>
</tr>
<tr>
<td>@Range(min=, max=)</td>
<td>被注释的元素必须在合适的范围内</td>
</tr>
<tr>
<td>@NotBlank</td>
<td>被注释的字符串的必须非空</td>
</tr>
<tr>
<td>@URL(protocol=,host=,    port=, regexp=, flags=)</td>
<td>被注释的字符串必须是一个有效的url</td>
</tr>
<tr>
<td>@CreditCardNumber</td>
<td>被注释的字符串必须通过Luhn校验算法，银行卡，信用卡等号码一般都用Luhn计算合法性</td>
</tr>
<tr>
<td>@ScriptAssert(lang=, script=, alias=)</td>
<td>要有Java Scripting API 即JSR 223 (“Scripting for the JavaTM Platform”)的实现</td>
</tr>
<tr>
<td>@SafeHtml(whitelistType=, additionalTags=)</td>
<td>classpath中要有jsoup包</td>
</tr>
</tbody></table>
<p>hibernate补充的注解中，最后3个不常用，可忽略。<br>主要区分下<code>@NotNull</code>,<code>@NotEmpty</code>,<code>@NotBlank</code> 3个注解的区别：</p>
<ul>
<li>@NotNull：任何对象的value不能为null</li>
<li>@NotEmpty：集合对象的元素不为0，即集合不为空，也可以用于字符串不为null</li>
<li>@NotBlank：只能用于字符串不为null，并且字符串trim()以后length要大于0</li>
</ul>
<p>举个使用的例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;  </span><br><span class="line">      </span><br><span class="line">    <span class="meta">@NotBlank</span>  </span><br><span class="line">    <span class="keyword">private</span> String name;  </span><br><span class="line">      </span><br><span class="line">    <span class="comment">//年龄要大于18岁  </span></span><br><span class="line">    <span class="meta">@Min</span>(<span class="number">18</span>)  </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;  </span><br><span class="line">  </span><br><span class="line">    <span class="meta">@Email</span>  </span><br><span class="line">    <span class="keyword">private</span> String email;  </span><br><span class="line">      </span><br><span class="line">    <span class="comment">//嵌套验证  </span></span><br><span class="line">    <span class="meta">@Valid</span>  </span><br><span class="line">    <span class="keyword">private</span> Product products;  </span><br><span class="line">      </span><br><span class="line">    ... <span class="comment">//省略getter，setter  </span></span><br><span class="line">&#125;  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Product</span> </span>&#123;  </span><br><span class="line">      </span><br><span class="line">    <span class="meta">@NotBlank</span>  </span><br><span class="line">    <span class="keyword">private</span> String name;  </span><br><span class="line">      </span><br><span class="line">    <span class="comment">//价格在10元-50元之间  </span></span><br><span class="line">    <span class="meta">@Range</span>(min=<span class="number">10</span>,max=<span class="number">50</span>)  </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> price;  </span><br><span class="line">      </span><br><span class="line">    ... <span class="comment">//省略getter，setter  </span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Hibernate</tag>
      </tags>
  </entry>
  <entry>
    <title>使用JPress快速搭建系统</title>
    <url>/2016/10/19/%E4%BD%BF%E7%94%A8JPress%E5%BF%AB%E9%80%9F%E6%90%AD%E5%BB%BA%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<p><code>JPress</code>，一个<code>wordpress</code>的java代替版本，使用<code>JFinal</code>开发。支持类似<code>wordpress</code>的几乎所有功能，比如：模板，插件等。同时在模板上，<code>JPress</code>提出了“模板即模型”的概念，方便模板制作人灵活制作业务模型，移除了<code>widget</code>等繁杂功能，同时在模板和插件制作上比<code>wordpress</code>更加灵活简洁。<br>具体介绍见 <a href="http://jpress.io/" target="_blank" rel="noopener">官网</a></p>
<a id="more"></a>
<h1 id="下载JPress"><a href="#下载JPress" class="headerlink" title="下载JPress"></a>下载JPress</h1><p>首先下载jpress，附上地址：<a href="https://github.com/JpressProjects/jpress" target="_blank" rel="noopener">https://github.com/JpressProjects/jpress</a><br>下载完成后，解压出来，如图<br><img src="http://upload-images.jianshu.io/upload_images/4059338-374c1458da755c56?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
<h1 id="导入JPress"><a href="#导入JPress" class="headerlink" title="导入JPress"></a>导入JPress</h1><p>导入项目，我们只要导入jpress即可，第一个请忽略，我这边使用的eclipse，如图<br><img src="http://upload-images.jianshu.io/upload_images/4059338-33577e58931b5071?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
<h1 id="安装JPress"><a href="#安装JPress" class="headerlink" title="安装JPress"></a>安装JPress</h1><p>在项目上右击，run as -&gt; maven install 安装jpress，成功后如图<br><img src="http://upload-images.jianshu.io/upload_images/4059338-f2f94707e7e47b0d?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
<p>安装过程maven如果报错需要jdk版本，可以参考此 <a href="http://www.jianshu.com/p/0daa5abf8dea" target="_blank" rel="noopener">解决方案</a></p>
<h1 id="启动JPress"><a href="#启动JPress" class="headerlink" title="启动JPress"></a>启动JPress</h1><p>在项目上右击，run as -&gt; run configurations，在左侧Maven Build右键，new一个build脚本<br><img src="http://upload-images.jianshu.io/upload_images/4059338-2821066cb1bcbcd3?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
<ul>
<li>[1] 名字随意起一个</li>
<li>[2] 选择workspace中的jpress-web模块</li>
<li>[3] 名字随意，建议后面带上-war，表示打包成war</li>
<li>[4] 建议勾上</li>
<li>然后apply，之后run</li>
<li>看到server启动后，浏览器中输入 localhost:8080，看到如下图<br><img src="http://upload-images.jianshu.io/upload_images/4059338-0b9a950bc4c50437?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></li>
</ul>
<h1 id="配置数据库"><a href="#配置数据库" class="headerlink" title="配置数据库"></a>配置数据库</h1><p>点击上图的链接，即可开始配置<br>在配置之前，需要先在MySQL数据库中建好数据库，与配置的名字一致，数据库地址就写本地127.0.0.1。<br>之后就下一步，配置登录账号密码，即可完成。<br>此处根据提示，配置好没有自动刷新，可到eclipse后台，把server停了，在启动一下，页面就会刷新，可以登录了。</p>
<h1 id="管理自己的后台"><a href="#管理自己的后台" class="headerlink" title="管理自己的后台"></a>管理自己的后台</h1><p>首页会看到我们自己定义的名字，这时候，要进入后台去发表文章等操作，<br>在网址处输入<a href="http://localhost:8080/jpress-web/你的登录名" target="_blank" rel="noopener">http://localhost:8080/jpress-web/你的登录名</a> ，即可进入后台。<br>例如，我用户名为admin，所以我输入<br><a href="http://localhost:8080/jpress-web/admin" target="_blank" rel="noopener">http://localhost:8080/jpress-web/admin</a> 即可进入后台进行操作了。</p>
]]></content>
      <categories>
        <category>日常记录</category>
      </categories>
      <tags>
        <tag>JPress</tag>
      </tags>
  </entry>
  <entry>
    <title>Maven报错Please ensure you are using JDK 1.4 or above and not a JRE解决方法</title>
    <url>/2016/10/17/Maven%E6%8A%A5%E9%94%99Please%20ensure%20you%20are%20using%20JDK%201.4%20or%20above%20and%20not%20a%20JRE%E8%A7%A3%E5%86%B3%E6%96%B9%E6%B3%95/</url>
    <content><![CDATA[<p>在eclipse下用maven编译时，可能报以下提示 </p>
<blockquote>
<p>Please ensure you are using JDK 1.4 or above and not a JRE (the com.sun.tools.javac.Main class is required). </p>
</blockquote>
<p><strong>原因</strong>：eclipse默认是使用jre作为运行环境，而maven编译需要jdk作为运行环境。<br>因此，我们只要设置为jdk即可。</p>
<p><strong>具体步骤：</strong></p>
<a id="more"></a>
<ul>
<li><p>window -&gt; preferences -&gt; Java -&gt; installed jres，点击add，新加一个环境，设为默认环境，注意location选择为你的jdk目录。<br><img src="http://upload-images.jianshu.io/upload_images/4059338-d8c921a0bdfd3dd4?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
</li>
<li><p>之后，在你的项目上右击，build path -&gt; configure build path，编辑JRE System Library，选择刚才新建的默认环境<br><img src="http://upload-images.jianshu.io/upload_images/4059338-dd63617812a10ad6?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
</li>
</ul>
<p>这样就解决问题了。</p>
]]></content>
      <categories>
        <category>日常记录</category>
      </categories>
      <tags>
        <tag>Maven</tag>
        <tag>Eclipse</tag>
      </tags>
  </entry>
  <entry>
    <title>使用VMWare WorkStation安装Linux Mint</title>
    <url>/2016/10/16/%E4%BD%BF%E7%94%A8VMWare%20WorkStation%E5%AE%89%E8%A3%85Linux%20Mint/</url>
    <content><![CDATA[<p>Mint是Linux的一个衍生版，基于Ubuntu，是一款开源的桌面版Linux系统，适合新手学习Linux。同时，也带有集成了媒体播放的功能，对于不玩游戏的人来说也是OK的。<br>下面我们来安装这个系统。<br>如果想要直接装在物理机上，可以在官网下载好iso镜像文件后，刻录在u盘中，与普通windows系统安装一样。<br>PS：官网地址  <a href="https://www.linuxmint.com/download.php" target="_blank" rel="noopener">https://www.linuxmint.com/download.php</a><br>我这边选择安装在VMWare12虚拟机上。</p>
<a id="more"></a>
<ol>
<li><p>打开VMWare，选择新建虚拟机<br><img src="http://upload-images.jianshu.io/upload_images/4059338-d092b1d644878be7?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
</li>
<li><p>选择典型安装，这边选择下载好的iso文件，会提示不识别系统版本，没关系，下一步<br><img src="http://upload-images.jianshu.io/upload_images/4059338-e93dc752f3b2832e?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
</li>
<li><p>确认系统版本，如图<br><img src="http://upload-images.jianshu.io/upload_images/4059338-5e4467a3e99a4f96?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
</li>
<li><p>然后给系统起个名字，选择安装的位置，然后下一步</p>
</li>
<li><p>分配容量，20G够了，就默认选项，下一步，完成</p>
</li>
<li><p>此时，虚拟机配置好了，接下来开始安装，先点左边的开启虚拟机<br><img src="http://upload-images.jianshu.io/upload_images/4059338-f5813b44bba87666?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
</li>
<li><p>此时进入到安装桌面，点击桌面的install开始安装<br><img src="http://upload-images.jianshu.io/upload_images/4059338-74352339555972a3?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
</li>
<li><p>稍等一会，等他加载完成，先选择语言，这个随意，拉到底，可以选简体中文<br><img src="http://upload-images.jianshu.io/upload_images/4059338-4cfc8573561b4bfd?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
</li>
<li><p>安装类型，由于我装虚拟机，所以不能清除磁盘，选其他，如果直接装物理机，则默认安装即可<br><img src="http://upload-images.jianshu.io/upload_images/4059338-9978bda43a6ddbd5?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
</li>
<li><p>然后点击新建分区，选中空闲分区，点击小+号，添加交换空间，分配1G<br><img src="http://upload-images.jianshu.io/upload_images/4059338-4d50e1a6820cc18a?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
</li>
<li><p>继续分配剩下的，作为主分区，格式一般选ext4（个人习惯，xfs，btrf也可以），挂载点选  /<br><img src="http://upload-images.jianshu.io/upload_images/4059338-5200620009dc186f?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
</li>
<li><p>点击现在安装，设置地点，键盘布局，就默认好了，键盘习惯用英语（美国），然后设置个人用户名，密码，之后就开始安装系统了。<br>安装过程会比较久，其中下载语言包这部可以skip，节约时间，其他就不用操作，等着就好了。<br>说LinuxMint是开箱即用的系统，不无道理，系统装完后，flash、Java就已预装，虽然只是两个小程序，但这样的设计为用户节省了宝贵的时间。<br>同时，Mint也自带了图片查看器，音乐播放器，视频播放器，办公软件，邮箱系统等等。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>日常记录</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>VMWare WorkStation</tag>
      </tags>
  </entry>
  <entry>
    <title>关于VBA Excel开发中连接MySQL数据库的问题</title>
    <url>/2016/10/15/%E5%85%B3%E4%BA%8EVBA%20Excel%E5%BC%80%E5%8F%91%E4%B8%AD%E8%BF%9E%E6%8E%A5MySQL%E6%95%B0%E6%8D%AE%E5%BA%93%E7%9A%84%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<p>这两天把Access移植到MySQL上，遇到个问题，始终报错说—–80004005 - 未找到数据源名称并且未指定默认驱动程序。<br>于是上网查，折腾了一整天，最后发现原来是odbc的驱动的问题。<br>我电脑是64位的，要用64bit的odbc数据源来配置，不能直接在控制面板下的管理工具中配。直接配的后果就是excel中找不到。</p>
<a id="more"></a>
<hr>
<p>做法就是：运行 C:\Windows\SysWOW64\odbcad32.exe，在这里配，就解决问题了。<br>还有就是，我用64位的odbc驱动竟然没有这个选项，用32位却成功了。<br>最后得出一个结论，office是x86还是x64无所谓，但是MySQL与odbc驱动的版本一定要配套，不然容易出错。<br>在vba代码中，驱动名一定要与配置odbc数据源的名字相同，不然也会找不到。<br>最后贴出我的代码，供大家参考下。</p>
<figure class="highlight vb"><table><tr><td class="code"><pre><span class="line"><span class="keyword">Sub</span> connMySql()</span><br><span class="line">    <span class="keyword">Dim</span> conn <span class="keyword">As</span> ADODB.Connection</span><br><span class="line">    <span class="keyword">Dim</span> rs <span class="keyword">As</span> ADODB.Recordset</span><br><span class="line">    <span class="keyword">Dim</span> SQL <span class="keyword">As</span> <span class="built_in">String</span></span><br><span class="line">    <span class="keyword">Set</span> conn = <span class="keyword">New</span> ADODB.Connection</span><br><span class="line"></span><br><span class="line">    sevip = <span class="string">"localhost"</span>    <span class="comment">'主机IP</span></span><br><span class="line">    Db = <span class="string">"loandb"</span>    <span class="comment">'将插入的 DataBase</span></span><br><span class="line">    user = <span class="string">"root"</span></span><br><span class="line">    pwd = <span class="string">"admin"</span></span><br><span class="line">    </span><br><span class="line">    conn.Open <span class="string">"DRIVER=&#123;MySQL ODBC 5.3 Unicode Driver&#125;;SERVER="</span> &amp; sevip &amp; <span class="string">";Database="</span> &amp; Db &amp; <span class="string">";Uid="</span> &amp; user &amp; <span class="string">";PWD="</span> &amp; pwd</span><br><span class="line">    conn.Execute <span class="string">"create table test2(name text,pass text)"</span></span><br><span class="line">    conn.Close</span><br><span class="line"><span class="keyword">End</span> <span class="keyword">Sub</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>VBA</tag>
        <tag>MySQL</tag>
      </tags>
  </entry>
</search>
